<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.7.0/pml.xsd"
      id="111-java-maven-dependencies"
      interactive="true">
  <metadata>
    <author>Juan Antonio Breña Moral</author>
    <version>0.15.0</version>
    <license>Apache-2.0</license>
    <description>Use when you need to add or evaluate Maven dependencies that improve code quality — including nullness annotations (JSpecify), static analysis (Error Prone + NullAway), functional programming (VAVR), or architecture testing (ArchUnit) — and want a consultative, question-driven approach that adds only what you actually need. This should trigger for requests such as Add Maven dependencies; Add JSpecify nullness dependencies; Add Error Prone NullAway dependencies; Add VAVR functional dependencies; Add ArchUnit architecture testing dependencies.</description>
  </metadata>

  <title>Add Maven dependencies for improved code quality</title>
  <goal><![CDATA[
Add essential Maven dependencies that enhance code quality and safety through a consultative, question-driven approach. **This is an interactive SKILL**.

**What is covered in this Skill?**

- JSpecify: (nullness annotations, `provided` scope)
- Error Prone + NullAway: (enhanced static analysis with compile-time null checking)
- VAVR: (functional programming with Try/Either and immutable collections)
- ArchUnit: (architecture rule enforcement, `test` scope)
]]></goal>

  <constraints>
    <constraints-description>Before adding Maven dependencies, ensure the project is in a valid state. Use a consultative, question-driven flow that adds only what the user selects.</constraints-description>
    <constraint-list>
      <constraint>**MANDATORY**: Run `./mvnw validate` or `mvn validate` before any changes</constraint>
      <constraint>**SAFETY**: If validation fails, stop and ask the user to fix issues—do not proceed until resolved</constraint>
      <constraint><![CDATA[**BEFORE ASKING QUESTIONS**: Read the reference to use the exact wording and options from the template. Ask questions one-by-one in strict order (JSpecify → Enhanced Compiler Analysis (conditional) → VAVR → ArchUnit) and add only what the user selects. Use consultative language, present trade-offs, and wait for user responses before implementing]]></constraint>
    </constraint-list>
  </constraints>

  <triggers>
    <trigger-list>
        <trigger>Add Maven dependencies</trigger>
        <trigger>Add JSpecify nullness dependencies</trigger>
        <trigger>Add Error Prone NullAway dependencies</trigger>
        <trigger>Add VAVR functional dependencies</trigger>
        <trigger>Add ArchUnit architecture testing dependencies</trigger>
    </trigger-list>
  </triggers>

  <steps>
    <step number="1">
      <step-title>Validate project before changes</step-title>
      <step-content>Run `./mvnw validate` or `mvn validate` and stop if validation fails.</step-content>
    </step>
    <step number="2">
      <step-title>Read dependency reference and ask guided questions</step-title>
      <step-content>Read `references/111-java-maven-dependencies.md` and ask one-by-one in strict order: JSpecify, conditional Enhanced Compiler Analysis, VAVR, and ArchUnit.</step-content>
    </step>
    <step number="3">
      <step-title>Add only selected dependencies</step-title>
      <step-content>Implement only the dependencies and scopes chosen by the user, preserving existing pom.xml structure.</step-content>
    </step>
    <step number="4">
      <step-title>Report trade-offs and next checks</step-title>
      <step-content>Summarize what was added, why, and any recommended follow-up validations or tooling alignment.</step-content>
    </step>
  </steps>
</prompt>
