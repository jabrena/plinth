<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.7.0/pml.xsd"
      id="112-java-maven-plugins"
      interactive="true">
  <metadata>
    <author>Juan Antonio Breña Moral</author>
    <version>0.15.0</version>
    <license>Apache-2.0</license>
    <description>Use when you need to add or configure Maven plugins in your pom.xml — including quality tools (enforcer, surefire, failsafe, jacoco, pitest, spotbugs, pmd), security scanning (OWASP), code formatting (Spotless), version management, container image build (Jib), build information tracking, and benchmarking (JMH) — through a consultative, modular step-by-step approach that only adds what you actually need. This should trigger for requests such as Add Maven plugins in pom.xml; Improve Maven plugins in pom.xml.</description>
  </metadata>

  <title>Maven Plugins: pom.xml Configuration Best Practices</title>
  <goal><![CDATA[
Configure Maven plugins and profiles in pom.xml using a structured, question-driven process that preserves existing configuration. **This is an interactive SKILL**.

**What is covered in this Skill?**

Maven plugins:

- Maven Compiler
- Maven Enforcer
- Maven Surefire
- Maven Failsafe
- HTML test reports (Surefire Report, JXR)
- Maven Spotless
- Maven Flatten
- Maven Versions
- Maven Git Commit ID
- Maven Jib

Maven profiles:

- JaCoCo (code coverage)
- PiTest (mutation testing)
- Security (OWASP dependency check)
- Static analysis (SpotBugs, PMD)
- SonarQube/SonarCloud
- JMH (Java Microbenchmark Harness)
- Cyclomatic complexity
]]></goal>

  <constraints>
    <constraints-description>Before applying plugin recommendations, ensure the project is in a valid state. Use a structured, question-driven process that preserves existing configuration and adds only what the user selects.</constraints-description>
    <constraint-list>
      <constraint>**MANDATORY**: Run `./mvnw validate` or `mvn validate` before applying any plugin recommendations</constraint>
      <constraint>**SAFETY**: If validation fails, stop and ask the user to fix issues—do not proceed until resolved</constraint>
      <constraint>**SCOPE**: Begin with Step 1 (existing configuration analysis) before any changes. Never remove or replace existing plugins; only add new ones that do not conflict</constraint>
      <constraint>**BEFORE APPLYING**: Read the reference for detailed plugin configurations, XML templates, and constraints for each step</constraint>
    </constraint-list>
  </constraints>

  <triggers>
    <trigger-list>
        <trigger>Add Maven plugins in pom.xml</trigger>
        <trigger>Improve Maven plugins in pom.xml</trigger>
    </trigger-list>
  </triggers>

  <steps>
    <step number="1">
      <step-title>Validate project before plugin changes</step-title>
      <step-content>Run `./mvnw validate` or `mvn validate` and stop if validation fails.</step-content>
    </step>
    <step number="2">
      <step-title>Analyze current plugin and profile configuration</step-title>
      <step-content>Start with existing configuration analysis to identify what is already declared and avoid conflicts or replacement.</step-content>
    </step>
    <step number="3">
      <step-title>Read plugin reference and collect selections</step-title>
      <step-content>Read `references/112-java-maven-plugins.md`, then use a question-driven flow to select only needed plugins/profiles.</step-content>
    </step>
    <step number="4">
      <step-title>Add non-conflicting plugin configuration</step-title>
      <step-content>Add selected plugins and profiles without removing existing ones, preserving project structure and compatibility.</step-content>
    </step>
    <step number="5">
      <step-title>Summarize applied plugin setup</step-title>
      <step-content>Report added plugins/profiles, rationale, and recommended follow-up commands or checks.</step-content>
    </step>
  </steps>
</prompt>
