<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.7.0/pml.xsd"
      id="500-frameworks-micronaut-create-project">
  <metadata>
    <author>Juan Antonio Breña Moral</author>
    <version>0.17.0-SNAPSHOT</version>
    <license>Apache-2.0</license>
    <description>Use when you need to create a new Maven-based Micronaut 4.x project using SDKMAN-managed Java and Micronaut CLI tooling. This should trigger for requests such as Create a Micronaut Maven project; Bootstrap Micronaut project with SDKMAN; Generate a new Micronaut service; Create Micronaut 4 Maven project; Scaffold Micronaut service with Java 25.</description>
  </metadata>

  <title>Create Micronaut Maven Project</title>
  <goal><![CDATA[
Create a new Micronaut Maven project through SDKMAN-managed tooling, aligned with Java 25 and the repository's Micronaut 4.x baseline.

**What is covered in this Skill?**

- Verify SDKMAN and Java availability before project creation
- Install or select the required Java and Micronaut CLI candidates through SDKMAN
- Gather project coordinates, package name, Java version, features, and target directory
- Create a Maven project with the Micronaut CLI
- Prefer Maven wrapper commands for validation
- Verify the generated project with Maven before reporting completion
]]></goal>

  <constraints>
    <constraints-description>Project creation must be explicit, reproducible, and Maven-based.</constraints-description>
    <constraint-list>
      <constraint>**PREREQUISITE**: Verify SDKMAN is installed with `sdk version`; if missing, stop and provide setup guidance instead of installing it silently</constraint>
      <constraint>**JAVA BASELINE**: Prefer Java 25 unless the user requests another supported version</constraint>
      <constraint>**FRAMEWORK BASELINE**: Target the current Micronaut 4.x line by default</constraint>
      <constraint>**BUILD TOOL**: Generate a Maven project, not Gradle</constraint>
      <constraint>**SAFETY**: Do not overwrite an existing non-empty target directory without explicit user confirmation</constraint>
      <constraint>**VERIFY**: Run `./mvnw clean verify` from the generated project when the Maven wrapper exists; otherwise run `mvn clean verify`</constraint>
      <constraint>**BEFORE APPLYING**: Read the reference for the full SDKMAN and Micronaut CLI workflow</constraint>
    </constraint-list>
  </constraints>

  <triggers>
    <trigger-list>
      <trigger>Create a Micronaut Maven project</trigger>
      <trigger>Bootstrap Micronaut project with SDKMAN</trigger>
      <trigger>Generate a new Micronaut service</trigger>
        <trigger>Create Micronaut 4 Maven project</trigger>
        <trigger>Scaffold Micronaut service with Java 25</trigger>
    </trigger-list>
  </triggers>

  <steps>
    <step number="1"><step-title>Read reference and gather project inputs</step-title><step-content>Read `references/500-frameworks-micronaut-create-project.md`, then gather project directory, group, artifact, package name, Java version, application type, and desired Micronaut features.</step-content></step>
    <step number="2"><step-title>Verify SDKMAN-managed tooling</step-title><step-content>Check SDKMAN, Java, and Micronaut CLI availability. Install or switch candidates only after confirming the intended versions with the user.</step-content></step>
    <step number="3"><step-title>Create the Maven project</step-title><step-content>Use the Micronaut CLI to create a Maven project, keeping features explicit and the Micronaut 4.x baseline as the default.</step-content></step>
    <step number="4"><step-title>Verify and report</step-title><step-content>Run Maven verification in the generated project and summarize commands used, selected options, generated path, and any follow-up setup.</step-content></step>
  </steps>

  <references>
    <reference-list>
      <reference>references/500-frameworks-micronaut-create-project.md</reference>
    </reference-list>
  </references>
</prompt>
