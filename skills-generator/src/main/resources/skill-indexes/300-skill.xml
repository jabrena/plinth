<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.7.0/pml.xsd"
      id="300-frameworks-spring-boot-create-project">
  <metadata>
    <author>Juan Antonio Breña Moral</author>
    <version>0.17.0-SNAPSHOT</version>
    <license>Apache-2.0</license>
    <description>Use when you need to create a new Maven-based Spring Boot 4.0.x project using SDKMAN-managed Java and Spring Boot CLI tooling. This should trigger for requests such as Create a Spring Boot Maven project; Bootstrap Spring Boot project with SDKMAN; Generate a new Spring Boot service.</description>
  </metadata>

  <title>Create Spring Boot Maven Project</title>
  <goal><![CDATA[
Create a new Spring Boot Maven project through SDKMAN-managed tooling, aligned with Java 25 and the repository's Spring Boot 4.0.x baseline.

**What is covered in this Skill?**

- Verify SDKMAN and Java availability before project creation
- Install or select the required Java and Spring Boot CLI candidates through SDKMAN
- Gather project coordinates, package name, Java version, packaging, and dependencies
- Create a Maven project with Spring Initializr-backed Spring Boot CLI commands
- Prefer Maven wrapper commands for validation
- Verify the generated project with Maven before reporting completion
]]></goal>

  <constraints>
    <constraints-description>Project creation must be explicit, reproducible, and Maven-based.</constraints-description>
    <constraint-list>
      <constraint>**PREREQUISITE**: Verify SDKMAN is installed with `sdk version`; if missing, stop and provide setup guidance instead of installing it silently</constraint>
      <constraint>**JAVA BASELINE**: Prefer Java 25 unless the user requests another supported version</constraint>
      <constraint>**FRAMEWORK BASELINE**: Target Spring Boot 4.0.x by default</constraint>
      <constraint>**BUILD TOOL**: Generate a Maven project, not Gradle</constraint>
      <constraint>**SAFETY**: Do not overwrite an existing non-empty target directory without explicit user confirmation</constraint>
      <constraint>**VERIFY**: Run `./mvnw clean verify` from the generated project when the Maven wrapper exists; otherwise run `mvn clean verify`</constraint>
      <constraint>**BEFORE APPLYING**: Read the reference for the full SDKMAN and Spring Boot CLI workflow</constraint>
    </constraint-list>
  </constraints>

  <triggers>
    <trigger-list>
      <trigger>Create a Spring Boot Maven project</trigger>
      <trigger>Bootstrap Spring Boot project with SDKMAN</trigger>
      <trigger>Generate a new Spring Boot service</trigger>
    </trigger-list>
  </triggers>

  <steps>
    <step number="1"><step-title>Read reference and gather project inputs</step-title><step-content>Read `references/300-frameworks-spring-boot-create-project.md`, then gather project directory, group, artifact, package name, Java version, packaging, and initial dependencies.</step-content></step>
    <step number="2"><step-title>Verify SDKMAN-managed tooling</step-title><step-content>Check SDKMAN, Java, and Spring Boot CLI availability. Install or switch candidates only after confirming the intended versions with the user.</step-content></step>
    <step number="3"><step-title>Create the Maven project</step-title><step-content>Use Spring Boot CLI project creation backed by Spring Initializr, making Maven the selected build tool and Spring Boot 4.0.x the default baseline.</step-content></step>
    <step number="4"><step-title>Verify and report</step-title><step-content>Run Maven verification in the generated project and summarize commands used, selected options, generated path, and any follow-up setup.</step-content></step>
  </steps>

  <references>
    <reference-list>
      <reference>references/300-frameworks-spring-boot-create-project.md</reference>
    </reference-list>
  </references>
</prompt>
