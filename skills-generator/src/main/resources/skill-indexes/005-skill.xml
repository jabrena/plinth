<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.8.0/pml.xsd"
      id="005-agents-installation"
      interactive="true">
  <metadata>
    <author>Juan Antonio Breña Moral</author>
    <version>0.18.0-SNAPSHOT</version>
    <license>Apache-2.0</license>
    <description>Use when you need to install the embedded robot agents into .github/agents, .claude/agents, .cursor/agents, or .codex/agents, selecting the destination interactively and copying the embedded agent definitions from project assets. This should trigger for requests such as Install embedded agents; Bootstrap .github/agents; Bootstrap .cursor/agents; Bootstrap .claude/agents; Bootstrap .codex/agents; Copy robot agents.</description>
  </metadata>

  <title>Embedded agents installer</title>
  <goal><![CDATA[
Install a predefined set of embedded agent definitions from repository assets into a user-selected target directory. This is an interactive skill.

**What is covered in this Skill?**

- Interactive target selection (`.github/agents`, `.claude/agents`, `.cursor/agents`, or `.codex/agents`)
- Deterministic copy of all embedded agents defined via XInclude from `assets/agents`
- Idempotent re-installation with clear overwrite reporting
]]></goal>

  <constraints>
    <constraints-description>This skill installs only the embedded robot agents bundle and must ask for destination before writing files.</constraints-description>
    <constraint-list>
      <constraint>**MUST** ask the user to choose `.github/agents`, `.claude/agents`, `.cursor/agents`, or `.codex/agents` before installing</constraint>
      <constraint>**MUST** copy all embedded agent files defined in `references/005-agents-installation.md`</constraint>
      <constraint>**MUST** preserve file names from the reference content and report overwrite actions</constraint>
    </constraint-list>
  </constraints>

  <triggers>
    <trigger-list>
      <trigger>Install embedded agents</trigger>
      <trigger>Bootstrap .github/agents</trigger>
      <trigger>Bootstrap .cursor/agents</trigger>
      <trigger>Bootstrap .claude/agents</trigger>
      <trigger>Bootstrap .codex/agents</trigger>
      <trigger>Copy robot agents</trigger>
    </trigger-list>
  </triggers>

  <steps>
    <step number="1">
      <step-title>Choose destination</step-title>
      <step-content>Ask exactly one question to choose `.github/agents`, `.claude/agents`, `.cursor/agents`, or `.codex/agents` and wait for an explicit answer before copying files.</step-content>
      <step-constraints>
        <step-constraint-list>
          <step-constraint>Do not copy any file until destination is explicitly confirmed</step-constraint>
          <step-constraint>If destination is ambiguous, ask a clarification question</step-constraint>
        </step-constraint-list>
      </step-constraints>
    </step>
    <step number="2">
      <step-title>Install embedded agents</step-title>
      <step-content>Create the destination directory if needed, then copy all embedded agent files defined in the reference content, preserving filenames and warning before overwriting existing files.</step-content>
    </step>
    <step number="3">
      <step-title>Report installation result</step-title>
      <step-content>Return a concise checklist with selected destination, created/updated files, overwrite actions, and an optional verification command.</step-content>
    </step>
  </steps>
</prompt>
