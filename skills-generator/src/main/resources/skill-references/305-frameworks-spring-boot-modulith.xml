<?xml version="1.0" encoding="UTF-8"?>
<prompt>
    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.15.0</version>
        <license>Apache-2.0</license>
        <title>Spring Boot - Spring Modulith</title>
        <description>Use when you need to design, review, or improve modular monoliths with Spring Modulith in Spring Boot applications - including module package structure, ApplicationModules verification, named interfaces, allowed dependencies, domain events, @ApplicationModuleTest, Scenario-based module tests, generated documentation, actuator exposure, observability, and event publication registry choices.</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in Spring Boot, modular monolith architecture, domain-driven design, and Spring Modulith</role>

    <goal><![CDATA[
        Apply Spring Modulith as an architectural fitness tool for Spring Boot modular monoliths. Keep business capabilities in explicit application modules, expose small module APIs, hide implementation details in internal packages, verify module dependencies with `ApplicationModules`, prefer domain events over direct cross-module service calls, and test modules with `@ApplicationModuleTest`. Use Spring Modulith documentation, actuator, observability, and event publication registry features when they provide concrete operational value.

        Official references:
        - https://docs.spring.io/spring-modulith/reference/index.html
        - https://spring.io/projects/spring-modulith
    ]]></goal>

    <constraints>
        <constraints-description>
            Before applying recommendations, ensure the Spring Boot project compiles. Spring Modulith changes affect architecture, package boundaries, and test bootstrapping, so failures must be addressed before refactoring.
        </constraints-description>
        <constraint-list>
            <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
            <constraint>**PREREQUISITE**: Confirm the project is a Spring Boot application; for non-Boot Java modularity use architecture or design skills instead</constraint>
            <constraint>**BASELINE**: Align dependency choices with the active Spring Boot 4.0.x baseline and the Spring Modulith compatibility matrix</constraint>
            <constraint>**SAFETY**: If compilation fails, stop immediately and report the blocking errors</constraint>
            <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements; include module structure tests when adding Spring Modulith</constraint>
            <constraint>**BOUNDARIES**: Do not use Spring Modulith to hide cyclic, chatty, or anemic module design; use verification failures to refine package boundaries and APIs</constraint>
        </constraint-list>
    </constraints>

    <examples>
        <toc auto-generate="true" />

        <example number="1" id="spring-modulith-dependencies">
            <example-header>
                <example-title>Maven coordinates</example-title>
                <example-subtitle>Use the Spring Modulith BOM and add only the modules needed by the application</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                Spring Modulith is split into feature artifacts. Import the Spring Modulith BOM, then add the core starter and test starter first. Add runtime features such as actuator, observability, or event registry implementations only when the project will use those features. Do not pin old Modulith versions or mix unrelated generations with a Spring Boot 4.0.x application.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="xml"><![CDATA[<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-bom</artifactId>
            <version>${spring-modulith.version}</version>
            <scope>import</scope>
            <type>pom</type>
        </dependency>
    </dependencies>
</dependencyManagement>

<dependencies>
    <dependency>
        <groupId>org.springframework.modulith</groupId>
        <artifactId>spring-modulith-starter-core</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.modulith</groupId>
        <artifactId>spring-modulith-starter-test</artifactId>
        <scope>test</scope>
    </dependency>
</dependencies>]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="xml"><![CDATA[<!-- Bad: old, pinned, and no dependency-management alignment -->
<dependency>
    <groupId>org.springframework.modulith</groupId>
    <artifactId>spring-modulith-core</artifactId>
    <version>1.0.0</version>
</dependency>

<!-- Bad: adding runtime features before the app needs them -->
<dependency>
    <groupId>org.springframework.modulith</groupId>
    <artifactId>spring-modulith-actuator</artifactId>
    <version>1.0.0</version>
</dependency>]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="2" id="application-module-package-structure">
            <example-header>
                <example-title>Application module package structure</example-title>
                <example-subtitle>Let package boundaries express business capabilities</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                By default, Spring Modulith treats direct sub-packages of the main application package as application modules. Organize modules by business capability, keep public API types in the module root or named interfaces, and keep implementation details in sub-packages such as `internal`. Avoid technical top-level packages that force every feature to depend on shared service, repository, or mapper buckets.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="text"><![CDATA[src/main/java/com/acme/shop
├── ShopApplication.java
├── catalog
│   ├── CatalogApi.java
│   ├── ProductReserved.java
│   └── internal
│       ├── CatalogService.java
│       └── ProductRepository.java
├── ordering
│   ├── OrderApi.java
│   ├── OrderPlaced.java
│   └── internal
│       ├── OrderService.java
│       └── OrderRepository.java
└── fulfillment
    ├── FulfillmentApi.java
    └── internal
        └── FulfillmentService.java]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="text"><![CDATA[src/main/java/com/acme/shop
├── controller
├── service
├── repository
├── dto
└── mapper

# Bad: technical layers become global buckets and hide module ownership.]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="3" id="module-boundary-verification">
            <example-header>
                <example-title>Module boundary verification</example-title>
                <example-subtitle>Use ApplicationModules.verify() as an architecture test</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                Add a fast architecture test that builds the module model from the Spring Boot application class and verifies the dependency graph. This catches cycles, illegal access to internal packages, and unplanned coupling before it becomes application design debt.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import com.acme.shop.ShopApplication;
import org.junit.jupiter.api.Test;
import org.springframework.modulith.core.ApplicationModules;

class ModularityTests {

    @Test
    void verifiesApplicationModuleBoundaries() {
        ApplicationModules.of(ShopApplication.class).verify();
    }
}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[// Bad: module internals are injected directly from another module.
package com.acme.shop.ordering.internal;

import com.acme.shop.catalog.internal.ProductRepository;
import org.springframework.stereotype.Service;

@Service
class OrderService {
    private final ProductRepository products;

    OrderService(ProductRepository products) {
        this.products = products;
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="4" id="named-interfaces-and-allowed-dependencies">
            <example-header>
                <example-title>Named interfaces and allowed dependencies</example-title>
                <example-subtitle>Expose intentional APIs instead of whole modules</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                Use package-level annotations when a module needs more than one public surface or when allowed dependencies should be explicit. Named interfaces prevent accidental exposure of every public type in a package. Allowed dependencies make the intended module graph visible and testable.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[// src/main/java/com/acme/shop/catalog/package-info.java
@org.springframework.modulith.ApplicationModule(
    allowedDependencies = { "ordering :: spi" }
)
package com.acme.shop.catalog;

// src/main/java/com/acme/shop/ordering/spi/package-info.java
@org.springframework.modulith.NamedInterface("spi")
package com.acme.shop.ordering.spi;]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[// Bad: allow everything because the current design is tangled.
@org.springframework.modulith.ApplicationModule(type = org.springframework.modulith.ApplicationModule.Type.OPEN)
package com.acme.shop.catalog;]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="5" id="domain-events-between-modules">
            <example-header>
                <example-title>Domain events between modules</example-title>
                <example-subtitle>Publish events for decoupling instead of invoking another module's internals</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                Prefer immutable domain events for cross-module notification when the caller does not need an immediate answer. Use `ApplicationEventPublisher` from the originating module and handle events in interested modules with `@ApplicationModuleListener` or transactional event listeners. Keep event payloads stable, small, and free of persistence entities.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[package com.acme.shop.ordering;

public record OrderPlaced(String orderId, String customerId) {
}

package com.acme.shop.ordering.internal;

import com.acme.shop.ordering.OrderPlaced;
import org.springframework.context.ApplicationEventPublisher;
import org.springframework.stereotype.Service;

@Service
class OrderService {
    private final ApplicationEventPublisher events;

    OrderService(ApplicationEventPublisher events) {
        this.events = events;
    }

    void placeOrder(String orderId, String customerId) {
        // persist order first
        events.publishEvent(new OrderPlaced(orderId, customerId));
    }
}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[// Bad: ordering reaches into fulfillment internals synchronously for side effects.
import com.acme.shop.fulfillment.internal.FulfillmentService;

class OrderService {
    void placeOrder(Order order) {
        fulfillmentService.reserveWarehouseCapacity(order);
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="6" id="application-module-tests">
            <example-header>
                <example-title>Application module tests</example-title>
                <example-subtitle>Use @ApplicationModuleTest to bootstrap one module or its declared dependencies</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                Module tests should prove a module works through its exposed API while keeping coupling visible. Start with the default standalone mode. Use direct or all dependency bootstrap modes only when the scenario genuinely crosses module boundaries. If a module needs too many mocks for other modules, revisit the design and consider domain events.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[package com.acme.shop.ordering;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.modulith.test.ApplicationModuleTest;

@ApplicationModuleTest
class OrderingModuleTests {

    @Autowired OrderApi orders;

    @Test
    void placesOrderThroughModuleApi() {
        orders.placeOrder("customer-123");
    }
}]]></code-block>
                </good-example>
                <good-example>
                    <code-block language="java"><![CDATA[package com.acme.shop.ordering;

import org.junit.jupiter.api.Test;
import org.springframework.modulith.test.ApplicationModuleTest;
import org.springframework.modulith.test.Scenario;

@ApplicationModuleTest
class OrderingEventsTests {

    @Test
    void publishesOrderPlaced(Scenario scenario) {
        scenario.stimulate(() -> orders.placeOrder("customer-123"))
            .andWaitForEventOfType(OrderPlaced.class)
            .toArriveAndVerify(event -> {
                assertThat(event.customerId()).isEqualTo("customer-123");
            });
    }
}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[@SpringBootTest
class OrderingModuleTests {
    // Bad: full application bootstrap hides module boundaries and slows feedback.
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="7" id="documentation-actuator-observability">
            <example-header>
                <example-title>Documentation, actuator, and observability</example-title>
                <example-subtitle>Add runtime and documentation features deliberately</example-subtitle>
            </example-header>
            <example-description><![CDATA[
                Spring Modulith can generate architecture documentation from the module model and expose module information through Spring Boot actuator. Use these features when teams need living architecture docs or production visibility into module interactions. Keep actuator exposure behind normal security and management endpoint controls.
            ]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import com.acme.shop.ShopApplication;
import org.junit.jupiter.api.Test;
import org.springframework.modulith.core.ApplicationModules;
import org.springframework.modulith.docs.Documenter;

class ModulithDocumentationTests {

    @Test
    void writesModuleDocumentation() {
        var modules = ApplicationModules.of(ShopApplication.class);
        new Documenter(modules).writeDocumentation();
    }
}]]></code-block>
                </good-example>
                <good-example>
                    <code-block language="xml"><![CDATA[<dependency>
    <groupId>org.springframework.modulith</groupId>
    <artifactId>spring-modulith-actuator</artifactId>
    <scope>runtime</scope>
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="properties"><![CDATA[# Bad: exposing all management endpoints publicly to inspect modulith state.
management.endpoints.web.exposure.include=*]]></code-block>
                </bad-example>
            </code-examples>
        </example>
    </examples>

    <output-format>
        <output-format-list>
            <output-format-item>**ANALYZE** the Spring Boot package structure, module boundaries, cross-module dependencies, and current test strategy</output-format-item>
            <output-format-item>**RECOMMEND** Spring Modulith dependency and module modeling changes aligned with the active Spring Boot baseline</output-format-item>
            <output-format-item>**APPLY** minimal framework-aligned changes: module package boundaries, package annotations, architecture tests, domain events, and module tests</output-format-item>
            <output-format-item>**VERIFY** with compilation, module boundary tests, and `./mvnw clean verify` where feasible</output-format-item>
        </output-format-list>
    </output-format>
</prompt>
