<?xml version="1.0" encoding="UTF-8"?>
<prompt>
    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.16.0-SNAPSHOT</version>
        <license>Apache-2.0</license>
        <title>Micronaut — Database migrations (Flyway)</title>
        <description>Use when you need to add or review Flyway database migrations in a Micronaut application — including `micronaut-flyway` (or Flyway integration aligned with your Micronaut BOM), `classpath:db/migration` scripts, `V{version}__{description}.sql` naming, per-datasource Flyway configuration, and coordination with JDBC (`@511-frameworks-micronaut-jdbc`) or Micronaut Data (`@512-frameworks-micronaut-data`). Focus on repeatable, versioned schema evolution.</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in Micronaut, relational databases, and schema evolution with Flyway</role>

    <goal>
        Configure Flyway to run migrations from `src/main/resources/db/migration` against Micronaut-managed datasources, keeping the relational schema aligned with Micronaut Data entities or hand-written JDBC. Use forward-only, numbered migrations; avoid manual DDL in production except through controlled operational procedures. For raw JDBC see `@511-frameworks-micronaut-jdbc`; for repositories and `@MappedEntity` types see `@512-frameworks-micronaut-data`.
    </goal>

    <constraints>
        <constraints-description>
            Before applying any recommendations, ensure the project is in a valid state by running Maven compilation.
            Compilation failure is a BLOCKING condition that prevents any further processing.
        </constraints-description>
        <constraint-list>
            <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
            <constraint>**PREREQUISITE**: Project must compile successfully before Flyway or SQL edits</constraint>
            <constraint>**CRITICAL SAFETY**: If compilation fails, IMMEDIATELY STOP and DO NOT CONTINUE</constraint>
            <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
        </constraint-list>
    </constraints>

    <examples>
        <toc auto-generate="true" />

        <example number="1" id="micronaut-flyway-dependency">
            <example-header>
                <example-title>Maven dependency</example-title>
                <example-subtitle>Add Micronaut Flyway support alongside JDBC and your database driver</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Use the Micronaut BOM to align versions. Combine `micronaut-flyway` with `micronaut-jdbc-hikari` (or your datasource module) and a JDBC driver. The exact coordinate names follow your Micronaut minor version — verify against current Micronaut Launch / documentation when upgrading.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="xml"><![CDATA[<!-- pom.xml — illustrative; align versions with micronaut-parent / BOM -->
<dependency>
    <groupId>io.micronaut.flyway</groupId>
    <artifactId>micronaut-flyway</artifactId>
</dependency>
<dependency>
    <groupId>io.micronaut.sql</groupId>
    <artifactId>micronaut-jdbc-hikari</artifactId>
</dependency>
<dependency>
    <groupId>org.postgresql</groupId>
    <artifactId>postgresql</artifactId>
    <scope>runtime</scope>
</dependency>]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="xml"><![CDATA[<!-- Bad: Flyway without a configured DataSource bean / JDBC stack -->
<dependency>
    <groupId>io.micronaut.flyway</groupId>
    <artifactId>micronaut-flyway</artifactId>
</dependency>]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="2" id="micronaut-migration-scripts">
            <example-header>
                <example-title>Migration scripts</example-title>
                <example-subtitle>Default `classpath:db/migration` and Flyway naming</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Place SQL under `src/main/resources/db/migration`. Use versioned filenames. Align columns with `@MappedEntity` fields, including `@Version` columns for optimistic locking.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="sql"><![CDATA[-- src/main/resources/db/migration/V1__create_items.sql
CREATE TABLE items (
    id      BIGSERIAL PRIMARY KEY,
    name    VARCHAR(200) NOT NULL,
    version BIGINT NOT NULL DEFAULT 0
);]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="sql"><![CDATA[-- Bad: editing V1 after it ran in staging — checksum failure on next startup
-- (instead add V2__fix_items.sql)]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="3" id="micronaut-flyway-yaml">
            <example-header>
                <example-title>Configuration</example-title>
                <example-subtitle>Enable Flyway per datasource under `flyway.datasources.*`</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Micronaut Flyway typically scopes settings per datasource (e.g. `default`). Enable migrations for environments that should apply DDL automatically; use profiles for dev vs prod if you need different behavior. Set `baseline-on-migrate` when attaching Flyway to a database that already contains tables.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="yaml"><![CDATA[# application.yml (illustrative — keys per Micronaut Flyway module docs)
datasources:
  default:
    url: jdbc:postgresql://localhost:5432/app
    username: app
    password: app
    driver-class-name: org.postgresql.Driver
flyway:
  datasources:
    default:
      enabled: true
      locations: classpath:db/migration]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="yaml"><![CDATA[# Bad: Flyway enabled but locations point to a path with no scripts — silent no-op or failure
flyway:
  datasources:
    default:
      enabled: true
      locations: classpath:missing/migrations]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="4" id="micronaut-tests-and-flyway">
            <example-header>
                <example-title>Tests</example-title>
                <example-subtitle>Run migrations against Testcontainers-backed datasources in integration tests</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Prefer `@MicronautTest` with a Testcontainers-managed database and the same migration scripts as production. Avoid duplicating schema in hand-written `schema.sql` unless you have a deliberate, documented reason.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[// TestPropertyProvider or test resources: point default datasource to Testcontainers JDBC URL
// Flyway runs on context start when enabled — exercises real migration chain]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[// Bad: @MicronautTest with in-memory H2 and hand-written DDL while production uses PostgreSQL + Flyway
// — drifts and hides migration failures]]></code-block>
                </bad-example>
            </code-examples>
        </example>
    </examples>

    <output-format>
        <output-format-list>
            <output-format-item>**ANALYZE** Micronaut Flyway wiring: BOM dependencies, datasource config, `flyway.datasources.*`, and migration locations</output-format-item>
            <output-format-item>**CATEGORIZE** gaps (DEPENDENCY, CONFIG, SCRIPT, TEST DRIFT) by severity</output-format-item>
            <output-format-item>**APPLY** Micronaut-aligned fixes: coordinates, YAML/properties, and versioned SQL</output-format-item>
            <output-format-item>**ALIGN** with `@512-frameworks-micronaut-data` or `@511-frameworks-micronaut-jdbc` access patterns</output-format-item>
            <output-format-item>**TEST** with `@MicronautTest` and a containerized database mirroring production dialect</output-format-item>
            <output-format-item>**VALIDATE** with `./mvnw compile` before and `./mvnw clean verify` after changes</output-format-item>
        </output-format-list>
    </output-format>

    <safeguards>
        <safeguards-list>
            <safeguards-item>**IMMUTABLE APPLIED MIGRATIONS**: Do not modify files after they have run in shared environments</safeguards-item>
            <safeguards-item>**DATASOURCE SCOPE**: Configure Flyway for each datasource that needs independent schema</safeguards-item>
            <safeguards-item>**BLOCKING SAFETY CHECK**: Run `./mvnw compile` before recommending schema or build changes</safeguards-item>
        </safeguards-list>
    </safeguards>
</prompt>
