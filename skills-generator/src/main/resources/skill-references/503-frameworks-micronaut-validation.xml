<?xml version="1.0" encoding="UTF-8"?>
<prompt>
    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.16.0</version>
        <license>Apache-2.0</license>
        <title>Micronaut Validation Guidelines</title>
        <description>Use when you need to design, review, or improve validation in Micronaut applications — including Bean Validation on @Controller methods, @Body @Valid, query/path parameter validation, @ConfigurationProperties validation, custom constraints, nested DTO validation, and ExceptionHandler mapping for constraint violations.</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in Micronaut and Jakarta Bean Validation</role>

    <goal>
        Ensure Micronaut HTTP APIs validate inputs at boundaries, keep constraint definitions on DTOs and command models, and return stable validation error responses. Align with `@502-frameworks-micronaut-rest` for status codes and error bodies.
    </goal>

    <constraints>
        <constraints-description>
            Before applying recommendations, ensure the project compiles.
            Compilation failure is a blocking condition.
        </constraints-description>
        <constraint-list>
            <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
            <constraint>**SAFETY**: If compilation fails, stop immediately</constraint>
            <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
        </constraint-list>
    </constraints>

    <examples>
        <toc auto-generate="true" />

        <example number="1" id="controller-body-valid">
            <example-header>
                <example-title>Request body validation</example-title>
                <example-subtitle>@Body @Valid with constrained records</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Always pair constrained request types with `@Valid` (or `@Validated` for groups) on `@Body` parameters so Micronaut triggers Bean Validation before the controller method runs.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.HttpResponse;
import io.micronaut.http.annotation.Body;
import io.micronaut.http.annotation.Controller;
import io.micronaut.http.annotation.Post;
import jakarta.validation.Valid;
import jakarta.validation.constraints.Email;
import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.Size;

@Controller("/users")
class UserController {
    @Post
    HttpResponse<Void> create(@Body @Valid CreateUserRequest request) {
        return HttpResponse.noContent();
    }
}

record CreateUserRequest(
    @NotBlank String username,
    @NotBlank @Email String email,
    @NotBlank @Size(min = 8, max = 128) String password
) { }]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[@Post
HttpResponse<Void> create(@Body CreateUserRequest request) {
    return HttpResponse.noContent();
}
// Constraints on CreateUserRequest are not enforced without @Valid]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="2" id="micronaut-query-path-validation">
            <example-header>
                <example-title>Query and path parameters</example-title>
                <example-subtitle>@Min, @Max, @Pattern on @QueryValue and URI variables</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Validate simple parameters on the controller method. Prefer typed query values with constraints instead of parsing raw strings manually.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.Controller;
import io.micronaut.http.annotation.Get;
import io.micronaut.http.annotation.PathVariable;
import io.micronaut.http.annotation.QueryValue;
import jakarta.validation.constraints.Max;
import jakarta.validation.constraints.Min;
import jakarta.validation.constraints.Pattern;

@Controller("/items")
class ItemController {
    @Get("/{id}")
    String get(@PathVariable @Pattern(regexp = "^[a-z0-9-]+$") String id) {
        return id;
    }

    @Get
    String list(@QueryValue @Min(0) int page, @QueryValue @Min(1) @Max(100) int size) {
        return page + ":" + size;
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[@Get("/{id}")
String get(String id) {
    if (id.contains("..")) throw new IllegalArgumentException();
    return id;
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="3" id="micronaut-configuration-validated">
            <example-header>
                <example-title>@ConfigurationProperties validation</example-title>
                <example-subtitle>Fail-fast binding with Bean Validation constraints</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Put Jakarta constraints directly on `@ConfigurationProperties` values so invalid `application.yml` fails at startup. Pairs with `@501-frameworks-micronaut-core`.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.context.annotation.ConfigurationProperties;
import jakarta.validation.constraints.Max;
import jakarta.validation.constraints.Min;
import jakarta.validation.constraints.NotBlank;

@ConfigurationProperties("app.api")
record ApiSettings(
    @NotBlank String baseUrl,
    @Min(1) @Max(600) int timeoutSeconds
) { }]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[@ConfigurationProperties("app.api")
record ApiSettings(String baseUrl, int timeoutSeconds) { }
// No startup validation]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="4" id="micronaut-nested-valid">
            <example-header>
                <example-title>Nested and collection validation</example-title>
                <example-subtitle>@Valid on nested types and list elements</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Cascade validation with `@Valid` on nested fields and `List<@NotNull @Valid LineItem>` patterns.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import jakarta.validation.Valid;
import jakarta.validation.constraints.NotEmpty;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Positive;
import jakarta.validation.constraints.Size;
import java.util.List;

public record OrderRequest(
    @NotNull @Valid Address shipping,
    @NotEmpty List<@NotNull @Valid LineItem> lines
) { }

record Address(@NotNull @Size(max = 200) String street) { }

record LineItem(
    @NotNull @Size(min = 1, max = 64) String sku,
    @Positive int qty
) { }]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[public record OrderRequest(Address shipping, List<LineItem> lines) { }
// Missing @Valid: nested Address and LineItem constraints are never evaluated]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="5" id="micronaut-constraint-exception-handler">
            <example-header>
                <example-title>ConstraintViolationException handler</example-title>
                <example-subtitle>Map validation failures to HTTP 400</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Provide an `ExceptionHandler<ConstraintViolationException>` singleton that returns RFC 7807 Problem Details aligned with `@502-frameworks-micronaut-rest`: stable `type`, `title`, `status`, `detail`, `instance`, plus a `violations` extension for field-level messages. Use `HttpResponse.badRequest(body)` for concise construction and `.toList()` (Java 16+) instead of `Collectors.toList()`.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.HttpRequest;
import io.micronaut.http.HttpResponse;
import io.micronaut.http.server.exceptions.ExceptionHandler;
import jakarta.inject.Singleton;
import jakarta.validation.ConstraintViolationException;

import java.net.URI;
import java.util.List;

record ValidationProblemBody(URI type, String title, int status, String detail, URI instance, List<String> violations) { }

@Singleton
public class ConstraintViolationHandler
        implements ExceptionHandler<ConstraintViolationException, HttpResponse<ValidationProblemBody>> {

    private static final URI VALIDATION_TYPE = URI.create("https://example.com/problems/validation-error");

    @Override
    public HttpResponse<ValidationProblemBody> handle(HttpRequest request, ConstraintViolationException ex) {
        List<String> violations = ex.getConstraintViolations().stream()
            .map(v -> v.getPropertyPath() + ": " + v.getMessage())
            .toList();
        ValidationProblemBody body = new ValidationProblemBody(
            VALIDATION_TYPE,
            "Validation Error",
            400,
            "One or more request fields failed validation.",
            request.getUri(),
            violations);
        return HttpResponse.badRequest(body);
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[return HttpResponse.badRequest(
    java.util.Map.of("error", "VALIDATION_ERROR", "details", List.of(ex.getMessage())));
// Ad hoc Map shape — not RFC 7807; unstable message; may expose internal property paths]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="6" id="micronaut-custom-constraint">
            <example-header>
                <example-title>Class-level custom constraint</example-title>
                <example-subtitle>Reuse cross-field rules</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Implement `@Constraint(validatedBy = ...)` for DTO-level rules shared across endpoints.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import jakarta.validation.Constraint;
import jakarta.validation.ConstraintValidator;
import jakarta.validation.ConstraintValidatorContext;
import jakarta.validation.Payload;
import java.lang.annotation.*;

@Target(ElementType.TYPE)
@Retention(RetentionPolicy.RUNTIME)
@Constraint(validatedBy = SamePasswords.Validator.class)
@interface SamePasswords {
    String message() default "passwords must match";
    Class<?>[] groups() default {};
    Class<? extends Payload>[] payload() default {};

    class Validator implements ConstraintValidator<SamePasswords, PasswordForm> {
        @Override
        public boolean isValid(PasswordForm v, ConstraintValidatorContext ctx) {
            if (v == null) return true;
            return java.util.Objects.equals(v.password(), v.confirmPassword());
        }
    }
}

@SamePasswords
record PasswordForm(String password, String confirmPassword) { }]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[if (!p1.equals(p2)) throw new IllegalArgumentException("mismatch");]]></code-block>
                </bad-example>
            </code-examples>
        </example>
    </examples>

    <output-format>
        <output-format-list>
            <output-format-item>**ANALYZE** controllers, factories, and configuration beans for missing `@Valid`, missing cascades, and inconsistent 400 bodies</output-format-item>
            <output-format-item>**APPLY** Bean Validation at HTTP boundaries and on `@ConfigurationProperties`; centralize `ConstraintViolationException` handling</output-format-item>
            <output-format-item>**ALIGN** error responses with RFC 7807 Problem Details (`ValidationProblemBody` record) from `@502-frameworks-micronaut-rest`</output-format-item>
            <output-format-item>**VALIDATE** with `./mvnw compile` before and `./mvnw clean verify` after changes</output-format-item>
        </output-format-list>
    </output-format>

    <safeguards>
        <safeguards-list>
            <safeguards-item>Do not omit `@Valid` on constrained `@Body` parameters</safeguards-item>
            <safeguards-item>Do not return stack traces or raw validator messages to clients</safeguards-item>
            <safeguards-item>Verify validation behavior under compile-time HTTP clients and GraalVM native if applicable</safeguards-item>
        </safeguards-list>
    </safeguards>
</prompt>
