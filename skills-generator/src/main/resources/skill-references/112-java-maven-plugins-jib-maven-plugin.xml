<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xi="http://www.w3.org/2001/XInclude"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.8.0/pml.xsd">
    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.18.0-SNAPSHOT</version>
        <license>Apache-2.0</license>
        <title>Maven Plugins: pom.xml Configuration Best Practices</title>
        <description>Jib Maven plugin guidance for container image builds.</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in Java software development.</role>

    <tone>Treat the user as a knowledgeable partner. Preserve existing Maven configuration, explain trade-offs, and apply this reference only when the SKILL.md question flow selected this plugin or profile.</tone>

    <goal><![CDATA[
Configure Jib only when selected by the user in `112-java-maven-plugins`. Read this reference after the SKILL.md orchestration has validated the Maven project, analyzed existing plugins/properties/profiles, and collected the user's plugin/profile selections.
    ]]></goal>

    <constraints>
        <constraints-description>Apply only the selected Jib configuration and preserve existing project configuration.</constraints-description>
        <constraint-list>
            <constraint>Read this reference only when the user selected the corresponding Maven plugin or profile in the centralized question flow.</constraint>
            <constraint>Before adding configuration, check whether the plugin, property, reporting entry, support file, or profile already exists.</constraint>
            <constraint>Never remove or replace existing Maven configuration without explicit user confirmation.</constraint>
            <constraint>Add only the Maven properties, plugin configuration, profile configuration, and support files required by this reference.</constraint>
        </constraint-list>
    </constraints>

    <steps>
        <step number="1">
            <step-title>Add selected Maven properties</step-title>
            <step-content><![CDATA[
Add only the properties needed for this selected plugin/profile. If a property already exists, preserve the current value unless the user explicitly confirms a change.

```xml
<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
<maven.version>3.9.10</maven.version>
<java.version>[USER_SELECTED_JAVA_VERSION]</java.version>
<maven-plugin-jib.version>3.5.1</maven-plugin-jib.version>
```
            ]]></step-content>
        </step>
        <step number="2">
            <step-title>Jib Maven Plugin Configuration</step-title>
            <step-content><![CDATA[
**Purpose**: Configure Jib Maven plugin for building container images without Docker daemon, using the image name provided in the Jib target image question.

**Dependencies**: Only execute if user selected "Container image build (Jib)" in the SKILL.md question flow. Requires completion of the SKILL.md orchestration steps and selected property handling.

**CRITICAL PRESERVATION RULE**: Only ADD this plugin if it doesn't already exist. Never REPLACE or REMOVE existing plugins.

## Pre-Implementation Check

**BEFORE adding Jib plugin, check if it already exists in the pom.xml:**

If jib-maven-plugin already exists: Ask user "Jib plugin already exists. Do you want to enhance the existing configuration? (y/n)"

If user says "n": Skip this step entirely.
If user says "y": Proceed with adding missing configuration elements only.

**CONDITIONAL EXECUTION**: Only execute this step if user selected "Container image build (Jib)" in the SKILL.md question flow.

## Implementation Guidelines

1. **Replace image placeholder**: Use the target container image from Question 3.1 (e.g., `gcr.io/my-project/my-app`, `docker.io/username/myimage`, or `myimage` for local Docker)
2. **No Docker daemon required**: Jib builds images directly; useful for CI/CD and local development

## Jib Plugin Configuration

**ADD this plugin to the `<build><plugins>` section ONLY if it doesn't already exist:**

```xml
<plugin>
    <groupId>com.google.cloud.tools</groupId>
    <artifactId>jib-maven-plugin</artifactId>
    <version>${maven-plugin-jib.version}</version>
    <configuration>
        <to>
            <image>REPLACE_WITH_ACTUAL_IMAGE</image>
        </to>
    </configuration>
</plugin>
```

**Replace `REPLACE_WITH_ACTUAL_IMAGE`** with the value from Question 3.1 (e.g., `myimage`, `gcr.io/my-project/my-app`).

## Usage Examples

```bash
# Build container image (no Docker daemon required)
./mvnw compile jib:build

# Build to Docker daemon (for local testing)
./mvnw compile jib:dockerBuild

# Build image tarball
./mvnw compile jib:buildTar
```

## Validation

After adding this plugin, verify the configuration:

```bash
# Validate plugin configuration
./mvnw validate
```
                
            
            ]]></step-content>
            <step-constraints>
                <step-constraint-list>
                    <step-constraint>**MUST** only add Jib plugin if "Container image build (Jib)" was selected in the SKILL.md question flow</step-constraint>
                    <step-constraint>**MUST** check if plugin already exists before adding</step-constraint>
                    <step-constraint>**MUST** ask user permission before modifying existing plugin configuration</step-constraint>
                    <step-constraint>**MUST** use selected version properties for plugin version</step-constraint>
                    <step-constraint>**MUST** replace REPLACE_WITH_ACTUAL_IMAGE with the target container image from Question 3.1</step-constraint>
                    <step-constraint>**MUST** skip this step entirely if Container image build (Jib) was not selected</step-constraint>
                </step-constraint-list>
            </step-constraints>
        </step>
    </steps>
</prompt>
