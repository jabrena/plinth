<?xml version="1.0" encoding="UTF-8"?>
<prompt>
    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.16.0-SNAPSHOT</version>
        <license>Apache-2.0</license>
        <title>Spring Boot — Kafka messaging</title>
        <description>Use when you need Kafka with Spring Boot — including Maven dependencies (`spring-boot-starter-kafka`), typed event records, KafkaTemplate producers, @KafkaListener consumers, JSON serialization with Boot auto-configuration, retries and dead-letter topics, idempotent consumers, and integration testing with Testcontainers `@ServiceConnection` or `@EmbeddedKafka`. This should trigger for requests such as Add Kafka in Spring Boot; Review Spring Kafka consumers; Improve retries and DLT in Spring Kafka.</description>
    </metadata>
    <role>You are a Senior software engineer with extensive experience in Spring Boot and Apache Kafka</role>
    <goal><![CDATA[Design and implement reliable Kafka messaging in Spring Boot using `spring-boot-starter-kafka` and Spring Kafka auto-configuration. Prefer typed event records, keyed producers for ordered processing, and declarative error handling with dead-letter topics over silent exception swallowing. Keep listeners thin — delegate business logic to services. Guard consumers against poison messages and replay with idempotency on the eventId.

**What is covered in this Skill?**

- Maven `spring-boot-starter-kafka` dependency aligned with the Spring Boot BOM (raw `spring-kafka` only for non-Boot library modules)
- JSON event serialization via `DefaultKafkaProducerFactoryCustomizer` / `DefaultKafkaConsumerFactoryCustomizer` — do not duplicate manual `ProducerFactory` / `ConsumerFactory` beans when using `@ServiceConnection`
- Versioned event schemas as Java records with explicit `eventId`, `schemaVersion`, and aggregate key
- Topic naming conventions (`domain.entity.operation.v{N}`)
- `KafkaTemplate<K, V>` typed producer with explicit key strategy
- `@KafkaListener` consumer with explicit `groupId`, `topics`, and typed payload
- `ConcurrentKafkaListenerContainerFactory` for concurrency and batch vs. single-record modes
- `DefaultErrorHandler` with `FixedBackOff` / `ExponentialBackOff` and `DeadLetterPublishingRecoverer`
- Idempotent consumer pattern using `eventId` de-duplication store
- Kafka transactions for exactly-once producer semantics
- Integration testing with Testcontainers `@ServiceConnection` (full-stack `*IT`) and `@EmbeddedKafka` (in-process broker tests)
- `@TestConfiguration` with `NewTopic` beans imported only by integration tests

**Scope:** Apply recommendations based on the reference rules and good/bad code examples. For HTTP + Testcontainers wiring in `*IT` classes, cross-reference `@322-frameworks-spring-boot-testing-integration-tests`.
 ]]></goal>
    <constraints>
        <constraints-description>Before applying any Kafka changes, ensure the project compiles. Compilation failure is a BLOCKING condition.</constraints-description>
        <constraint-list>
            <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
            <constraint>**SAFETY**: If compilation fails, stop immediately</constraint>
            <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
            <constraint>**INJECTION**: Never build topic names or Kafka header values from untrusted user input</constraint>
            <constraint>**BEFORE APPLYING**: Read the reference for detailed rules and good/bad patterns</constraint>
            <constraint>**EDGE CASE**: If the user goal is ambiguous, stop and ask a clarifying question before editing files</constraint>
            <constraint>**EDGE CASE**: If required context, files, or tools are missing, report the blocker explicitly</constraint>
        </constraint-list>
    </constraints>
    <examples>
        <toc auto-generate="true" />
        <example number="1" id="314-frameworks-spring-kafka-maven-dependency">
            <example-header>
                <example-title>Maven dependency</example-title>
                <example-subtitle>Use spring-boot-starter-kafka in Boot apps; raw spring-kafka only in library modules</example-subtitle>
            </example-header>
            <example-description><![CDATA[Spring Boot manages Kafka versions via its BOM. In Spring Boot applications, prefer `spring-boot-starter-kafka` so `KafkaConnectionDetails`, `@ServiceConnection`, and factory customizers integrate correctly. Declaring an explicit version pins the library and can cause classpath conflicts. Use the raw `spring-kafka` artifact only in library modules that do not depend on Spring Boot auto-configuration.]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="xml"><![CDATA[<!-- pom.xml — Spring Boot application: use the starter -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-kafka</artifactId>
</dependency>
<!-- Optional for JSON event records at compile time -->
<dependency>
    <groupId>com.fasterxml.jackson.core</groupId>
    <artifactId>jackson-databind</artifactId>
</dependency>]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="xml"><![CDATA[<!-- Bad in a Boot app: raw artifact skips Boot Kafka auto-configuration;
     explicit version fights the BOM -->
<dependency>
    <groupId>org.springframework.kafka</groupId>
    <artifactId>spring-kafka</artifactId>
    <version>3.1.0</version>
</dependency>]]></code-block>
                </bad-example>
            </code-examples>
        </example>
        <example number="2" id="314-frameworks-spring-kafka-event-schema-design">
            <example-header>
                <example-title>Event schema design</example-title>
                <example-subtitle>Versioned immutable records with eventId and aggregate key</example-subtitle>
            </example-header>
            <example-description><![CDATA[Define each Kafka event as a Java record. Include a unique `eventId` (UUID) for consumer de-duplication, a `schemaVersion` string for forward compatibility, and the aggregate's natural key (e.g. `orderId`). Use the aggregate key as the Kafka message key so all events for the same aggregate land in the same partition, preserving ordering. Topic names follow the pattern `domain.entity.operation.v{N}` to allow parallel consumers on older and newer versions.]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import java.time.Instant;

// Immutable event schema — one record per event type
public record OrderCreatedEvent(
    String eventId,       // UUID; used for consumer-side de-duplication
    String schemaVersion, // "v1"; bump when non-backward-compatible fields change
    String orderId,       // aggregate key → use as Kafka message key
    String customerId,
    Instant occurredAt
) {
    public static OrderCreatedEvent of(String orderId, String customerId) {
        return new OrderCreatedEvent(
            java.util.UUID.randomUUID().toString(),
            "v1",
            orderId,
            customerId,
            Instant.now()
        );
    }
}
// topic: orders.created.v1  key: orderId]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[// Bad: generic map payload — no schema, no version, no stable key
Map<String, Object> event = Map.of(
    "type", "orderCreated",
    "data", "..."
);
// topic: events   key: null (random partition assignment)]]></code-block>
                </bad-example>
            </code-examples>
        </example>
        <example number="3" id="314-frameworks-spring-kafka-producer-implementation">
            <example-header>
                <example-title>Producer implementation</example-title>
                <example-subtitle>Typed KafkaTemplate with explicit key; handle send failure</example-subtitle>
            </example-header>
            <example-description><![CDATA[Inject a typed `KafkaTemplate&lt;String, OrderCreatedEvent&gt;` so the compiler enforces the key/value contract. Always pass the aggregate's key as the Kafka record key. Return the `CompletableFuture` to the caller (or await it at a command boundary) so broker-side send failures are observable. Do not throw from a `whenComplete` callback and assume the caller will see it.]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import org.springframework.kafka.core.KafkaTemplate;
import org.springframework.kafka.support.SendResult;
import org.springframework.stereotype.Service;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

@Service
class OrderEventPublisher {

    private static final String TOPIC = "orders.created.v1";

    private final KafkaTemplate<String, OrderCreatedEvent> template;

    OrderEventPublisher(KafkaTemplate<String, OrderCreatedEvent> template) {
        this.template = template;
    }

    CompletableFuture<SendResult<String, OrderCreatedEvent>> publish(OrderCreatedEvent event) {
        return template.send(TOPIC, event.orderId(), event)
            .exceptionally(ex -> {
                throw new CompletionException("Kafka send failed for order: " + event.orderId(), ex);
            });
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[// Bad: raw KafkaTemplate<String, String> loses type safety;
// null key loses partition ordering; ignored CompletableFuture drops send errors
@Service
class OrderEventPublisher {
    @Autowired
    KafkaTemplate<String, String> template;

    void publish(OrderCreatedEvent event) {
        template.send("events", null, event.toString()); // no key, serialized via toString()
        // CompletableFuture discarded — broker errors silently lost
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>
        <example number="4" id="314-frameworks-spring-kafka-consumer-implementation">
            <example-header>
                <example-title>Consumer implementation</example-title>
                <example-subtitle>@KafkaListener with typed payload and thin handler</example-subtitle>
            </example-header>
            <example-description><![CDATA[Annotate the listener class with `@Component`. Declare `topics` and `groupId` on the `@KafkaListener` so the consumer group is explicit and testable. Accept the typed event record as the method parameter. Delegate all business logic to an application service — the listener method should only translate the Kafka message into a service call.]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import org.springframework.kafka.annotation.KafkaListener;
import org.springframework.stereotype.Component;

@Component
class BillingEventListener {

    private final BillingService billingService;

    BillingEventListener(BillingService billingService) {
        this.billingService = billingService;
    }

    @KafkaListener(topics = "orders.created.v1", groupId = "billing-service-v1")
    void onOrderCreated(OrderCreatedEvent event) {
        billingService.createInvoice(event.orderId(), event.customerId());
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[// Bad: no groupId means a random group on each restart causing re-read from the beginning;
// String payload requires manual parsing; business logic mixed directly in listener
@KafkaListener(topics = "orders.created.v1")
void onMessage(String rawJson) {
    var orderId = rawJson.split(",")[0]; // fragile parsing
    // 50 lines of business logic inline ...
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>
        <example number="5" id="314-frameworks-spring-kafka-error-handling-and-dead-letter-topic">
            <example-header>
                <example-title>Error handling and dead-letter topic</example-title>
                <example-subtitle>DefaultErrorHandler with backoff and DeadLetterPublishingRecoverer</example-subtitle>
            </example-header>
            <example-description><![CDATA[Configure a `DefaultErrorHandler` bean that retries with exponential back-off and then routes unrecoverable messages to a dead-letter topic via `DeadLetterPublishingRecoverer`. The recoverer automatically targets `{topic}.DLT` by default. Register the handler on the container factory so it applies to all listeners. Do not swallow exceptions inside the listener — let them propagate so the container's error handler can decide.]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import org.apache.kafka.common.TopicPartition;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.kafka.core.KafkaTemplate;
import org.springframework.kafka.listener.DeadLetterPublishingRecoverer;
import org.springframework.kafka.listener.DefaultErrorHandler;
import org.springframework.util.backoff.ExponentialBackOff;

@Configuration
class KafkaErrorConfig {

    @Bean
    DefaultErrorHandler errorHandler(KafkaTemplate<Object, Object> template) {
        var recoverer = new DeadLetterPublishingRecoverer(template,
            (record, ex) -> new TopicPartition(record.topic() + ".DLT", -1));

        var backOff = new ExponentialBackOff(500L, 2.0);
        backOff.setMaxElapsedTime(30_000L); // stop retrying after 30 s

        return new DefaultErrorHandler(recoverer, backOff);
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[// Bad: swallowing the exception causes acknowledgement of poison messages;
// they are never retried and never routed to a DLT
@KafkaListener(topics = "orders.created.v1", groupId = "billing-service-v1")
void onOrderCreated(OrderCreatedEvent event) {
    try {
        billingService.createInvoice(event.orderId(), event.customerId());
    } catch (Exception ignored) {
        // silent: message acknowledged, data loss guaranteed
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>
        <example number="6" id="314-frameworks-spring-kafka-idempotent-consumer">
            <example-header>
                <example-title>Idempotent consumer</example-title>
                <example-subtitle>De-duplicate on eventId to survive retries and rebalances</example-subtitle>
            </example-header>
            <example-description><![CDATA[Kafka at-least-once delivery means a consumer can receive the same message more than once — on broker retries, after a rebalance, or when consuming from a DLT. Check the `eventId` against a de-duplication store (an in-memory set in dev/test; a database table or Redis in production) before processing. Persist the `eventId` inside the same transaction as the business side effect.]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import org.springframework.kafka.annotation.KafkaListener;
import org.springframework.stereotype.Component;
import org.springframework.transaction.annotation.Transactional;
import java.util.Set;
import java.util.concurrent.ConcurrentHashMap;

@Component
class IdempotentBillingListener {

    private final Set<String> processedEventIds = ConcurrentHashMap.newKeySet();
    private final BillingService billingService;

    IdempotentBillingListener(BillingService billingService) {
        this.billingService = billingService;
    }

    @KafkaListener(topics = "orders.created.v1", groupId = "billing-service-v1")
    @Transactional
    void onOrderCreated(OrderCreatedEvent event) {
        if (!processedEventIds.add(event.eventId())) {
            return; // already processed — skip without error
        }
        billingService.createInvoice(event.orderId(), event.customerId());
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[@KafkaListener(topics = "orders.created.v1", groupId = "billing-service-v1")
void onOrderCreated(OrderCreatedEvent event) {
    // Bad: no idempotency guard — rebalance or retry doubles billing
    billingService.createInvoice(event.orderId(), event.customerId());
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>
        <example number="7" id="314-frameworks-spring-kafka-integration-testing">
            <example-header>
                <example-title>Integration testing with @EmbeddedKafka</example-title>
                <example-subtitle>In-process broker for producer/consumer serialization tests</example-subtitle>
            </example-header>
            <example-description><![CDATA[Use `@EmbeddedKafka` when you need an in-process broker to verify serialization and topic routing without Docker. For full-stack integration tests that boot the application with HTTP and real infrastructure, prefer Testcontainers `@ServiceConnection` (see the next example). Do not mock `KafkaTemplate` in tests meant to verify messaging behaviour. In Spring Boot 4.x use `@MockitoBean`, not `@MockBean`.]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import org.apache.kafka.clients.consumer.Consumer;
import org.apache.kafka.clients.consumer.ConsumerRecords;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.kafka.core.ConsumerFactory;
import org.springframework.kafka.test.EmbeddedKafkaBroker;
import org.springframework.kafka.test.context.EmbeddedKafka;
import org.springframework.kafka.test.utils.KafkaTestUtils;

import static org.assertj.core.api.Assertions.assertThat;

@SpringBootTest
@EmbeddedKafka(topics = {"orders.created.v1"}, partitions = 1)
class OrderEventPublisherIT {

    @Autowired
    OrderEventPublisher publisher;

    @Autowired
    EmbeddedKafkaBroker broker;

    @Autowired
    ConsumerFactory<String, OrderCreatedEvent> consumerFactory;

    @Test
    void publish_sendsEventToTopic() {
        OrderCreatedEvent event = OrderCreatedEvent.of("order-1", "cust-1");
        publisher.publish(event);

        try (Consumer<String, OrderCreatedEvent> consumer = consumerFactory.createConsumer("test-group", "")) {
            broker.consumeFromAnEmbeddedTopic(consumer, "orders.created.v1");
            ConsumerRecords<String, OrderCreatedEvent> records = KafkaTestUtils.getRecords(consumer);
            assertThat(records.count()).isEqualTo(1);
            assertThat(records.iterator().next().value().orderId()).isEqualTo("order-1");
        }
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[// Bad: mocking KafkaTemplate proves nothing — does not test serialization or topic routing
@SpringBootTest
class OrderEventPublisherTest {

    @MockitoBean
    KafkaTemplate<String, OrderCreatedEvent> template;

    @Autowired
    OrderEventPublisher publisher;

    @Test
    void publish_callsTemplate() {
        publisher.publish(OrderCreatedEvent.of("o1", "c1"));
        verify(template).send(any(), any(), any()); // verifies Mockito glue only
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>
        <example number="8" id="314-frameworks-spring-kafka-json-serde-auto-configuration">
            <example-header>
                <example-title>JSON serialization with Boot auto-configuration</example-title>
                <example-subtitle>DefaultKafkaProducerFactoryCustomizer / DefaultKafkaConsumerFactoryCustomizer — not manual factories</example-subtitle>
            </example-header>
            <example-description><![CDATA[When publishing typed Java records, configure JSON serializers on Boot's auto-configured Kafka client factories. Use `DefaultKafkaProducerFactoryCustomizer` and `DefaultKafkaConsumerFactoryCustomizer` with `updateConfigs()` so bootstrap servers from `@ServiceConnection` or `spring.kafka.bootstrap-servers` stay intact.

Do **not** declare separate `@Bean ProducerFactory` / `@Bean ConsumerFactory` built from `KafkaProperties.buildProducerProperties()` — those often capture `localhost:9092` while Testcontainers runs on a dynamic port, and `@ServiceConnection` may still default producers to `StringSerializer`, causing `SerializationException` on typed events.

In Spring Boot 4.x, `KafkaProperties.buildProducerProperties()` and `buildConsumerProperties()` take no arguments.]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import java.util.Map;
import org.apache.kafka.clients.consumer.ConsumerConfig;
import org.apache.kafka.clients.producer.ProducerConfig;
import org.apache.kafka.common.serialization.StringDeserializer;
import org.apache.kafka.common.serialization.StringSerializer;
import org.springframework.boot.kafka.autoconfigure.DefaultKafkaConsumerFactoryCustomizer;
import org.springframework.boot.kafka.autoconfigure.DefaultKafkaProducerFactoryCustomizer;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.kafka.support.serializer.JsonDeserializer;
import org.springframework.kafka.support.serializer.JsonSerializer;

@Configuration
class KafkaConfig {

    @Bean
    DefaultKafkaProducerFactoryCustomizer jsonProducerFactoryCustomizer() {
        return producerFactory -> producerFactory.updateConfigs(Map.of(
            ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG, StringSerializer.class,
            ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG, JsonSerializer.class));
    }

    @Bean
    DefaultKafkaConsumerFactoryCustomizer jsonConsumerFactoryCustomizer() {
        return consumerFactory -> consumerFactory.updateConfigs(Map.of(
            ConsumerConfig.KEY_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class,
            ConsumerConfig.VALUE_DESERIALIZER_CLASS_CONFIG, JsonDeserializer.class,
            JsonDeserializer.TRUSTED_PACKAGES, "com.example.events",
            JsonDeserializer.VALUE_DEFAULT_TYPE, OrderCreatedEvent.class.getName()));
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[// Bad: manual factories bypass @ServiceConnection bootstrap wiring and often stick to localhost:9092
@Configuration
class KafkaConfig {

    @Bean
    ProducerFactory<String, OrderCreatedEvent> producerFactory(KafkaProperties props) {
        Map<String, Object> config = props.buildProducerProperties();
        config.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG, JsonSerializer.class);
        return new DefaultKafkaProducerFactory<>(config); // wrong broker in *IT tests
    }

    @Bean
    KafkaTemplate<String, OrderCreatedEvent> template(ProducerFactory<String, OrderCreatedEvent> pf) {
        return new KafkaTemplate<>(pf);
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>
        <example number="9" id="314-frameworks-spring-kafka-testcontainers-service-connection">
            <example-header>
                <example-title>Full-stack integration test with Testcontainers</example-title>
                <example-subtitle>@ServiceConnection on KafkaContainer; NewTopic in @TestConfiguration</example-subtitle>
            </example-header>
            <example-description><![CDATA[For `*IT` classes that POST to HTTP and assert a `@KafkaListener` received an event, start a `KafkaContainer` with Testcontainers and wire it via `@ServiceConnection` (Spring Boot 4.x). Do not set `spring.kafka.bootstrap-servers=localhost:9092` in shared `src/test/resources/application.properties` — that overrides the container port.

Declare `NewTopic` in a `@TestConfiguration` imported only by the IT class. Enable listeners in the IT (`spring.kafka.listener.auto-startup=true`) while keeping them disabled in unit tests. Use Maven Failsafe for `*IT` classes. Testcontainers 2.x Maven artifacts are `testcontainers-kafka` and `testcontainers-junit-jupiter`. Cross-reference `@322-frameworks-spring-boot-testing-integration-tests` for HTTP client and container lifecycle details.]]></example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import org.apache.kafka.clients.admin.NewTopic;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.context.TestConfiguration;
import org.springframework.boot.testcontainers.service.connection.ServiceConnection;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Import;
import org.springframework.kafka.config.TopicBuilder;
import org.testcontainers.junit.jupiter.Container;
import org.testcontainers.junit.jupiter.Testcontainers;
import org.testcontainers.kafka.KafkaContainer;
import org.testcontainers.utility.DockerImageName;

@Testcontainers
@Import(OrderKafkaTestConfiguration.class)
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT,
    properties = "spring.kafka.listener.auto-startup=true")
class OrderCreatedIT {

    @Container
    @ServiceConnection
    static KafkaContainer kafka = new KafkaContainer(DockerImageName.parse("apache/kafka-native:3.8.0"));

    @Test
    void createOrder_publishesOrderCreatedEvent() {
        // POST via TestRestTemplate; await listener-side assertion
    }
}

@TestConfiguration
class OrderKafkaTestConfiguration {

    @Bean
    NewTopic orderCreatedTopic() {
        return TopicBuilder.name("orders.created.v1").partitions(1).replicas(1).build();
    }
}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[# src/test/resources/application.properties — Bad with @ServiceConnection ITs:
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.listener.auto-startup=false

// Forces producer/consumer to localhost:9092 while Testcontainers exposes a random port.
// Also disables the listener in full-stack IT unless every class overrides properties.]]></code-block>
                </bad-example>
            </code-examples>
        </example>
    </examples>
    <output-format>
        <output-format-list>
            <output-format-item>**ANALYZE** Kafka code: event schema versioning, producer key strategy, listener group isolation, error handler registration, idempotency guards, and test coverage</output-format-item>
            <output-format-item>**CATEGORIZE** issues by impact (RELIABILITY for missing retries/DLT, CORRECTNESS for missing idempotency, MAINTAINABILITY for untyped schemas, SECURITY for untrusted topic name injection)</output-format-item>
            <output-format-item>**APPLY** Spring Kafka–aligned fixes: type the templates, key producers on the aggregate id, register DefaultErrorHandler with backoff and DLT, add eventId de-duplication</output-format-item>
            <output-format-item>**IMPLEMENT** changes so topic configs, serializers, and tests stay consistent</output-format-item>
            <output-format-item>**EXPLAIN** trade-offs (at-least-once vs exactly-once, fixed vs exponential backoff, in-process vs Testcontainers Kafka)</output-format-item>
            <output-format-item>**TEST** with Testcontainers `@ServiceConnection` for full-stack `*IT` tests; use `@EmbeddedKafka` for in-process serialization tests; `@MockitoBean KafkaTemplate` only for context-load tests without a broker</output-format-item>
            <output-format-item>**VALIDATE** with `./mvnw compile` before and `./mvnw clean verify` after changes</output-format-item>
        </output-format-list>
    </output-format>
    <safeguards>
        <safeguards-list>
            <safeguards-item>**BLOCKING SAFETY CHECK**: Run `./mvnw compile` before ANY Kafka refactoring — compilation failure is a HARD STOP</safeguards-item>
            <safeguards-item>**CRITICAL VALIDATION**: Run `./mvnw clean verify` after changes; exercise listener integration tests before promoting</safeguards-item>
            <safeguards-item>**INJECTION SAFETY**: Never construct topic names or message headers from untrusted user input</safeguards-item>
            <safeguards-item>**ERROR HANDLING**: Never swallow exceptions inside a `@KafkaListener` — propagate so the container's error handler can retry or route to DLT</safeguards-item>
            <safeguards-item>**IDEMPOTENCY**: Always de-duplicate on `eventId` — Kafka's at-least-once guarantee means consumers must tolerate duplicates</safeguards-item>
            <safeguards-item>**KEY STRATEGY**: Always set the Kafka message key to the aggregate's natural key; null keys disable partition-level ordering</safeguards-item>
            <safeguards-item>**DLT MONITORING**: Set up alerting on the DLT topic — messages landing there indicate systematic processing failures</safeguards-item>
            <safeguards-item>**SERVICE CONNECTION**: Never hand-wire `spring.kafka.bootstrap-servers` when `@ServiceConnection` on `KafkaContainer` already applies; never duplicate manual `ProducerFactory` / `ConsumerFactory` beans unless you also wire `KafkaConnectionDetails`</safeguards-item>
            <safeguards-item>**JSON SERDE**: For typed event records with `@ServiceConnection`, prefer factory customizers with `updateConfigs()` over `application.properties` alone — defaults may still use `StringSerializer`</safeguards-item>
            <safeguards-item>**INCREMENTAL SAFETY**: Change one producer/consumer surface at a time; verify with `@EmbeddedKafka` or Testcontainers `*IT` tests between steps</safeguards-item>
        </safeguards-list>
    </safeguards>
</prompt>
