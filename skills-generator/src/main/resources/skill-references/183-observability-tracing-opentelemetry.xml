<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xi="http://www.w3.org/2001/XInclude">

    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.15.0-SNAPSHOT</version>
        <license>Apache-2.0</license>
        <title>Java Distributed Tracing with OpenTelemetry</title>
        <description>Use when you need to implement or improve distributed tracing with OpenTelemetry in Java — including trace/span modeling, context propagation, semantic conventions, span attributes/events/status, sampling strategy, baggage usage, privacy safeguards, and backend integration with OTLP collectors.</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in Java software development</role>

    <goal>
        Effective distributed tracing with OpenTelemetry in Java requires clear span boundaries, reliable context propagation,
        semantic consistency, and strict privacy controls.
        Traces should help diagnose latency, failure causes, and cross-service dependencies without introducing excessive overhead.
    </goal>

    <constraints>
        <constraints-description>
            Tracing data must remain safe and operationally useful. Incorrect propagation or sensitive attributes reduce trust in telemetry.
        </constraints-description>
        <constraint-list>
            <constraint>**CONTEXT CONTINUITY**: Ensure trace context is propagated across HTTP/messaging/async boundaries</constraint>
            <constraint>**SEMANTIC CONSISTENCY**: Use stable span names and OpenTelemetry semantic conventions where applicable</constraint>
            <constraint>**DATA MINIMIZATION**: Avoid high-cardinality and sensitive attributes in spans/events</constraint>
            <constraint>**SAMPLING STRATEGY**: Configure sampling per environment to balance cost and diagnostic depth</constraint>
        </constraint-list>
    </constraints>

    <examples>
        <toc auto-generate="true"/>
        <example number="1" id="basic-span-modeling">
            <example-header>
                <example-title>Create Spans with Clear Boundaries</example-title>
                <example-subtitle>Model request and dependency calls as parent/child spans</example-subtitle>
            </example-header>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[
import io.opentelemetry.api.GlobalOpenTelemetry;
import io.opentelemetry.api.trace.Span;
import io.opentelemetry.api.trace.SpanKind;
import io.opentelemetry.api.trace.StatusCode;
import io.opentelemetry.api.trace.Tracer;
import io.opentelemetry.context.Scope;

public final class CheckoutTracing {
    private static final Tracer TRACER = GlobalOpenTelemetry.getTracer("checkout-service");

    public void checkout(String orderId) {
        Span parent = TRACER.spanBuilder("checkout.process")
                .setSpanKind(SpanKind.INTERNAL)
                .startSpan();
        try (Scope ignored = parent.makeCurrent()) {
            parent.setAttribute("order.id.present", orderId != null);

            callInventory();
            callPayment();

            parent.setStatus(StatusCode.OK);
        } catch (RuntimeException ex) {
            parent.recordException(ex);
            parent.setStatus(StatusCode.ERROR, "Checkout failed");
            throw ex;
        } finally {
            parent.end();
        }
    }

    private void callInventory() {
        Span span = TRACER.spanBuilder("inventory.reserve").setSpanKind(SpanKind.CLIENT).startSpan();
        try (Scope ignored = span.makeCurrent()) {
            // HTTP call / client SDK call
            span.setStatus(StatusCode.OK);
        } catch (RuntimeException ex) {
            span.recordException(ex);
            span.setStatus(StatusCode.ERROR);
            throw ex;
        } finally {
            span.end();
        }
    }

    private void callPayment() {
        Span span = TRACER.spanBuilder("payment.charge").setSpanKind(SpanKind.CLIENT).startSpan();
        try (Scope ignored = span.makeCurrent()) {
            span.setStatus(StatusCode.OK);
        } finally {
            span.end();
        }
    }
}
]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[
public final class CheckoutTracingBad {
    public void checkout(String userEmail, String token) {
        // BAD: no spans/context, impossible to diagnose flow latency
        // BAD: sensitive values often leak when teams later add naive attributes
        System.out.println("processing " + userEmail + " with token " + token);
    }
}
]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="2" id="propagation-and-safety">
            <example-header>
                <example-title>Propagate Context and Keep Attributes Safe</example-title>
                <example-subtitle>Ensure continuity and avoid cardinality/privacy problems</example-subtitle>
            </example-header>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[
import io.opentelemetry.api.trace.Span;
import io.opentelemetry.context.Context;
import java.util.concurrent.Executor;

public final class AsyncFlow {
    private final Executor executor;

    public AsyncFlow(Executor executor) {
        this.executor = executor;
    }

    public void submitTask() {
        Context captured = Context.current();
        executor.execute(() -> {
            try (var ignored = captured.makeCurrent()) {
                Span.current().addEvent("async.task.started");
                // do work
            }
        });
    }
}
]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[
import io.opentelemetry.api.trace.Span;

public final class AsyncFlowBad {
    public void submitTask(String userId, String jwtToken) {
        // BAD: no context propagation, detached spans
        // BAD: high cardinality + sensitive attributes
        Span.current().setAttribute("user.id", userId);
        Span.current().setAttribute("auth.token", jwtToken);
    }
}
]]></code-block>
                </bad-example>
            </code-examples>
        </example>
    </examples>

    <output-format>
        <output-format-list>
            <output-format-item>**ANALYZE** tracing gaps across span modeling, propagation, and semantic consistency</output-format-item>
            <output-format-item>**APPLY** OpenTelemetry instrumentation with clear parent/child relationships and stable names</output-format-item>
            <output-format-item>**HARDEN** attributes/events/status and remove sensitive or high-cardinality data</output-format-item>
            <output-format-item>**VALIDATE** end-to-end trace continuity in tests and runtime backends</output-format-item>
        </output-format-list>
    </output-format>

    <safeguards>
        <safeguards-list>
            <safeguards-item>**PRIVACY FIRST**: Never place secrets or PII in trace payloads</safeguards-item>
            <safeguards-item>**PROPAGATION CHECK**: Verify traceId/spanId continuity across service boundaries</safeguards-item>
            <safeguards-item>**CARDINALITY CONTROL**: Keep attribute values bounded and query-friendly</safeguards-item>
            <safeguards-item>**PERF AWARENESS**: Avoid unnecessary spans in hot loops and configure sampling appropriately</safeguards-item>
        </safeguards-list>
    </safeguards>
</prompt>
