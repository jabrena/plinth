<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xi="http://www.w3.org/2001/XInclude">

    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.15.0-SNAPSHOT</version>
        <license>Apache-2.0</license>
        <title>Java Metrics Observability with Micrometer</title>
        <description>Use when you need to implement or improve Java metrics observability with Micrometer — including meter design, naming/tag conventions, cardinality control, timers/counters/gauges/distribution summaries, percentiles/histograms, Actuator/Prometheus integration, and metrics validation through tests.</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in Java software development</role>

    <goal>
        Effective Java metrics observability with Micrometer starts by defining a clear metric contract for service-level signals:
        throughput, latency, errors, and saturation.
        Instrumentation must be stable over time, use semantic names, and avoid unbounded labels.
        The right meter type (Counter, Timer, Gauge, DistributionSummary, LongTaskTimer) should match the behavior being measured.
        Histograms and percentiles should be enabled selectively to control overhead and cost.
        Finally, metrics should be validated through tests and runtime checks to ensure dashboards and alerts remain reliable.
    </goal>

    <constraints>
        <constraints-description>
            Metrics instrumentation must avoid high-cardinality dimensions and dynamic meter churn.
            Uncontrolled tagging can overload telemetry backends and make observability unreliable.
        </constraints-description>
        <constraint-list>
            <constraint>**CARDINALITY SAFETY**: Never use user-controlled, unique, or high-cardinality tag values (UUID, email, request id, full path with IDs)</constraint>
            <constraint>**METER SEMANTICS**: Select meter types based on behavior (Counter for events, Timer for latency, Gauge for state, DistributionSummary for value distributions)</constraint>
            <constraint>**INSTRUMENTATION SCOPE**: Prefer business and platform signals tied to SLOs over noisy low-value metrics</constraint>
            <constraint>**VALIDATION**: Verify expected metrics names, tags, and values through automated tests and runtime checks</constraint>
        </constraint-list>
    </constraints>

    <examples>
        <toc auto-generate="true"/>
        <example number="1" id="meter-selection">
            <example-header>
                <example-title>Select the Right Meter Type</example-title>
                <example-subtitle>Use Counters, Timers, Gauges and Summaries with Correct Semantics</example-subtitle>
            </example-header>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[
import io.micrometer.core.instrument.Counter;
import io.micrometer.core.instrument.DistributionSummary;
import io.micrometer.core.instrument.Gauge;
import io.micrometer.core.instrument.MeterRegistry;
import io.micrometer.core.instrument.Timer;
import java.time.Duration;
import java.util.Objects;
import java.util.concurrent.atomic.AtomicInteger;

public final class CheckoutMetrics {

    private final Counter checkoutSuccess;
    private final Counter checkoutFailure;
    private final Timer checkoutLatency;
    private final DistributionSummary payloadBytes;
    private final AtomicInteger queueDepth;

    public CheckoutMetrics(MeterRegistry registry) {
        this.checkoutSuccess = Counter.builder("checkout.requests")
                .description("Total successful checkout requests")
                .tag("outcome", "success")
                .register(registry);
        this.checkoutFailure = Counter.builder("checkout.requests")
                .description("Total failed checkout requests")
                .tag("outcome", "failure")
                .register(registry);
        this.checkoutLatency = Timer.builder("checkout.latency")
                .description("Checkout latency")
                .publishPercentileHistogram()
                .maximumExpectedValue(Duration.ofSeconds(5))
                .register(registry);
        this.payloadBytes = DistributionSummary.builder("checkout.payload.bytes")
                .description("Payload size of checkout requests")
                .baseUnit("bytes")
                .register(registry);
        this.queueDepth = registry.gauge("checkout.queue.depth", new AtomicInteger(0));
    }

    public void recordSuccess(long nanos, int bytes) {
        checkoutSuccess.increment();
        checkoutLatency.record(nanos, java.util.concurrent.TimeUnit.NANOSECONDS);
        payloadBytes.record(bytes);
    }

    public void recordFailure() {
        checkoutFailure.increment();
    }

    public void setQueueDepth(int depth) {
        queueDepth.set(Math.max(depth, 0));
    }
}
]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[
import io.micrometer.core.instrument.MeterRegistry;

public final class BadMetrics {

    public void record(MeterRegistry registry, String userId, long latencyMillis) {
        // BAD: every signal is a gauge/counter without semantics
        registry.counter("checkout.metric", "type", "latency").increment(latencyMillis);

        // BAD: high cardinality tag
        registry.counter("checkout.requests", "userId", userId).increment();

        // BAD: timer should be used for latency, not counter with duration values
        registry.counter("checkout.duration.millis").increment(latencyMillis);
    }
}
]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="2" id="naming-tagging">
            <example-header>
                <example-title>Use Stable Naming and Low-Cardinality Tags</example-title>
                <example-subtitle>Keep Metric Schema Predictable for Dashboards and Alerts</example-subtitle>
            </example-header>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[
import io.micrometer.core.instrument.Counter;
import io.micrometer.core.instrument.MeterRegistry;
import java.util.Set;

public final class PaymentMetrics {

    private static final Set<String> ALLOWED_RESULTS = Set.of("approved", "declined", "error");
    private final MeterRegistry registry;

    public PaymentMetrics(MeterRegistry registry) {
        this.registry = registry;
    }

    public void recordResult(String provider, String result) {
        String safeProvider = normalizeProvider(provider); // bounded set
        String safeResult = ALLOWED_RESULTS.contains(result) ? result : "error";

        Counter.builder("payment.authorization.requests")
                .tag("provider", safeProvider)
                .tag("result", safeResult)
                .register(registry)
                .increment();
    }

    private String normalizeProvider(String provider) {
        return switch (provider) {
            case "adyen", "stripe", "paypal" -> provider;
            default -> "unknown";
        };
    }
}
]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[
import io.micrometer.core.instrument.MeterRegistry;

public final class PaymentMetricsBad {
    public void record(MeterRegistry registry, String fullUrl, String requestId, String exceptionMessage) {
        // BAD: unbounded tags explode cardinality
        registry.counter("payment.requests",
                "url", fullUrl,
                "requestId", requestId,
                "error", exceptionMessage).increment();
    }
}
]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="3" id="test-metrics">
            <example-header>
                <example-title>Validate Instrumentation with Tests</example-title>
                <example-subtitle>Assert Metric Presence, Tags, and Values</example-subtitle>
            </example-header>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[
import io.micrometer.core.instrument.simple.SimpleMeterRegistry;
import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.assertThat;

class PaymentMetricsTest {

    @Test
    void shouldRecordApprovedPayment() {
        var registry = new SimpleMeterRegistry();
        var metrics = new PaymentMetrics(registry);

        metrics.recordResult("stripe", "approved");
        metrics.recordResult("stripe", "approved");

        double count = registry.get("payment.authorization.requests")
                .tag("provider", "stripe")
                .tag("result", "approved")
                .counter()
                .count();

        assertThat(count).isEqualTo(2.0d);
    }
}
]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[
class PaymentMetricsTestBad {
    // BAD: no metric assertions; instrumentation can silently break.
}
]]></code-block>
                </bad-example>
            </code-examples>
        </example>
    </examples>

    <output-format>
        <output-format-list>
            <output-format-item>**ANALYZE** current instrumentation and classify issues by cardinality, semantic correctness, naming consistency, and operational value</output-format-item>
            <output-format-item>**DESIGN** a stable metric contract: names, units, bounded tags, meter types, and where each metric is emitted</output-format-item>
            <output-format-item>**APPLY** Micrometer best practices directly in code with minimal, explicit, and reusable instrumentation points</output-format-item>
            <output-format-item>**EXPLAIN** how each metric supports dashboards, alerts, and SLO/SLA reporting</output-format-item>
            <output-format-item>**VALIDATE** correctness through tests and runtime endpoint checks, including expected labels and values</output-format-item>
        </output-format-list>
    </output-format>

    <safeguards>
        <safeguards-list>
            <safeguards-item>**CARDINALITY GUARDRAIL**: Reject unbounded labels and normalize dynamic values into bounded buckets</safeguards-item>
            <safeguards-item>**NO METER CHURN**: Avoid building meters in tight loops with dynamic names/tags</safeguards-item>
            <safeguards-item>**BACKEND COMPATIBILITY**: Ensure names and tags remain compatible with Prometheus/OpenTelemetry conventions</safeguards-item>
            <safeguards-item>**OPERABILITY CHECK**: Confirm metrics are scrapeable/exported and usable in dashboards and alerts</safeguards-item>
            <safeguards-item>**REGRESSION SAFETY**: Keep existing metric names stable unless migration is explicitly planned</safeguards-item>
        </safeguards-list>
    </safeguards>
</prompt>
