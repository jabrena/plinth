<?xml version="1.0" encoding="UTF-8"?>
<prompt>
    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.15.0-SNAPSHOT</version>
        <license>Apache-2.0</license>
        <title>Spring Boot Security Guidelines</title>
        <description>Use when you need to design, review, or improve security in Spring Boot applications — including Spring Security configuration, authentication/authorization, endpoint protection, method security, and secure handling of sensitive data.</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in Spring Security and secure API design</role>

    <goal>
        Apply secure-by-default Spring Boot practices: enforce authentication and authorization consistently, define least-privilege access control, and avoid sensitive data exposure in logs and API responses.
    </goal>

    <constraints>
        <constraints-description>
            Before applying recommendations, ensure the project compiles.
            Compilation failure is a blocking condition.
        </constraints-description>
        <constraint-list>
            <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
            <constraint>**SAFETY**: If compilation fails, stop immediately</constraint>
            <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
        </constraint-list>
    </constraints>

    <examples>
        <toc auto-generate="true" />
        <example number="1" id="security-filter-chain-and-method-security">
            <example-header>
                <example-title>Secure endpoint and method access</example-title>
                <example-subtitle>Use SecurityFilterChain and method-level authorization</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Define explicit security policies for public and protected routes. Use role-based authorization for privileged operations.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.method.configuration.EnableMethodSecurity;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.web.SecurityFilterChain;

@Configuration
@EnableMethodSecurity
class SecurityConfig {
    @Bean
    SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        return http
            .authorizeHttpRequests(registry -> registry
                .requestMatchers("/actuator/health").permitAll()
                .requestMatchers("/admin/**").hasRole("ADMIN")
                .anyRequest().authenticated())
            .build();
    }
}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[@Configuration
class InsecureConfig {
    @Bean
    SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        return http
            .authorizeHttpRequests(registry -> registry.anyRequest().permitAll())
            .build();
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>
    </examples>

    <output-format>
        <output-format-list>
            <output-format-item>**ANALYZE** current authentication and authorization boundaries</output-format-item>
            <output-format-item>**APPLY** secure defaults, route protection, and least-privilege policies</output-format-item>
            <output-format-item>**HARDEN** logging/error behavior to avoid sensitive data leakage</output-format-item>
            <output-format-item>**VALIDATE** with compile and full verification commands</output-format-item>
        </output-format-list>
    </output-format>

    <safeguards>
        <safeguards-list>
            <safeguards-item>Do not weaken endpoint protection to bypass failing tests</safeguards-item>
            <safeguards-item>Do not expose tokens, secrets, or credentials in logs/responses</safeguards-item>
            <safeguards-item>Prefer incremental security changes with verification between steps</safeguards-item>
        </safeguards-list>
    </safeguards>
</prompt>
