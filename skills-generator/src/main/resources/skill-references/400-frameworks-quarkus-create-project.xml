<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.8.0/pml.xsd">
    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.18.0-SNAPSHOT</version>
        <license>Apache-2.0</license>
        <title>Create Quarkus Maven Project</title>
        <description>Use when creating a new Maven-based Quarkus 3.x project with SDKMAN-managed Java and Quarkus CLI tooling.</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in Quarkus, Maven, SDKMAN, and Java enterprise project bootstrapping</role>

    <goal>
        Create a new Quarkus Maven project in a predictable way. Use SDKMAN to manage Java and the Quarkus CLI, gather project coordinates before running commands, generate the project with Maven as the build tool, and verify the result with the Maven wrapper when available.
    </goal>

    <constraints>
        <constraints-description>New project generation changes the filesystem, so confirm target paths and avoid overwriting user work.</constraints-description>
        <constraint-list>
            <constraint>**PREREQUISITE**: Run `sdk version` before using SDKMAN-managed tooling</constraint>
            <constraint>**MISSING SDKMAN**: If SDKMAN is not installed, stop and provide the official installation prerequisite instead of installing it silently</constraint>
            <constraint>**JAVA BASELINE**: Prefer Java 25 unless the user requests another supported version</constraint>
            <constraint>**FRAMEWORK BASELINE**: Use the current Quarkus 3.x line by default</constraint>
            <constraint>**BUILD TOOL**: Generate Maven projects only; do not generate Gradle projects from this skill</constraint>
            <constraint>**TARGET DIRECTORY**: Check whether the target directory exists and is empty before project creation</constraint>
            <constraint>**EXTENSIONS**: Ask for desired Quarkus extensions when the request does not specify them</constraint>
            <constraint>**VERIFY**: Run `./mvnw clean verify` in the generated project when `mvnw` exists; otherwise run `mvn clean verify`</constraint>
        </constraint-list>
    </constraints>

    <steps>
        <step number="1">
            <step-title>Gather project inputs</step-title>
            <step-content>Collect the target directory, group id, artifact id, package name, Java version, Quarkus platform/version preference, and desired extensions such as `rest`, `hibernate-validator`, `jdbc-postgresql`, or `smallrye-openapi`.</step-content>
        </step>
        <step number="2">
            <step-title>Verify SDKMAN and Java</step-title>
            <step-content>Run `sdk version`, then check Java with `java -version`. If a Java switch is required, use `sdk list java`, `sdk install java &lt;version&gt;`, or `sdk use java &lt;version&gt;` after confirming the selected candidate.</step-content>
        </step>
        <step number="3">
            <step-title>Verify Quarkus CLI</step-title>
            <step-content>Check `quarkus --version`. If the Quarkus CLI is missing, use SDKMAN candidate discovery such as `sdk list quarkus`, then install or use the agreed candidate with `sdk install quarkus &lt;version&gt;` or `sdk use quarkus &lt;version&gt;`.</step-content>
        </step>
        <step number="4">
            <step-title>Create the Maven project</step-title>
            <step-content>Use `quarkus create app` with explicit Maven options, for example `quarkus create app &lt;group&gt;:&lt;artifact&gt;:&lt;version&gt; --maven --java=25 --package-name=&lt;package&gt; --extension=&lt;extensions&gt; --output-directory=&lt;target-directory&gt;`. Keep extension choices explicit and use the Quarkus 3.x CLI/platform defaults unless the user requests a specific compatible version.</step-content>
        </step>
        <step number="5">
            <step-title>Verify the generated project</step-title>
            <step-content>Change into the generated project directory, inspect the generated `pom.xml`, and run `./mvnw clean verify` when the wrapper exists. Report command output failures with the exact failing goal and the next corrective action.</step-content>
        </step>
        <step number="6">
            <step-title>Report completion</step-title>
            <step-content>Summarize the generated path, selected Java version, Quarkus version or platform stream, Maven command used, extensions, and verification result.</step-content>
        </step>
    </steps>
</prompt>
