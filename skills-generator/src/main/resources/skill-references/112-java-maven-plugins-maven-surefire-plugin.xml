<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xi="http://www.w3.org/2001/XInclude">
    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.16.0-SNAPSHOT</version>
        <license>Apache-2.0</license>
        <title>Maven Plugins: pom.xml Configuration Best Practices</title>
        <description>Maven Surefire plugin guidance for unit test execution.</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in Java software development.</role>

    <tone>Treat the user as a knowledgeable partner. Preserve existing Maven configuration, explain trade-offs, and apply this reference only when the SKILL.md question flow selected this plugin or profile.</tone>

    <goal><![CDATA[
Configure Maven Surefire only when selected by the user in `112-java-maven-plugins`. Read this reference after the SKILL.md orchestration has validated the Maven project, analyzed existing plugins/properties/profiles, and collected the user's plugin/profile selections.
    ]]></goal>

    <constraints>
        <constraints-description>Apply only the selected Maven Surefire configuration and preserve existing project configuration.</constraints-description>
        <constraint-list>
            <constraint>Read this reference only when the user selected the corresponding Maven plugin or profile in the centralized question flow.</constraint>
            <constraint>Before adding configuration, check whether the plugin, property, reporting entry, support file, or profile already exists.</constraint>
            <constraint>Never remove or replace existing Maven configuration without explicit user confirmation.</constraint>
            <constraint>Add only the Maven properties, plugin configuration, profile configuration, and support files required by this reference.</constraint>
        </constraint-list>
    </constraints>

    <steps>
        <step number="1">
            <step-title>Add selected Maven properties</step-title>
            <step-content><![CDATA[
Add only the properties needed for this selected plugin/profile. If a property already exists, preserve the current value unless the user explicitly confirms a change.

```xml
<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
<maven.version>3.9.10</maven.version>
<java.version>[USER_SELECTED_JAVA_VERSION]</java.version>
<maven-plugin-surefire.version>3.5.3</maven-plugin-surefire.version>
```
            ]]></step-content>
        </step>
        <step number="2">
            <step-title>Maven Surefire Plugin Configuration</step-title>
            <step-content><![CDATA[
**Purpose**: Configure maven-surefire-plugin for unit testing with proper includes/excludes and failure handling.

**Dependencies**: Only execute if user selected "Unit Testing (Surefire)" in the SKILL.md question flow. Requires completion of the SKILL.md orchestration steps and selected property handling.

**CRITICAL PRESERVATION RULE**: Only ADD this plugin if it doesn't already exist. Never REPLACE or REMOVE existing configuration.

**CONDITIONAL EXECUTION**: Only execute this step if user selected "Unit Testing (Surefire)" in the SKILL.md question flow.

## Pre-Implementation Check

**BEFORE adding maven-surefire-plugin, check if it already exists in the pom.xml:**

If maven-surefire-plugin already exists: Ask user "maven-surefire-plugin already exists. Do you want to enhance the existing configuration? (y/n)"

If user says "n": Skip this step entirely.
If user says "y": Proceed with adding missing configuration elements only.

## Maven Surefire Plugin Configuration

**ADD this plugin to the `<build><plugins>` section ONLY if it doesn't already exist:**

```xml
<plugin>
<groupId>org.apache.maven.plugins</groupId>
<artifactId>maven-surefire-plugin</artifactId>
<version>${maven-plugin-surefire.version}</version>
<configuration>
    <skipAfterFailureCount>1</skipAfterFailureCount>
    <includes>
        <include>**/*Test.java</include>
    </includes>
    <excludes>
        <exclude>**/*IT.java</exclude>
    </excludes>
</configuration>
</plugin>
```

## Validation

After adding this plugin, verify the configuration:

```bash
# Run unit tests
./mvnw test
```
                
            
            ]]></step-content>
            <step-constraints>
                <step-constraint-list>
                    <step-constraint>**MUST** only add surefire plugin if "Unit Testing (Surefire)" was selected in the SKILL.md question flow</step-constraint>
                    <step-constraint>**MUST** use selected version properties for plugin versions</step-constraint>
                    <step-constraint>**MUST** check if plugin already exists before adding</step-constraint>
                    <step-constraint>**MUST** ask user permission before modifying existing plugin configuration</step-constraint>
                    <step-constraint>**MUST** configure proper includes/excludes for test file patterns</step-constraint>
                    <step-constraint>**MUST** skip this step entirely if unit testing was not selected</step-constraint>
                </step-constraint-list>
            </step-constraints>
        </step>
    </steps>
</prompt>
