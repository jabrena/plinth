<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.7.0/pml.xsd"
      id="412-frameworks-quarkus-panache">
  <metadata>
    <author>Juan Antonio Breña Moral</author>
    <version>0.13.0-SNAPSHOT</version>
    <license>Apache-2.0</license>
    <description>Use when you need data access with Quarkus Hibernate ORM Panache — including PanacheEntity / PanacheEntityBase, PanacheRepository, named and HQL queries, transactions, pagination, and immutable-friendly patterns. This is the Quarkus analogue to Spring Data for relational persistence.</description>
  </metadata>

  <title>Hibernate ORM with Panache</title>
  <goal><![CDATA[
Apply Panache patterns for Hibernate ORM in Quarkus.

**What is covered in this Skill?**

- Active record (PanacheEntity) vs PanacheRepository
- Parameterized HQL / Panache queries (no unsafe concatenation)
- @Transactional application services
- Pagination and avoiding unbounded list loads
- Mapping entities vs exposing DTOs at REST boundaries
- Pairing with `@411` for raw SQL when needed

**Scope:** Apply recommendations based on the reference rules and good/bad code examples.
]]></goal>

  <constraints>
    <constraints-description>Compile before persistence changes; verify after.</constraints-description>
    <constraint-list>
      <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
      <constraint>**SAFETY**: If compilation fails, stop immediately</constraint>
      <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
      <constraint>**BEFORE APPLYING**: Read the reference for detailed rules and examples</constraint>
    </constraint-list>
  </constraints>

  <triggers>
    <trigger-list>
        <trigger>Review Panache entities or repositories in Quarkus</trigger>
        <trigger>Improve Hibernate ORM data access with Panache</trigger>
    </trigger-list>
  </triggers>

  <references>
    <reference-list>
        <reference>references/412-frameworks-quarkus-panache.md</reference>
    </reference-list>
  </references>
</prompt>
