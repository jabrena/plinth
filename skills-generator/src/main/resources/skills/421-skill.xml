<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.7.0/pml.xsd"
      id="421-frameworks-quarkus-testing-unit-tests">
  <metadata>
    <author>Juan Antonio Breña Moral</author>
    <version>0.14.0</version>
    <license>Apache-2.0</license>
    <description>Use when you need to write fast unit tests for Quarkus applications — including pure tests with @ExtendWith(MockitoExtension.class), @QuarkusTest with @InjectMock for full CDI mock replacement, @InjectSpy for partial CDI bean mocking, REST Assured for resource-focused tests, @ParameterizedTest with @CsvSource / @MethodSource, QuarkusTestProfile for test-specific configuration overrides, and naming conventions (*Test → Surefire, *IT → Failsafe). For framework-agnostic Java use @131-java-testing-unit-testing.</description>
  </metadata>

  <title>Quarkus Unit Testing</title>
  <goal><![CDATA[
Apply fast testing strategies for Quarkus: Mockito-first, QuarkusTest when CDI wiring matters.

**What is covered in this Skill?**

- Pure JUnit 5 + Mockito without container boot (@ExtendWith(MockitoExtension.class))
- @QuarkusTest with @InjectMock for full CDI bean replacement
- @InjectSpy for partial mocking — real bean wrapped as spy, specific methods overridden
- REST Assured for HTTP-level @QuarkusTest resource tests
- @ParameterizedTest with @CsvSource (inline data) and @MethodSource (complex objects)
- QuarkusTestProfile and @TestProfile for test-specific configuration overrides
- Naming conventions: *Test → Surefire (fast phase), *IT → Failsafe (verify phase)
- When to escalate to integration tests (`@422`)

**Scope:** Apply recommendations based on the reference rules and good/bad code examples.
]]></goal>

  <constraints>
    <constraints-description>Compile before test refactors; verify the full suite after.</constraints-description>
    <constraint-list>
      <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
      <constraint>**PREREQUISITE**: Project must compile before applying test improvements</constraint>
      <constraint>**SAFETY**: If compilation fails, stop immediately</constraint>
      <constraint>**BLOCKING CONDITION**: Compilation errors must be resolved by the user before proceeding</constraint>
      <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
      <constraint>**BEFORE APPLYING**: Read the reference for detailed rules and examples</constraint>
    </constraint-list>
  </constraints>

  <triggers>
    <trigger-list>
        <trigger>Add or improve unit tests in a Quarkus project</trigger>
        <trigger>Reduce slow @QuarkusTest usage with Mockito-first tests</trigger>
        <trigger>Add @InjectSpy partial mocking or QuarkusTestProfile configuration in Quarkus tests</trigger>
        <trigger>Convert repeated test methods to @ParameterizedTest with @CsvSource or @MethodSource</trigger>
    </trigger-list>
  </triggers>

  <references>
    <reference-list>
        <reference>references/421-frameworks-quarkus-testing-unit-tests.md</reference>
    </reference-list>
  </references>
</prompt>
