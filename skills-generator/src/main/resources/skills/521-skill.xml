<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.7.0/pml.xsd"
      id="521-frameworks-micronaut-testing-unit-tests">
  <metadata>
    <author>Juan Antonio Breña Moral</author>
    <version>0.13.0</version>
    <license>Apache-2.0</license>
    <description>Use when you need to write unit tests for Micronaut applications — Mockito-first with @ExtendWith(MockitoExtension.class), @MicronautTest with @MockBean, HttpClient @Client("/") assertions, @Property overrides, @ParameterizedTest, and *Test vs *IT naming. For framework-agnostic Java use @131-java-testing-unit-testing.</description>
  </metadata>

  <title>Micronaut Unit Testing</title>
  <goal><![CDATA[
Apply fast testing strategies for Micronaut: Mockito-first, narrow @MicronautTest when HTTP or DI replacement is required.

**What is covered in this Skill?**

- Pure JUnit 5 + Mockito without container boot
- @MicronautTest with @MockBean factory methods for collaborators
- HttpClient blocking exchanges against the embedded server
- @Property for deterministic configuration in tests
- @ParameterizedTest with @CsvSource / @MethodSource
- Naming: *Test → Surefire; *IT → Failsafe when configured
- When to escalate to `@522`

**Scope:** Apply recommendations based on the reference rules and good/bad code examples.
]]></goal>

  <constraints>
    <constraints-description>Compile before test refactors; verify the full suite after.</constraints-description>
    <constraint-list>
      <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
      <constraint>**SAFETY**: If compilation fails, stop immediately</constraint>
      <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
      <constraint>**BEFORE APPLYING**: Read the reference for detailed rules and examples</constraint>
    </constraint-list>
  </constraints>

  <triggers>
    <trigger-list>
        <trigger>Add or improve unit tests in a Micronaut project</trigger>
        <trigger>Reduce unnecessary @MicronautTest usage with Mockito-first tests</trigger>
    </trigger-list>
  </triggers>

  <references>
    <reference-list>
        <reference>references/521-frameworks-micronaut-testing-unit-tests.md</reference>
    </reference-list>
  </references>
</prompt>
