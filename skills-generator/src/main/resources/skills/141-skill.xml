<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.7.0/pml.xsd"
      id="141-java-refactoring-with-modern-features">
  <metadata>
    <author>Juan Antonio Breña Moral</author>
    <version>0.13.0-SNAPSHOT</version>
    <license>Apache-2.0</license>
    <description>Use when you need to refactor Java code to adopt modern Java features (Java 8+) — including migrating anonymous classes to lambdas, replacing Iterator loops with Stream API, adopting Optional for null safety, switching from legacy Date/Calendar to java.time, using collection factory methods, migrating to CompletableFuture for async operations, applying text blocks, var inference, or leveraging Java 25 features like flexible constructor bodies and module import declarations.</description>
  </metadata>

  <title>Modern Java Development Guidelines (Java 8+)</title>
  <goal><![CDATA[
Identify and apply modern Java (Java 8+) refactoring opportunities to improve readability, maintainability, and performance.

**What is covered in this Skill?**

- Lambda expressions and method references (over anonymous classes)
- Stream API for declarative collection processing
- `Optional` for null-safe APIs
- `java.time` API (replacing `Date`/`Calendar`)
- Default interface methods, `var` type inference
- Unmodifiable collection factory methods (`List.of()`, `Set.of()`, `Map.of()`)
- `CompletableFuture` for composable async programming
- Text blocks for multi-line strings
- Java 25 Flexible Constructor Bodies (JEP 513)
- Java 25 Module Import Declarations (JEP 511)

**Scope:** The reference is organized by examples (good/bad code patterns) for each core area. Apply recommendations based on applicable examples.
]]></goal>

  <constraints>
    <constraints-description>Before applying any modern Java refactoring, ensure the project compiles. If compilation fails, stop immediately — do not proceed until the project compiles successfully. After applying improvements, run full verification.</constraints-description>
    <constraint-list>
      <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any changes</constraint>
      <constraint>**SAFETY**: If compilation fails, stop immediately — do not proceed until the project compiles successfully</constraint>
      <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
      <constraint>**BEFORE APPLYING**: Read the reference for detailed good/bad examples, constraints, and safeguards for each modern Java feature</constraint>
    </constraint-list>
  </constraints>

  <references>
    <reference-list>
        <reference>references/141-java-refactoring-with-modern-features.md</reference>
    </reference-list>
  </references>
</prompt>
