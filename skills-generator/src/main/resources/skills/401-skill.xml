<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.7.0/pml.xsd"
      id="401-frameworks-quarkus-core">
  <metadata>
    <author>Juan Antonio Breña Moral</author>
    <version>0.13.0-SNAPSHOT</version>
    <license>Apache-2.0</license>
    <description>Use when you need to review, improve, or build Quarkus applications — including @QuarkusMain entry points, CDI scopes (@ApplicationScoped, @Singleton, @Dependent), constructor injection, @ConfigMapping and SmallRye Config, profiles (%dev, %test, %prod), build-time vs runtime configuration, lifecycle (@Startup, @PreDestroy), health and metrics hooks, and test-friendly bean design.</description>
  </metadata>

  <title>Quarkus Core Guidelines</title>
  <goal><![CDATA[
Apply Quarkus core guidelines for CDI beans, configuration, profiles, and lifecycle.

**What is covered in this Skill?**

- @QuarkusMain and application entry (when custom main is needed)
- CDI scopes: @ApplicationScoped, @Singleton, @Dependent
- Constructor injection with @Inject
- @ConfigMapping and structured configuration
- Profile-specific properties (%dev, %test, %prod) and @IfBuildProfile
- Startup and shutdown observers (@Startup, @PreDestroy)
- Operational hooks: health, metrics (when extensions are present)
- Alignment with fast startup and native-image constraints

**Scope:** Apply recommendations based on the reference rules and good/bad code examples.
]]></goal>

  <constraints>
    <constraints-description>Before applying any Quarkus changes, ensure the project compiles. If compilation fails, stop immediately. After applying improvements, run full verification.</constraints-description>
    <constraint-list>
      <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
      <constraint>**SAFETY**: If compilation fails, stop immediately — compilation failure is a blocking condition</constraint>
      <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
      <constraint>**BEFORE APPLYING**: Read the reference for detailed rules, good/bad patterns, and constraints</constraint>
    </constraint-list>
  </constraints>

  <triggers>
    <trigger-list>
        <trigger>Review Java code for Quarkus application structure and CDI</trigger>
        <trigger>Apply best practices for Quarkus configuration and beans</trigger>
    </trigger-list>
  </triggers>

  <references>
    <reference-list>
        <reference>references/401-frameworks-quarkus-core.md</reference>
    </reference-list>
  </references>
</prompt>
