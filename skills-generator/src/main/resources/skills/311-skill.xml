<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.7.0/pml.xsd"
      id="311-frameworks-spring-jdbc">
  <metadata>
    <author>Juan Antonio Breña Moral</author>
    <version>0.13.0</version>
    <license>Apache-2.0</license>
    <description>Use when you need to write or review programmatic JDBC with Spring — including JdbcTemplate, NamedParameterJdbcTemplate, JdbcClient (Spring Framework 6.1+), parameterized SQL, RowMapper mapping to records, batch operations, transactions, safe handling of generated keys, DataAccessException handling, read-only transactions, streaming large result sets, and @JdbcTest slice testing.</description>
  </metadata>

  <title>Spring JDBC — JdbcTemplate and JdbcClient</title>
  <goal><![CDATA[
Apply Spring JDBC guidelines using JdbcTemplate, NamedParameterJdbcTemplate, and JdbcClient.

**What is covered in this Skill?**

- Parameterized SQL (never concatenate user input)
- JdbcClient fluent API for new code (Spring Framework 6.1+)
- NamedParameterJdbcTemplate for named placeholders
- RowMapper and DataClassRowMapper (records)
- Batch operations
- Safe handling of generated keys (KeyHolder / JdbcClient returning keys)
- Service-layer @Transactional boundaries
- Read-only transactions (@Transactional(readOnly = true))
- Safe single-row access (optional() / findFirst() vs queryForObject)
- Streaming large result sets (RowCallbackHandler, ResultSetExtractor)
- DataAccessException handling (DuplicateKeyException, EmptyResultDataAccessException)
- @JdbcTest slice testing with @Sql fixtures

**Scope:** Apply recommendations based on the reference rules and good/bad code examples.
]]></goal>

  <constraints>
    <constraints-description>Before applying any Spring JDBC changes, ensure the project compiles. If compilation fails, stop immediately. After applying improvements, run full verification.</constraints-description>
    <constraint-list>
      <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
      <constraint>**SAFETY**: If compilation fails, stop immediately</constraint>
      <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
      <constraint>**SQL INJECTION**: Never concatenate untrusted input into SQL strings — always use bind parameters</constraint>
      <constraint>**BEFORE APPLYING**: Read the reference for detailed rules and good/bad patterns</constraint>
    </constraint-list>
  </constraints>

  <triggers>
    <trigger-list>
        <trigger>Review Java code for Spring JDBC (JdbcTemplate, JdbcClient, NamedParameterJdbcTemplate)</trigger>
        <trigger>Apply best practices for Spring JDBC data access in Java code</trigger>
        <trigger>Detect and fix SQL injection risks in JDBC code</trigger>
        <trigger>Improve transaction boundaries or exception handling for JDBC operations</trigger>
    </trigger-list>
  </triggers>

  <references>
    <reference-list>
        <reference>references/311-frameworks-spring-jdbc.md</reference>
    </reference-list>
  </references>
</prompt>
