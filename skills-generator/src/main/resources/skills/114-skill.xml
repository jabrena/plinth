<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://jabrena.github.io/pml/schemas/0.7.0/pml.xsd"
      id="114-java-maven-search">
  <metadata>
    <author>Juan Antonio Breña Moral</author>
    <version>0.14.0</version>
    <license>Apache-2.0</license>
    <description>Covers Maven Central search (Search API, maven-metadata.xml, artifact URLs) and project-local update reports via versions-maven-plugin (display-property-updates, display-dependency-updates, display-plugin-updates). Use when finding or verifying coordinates, browsing Central, or checking what newer versions apply to the user’s pom.xml.</description>
  </metadata>

  <title>Maven Central search and coordinates</title>
  <goal><![CDATA[
Help users search Maven Central, resolve **groupId:artifactId:version**, read version history, and build correct download URLs; and when working on **their** project, verify `versions-maven-plugin` and run `versions:display-*` goals for dependency, plugin, and property updates. **What is covered:**

- Maven Central Search API — e.g. keyword search for Spring Boot starters (`spring-boot-starter`) or coordinate filters (`g:org.springframework.boot AND a:spring-boot-starter-parent`)
- Direct repository layout and `maven-metadata.xml`
- POM, JAR, `-sources.jar`, `-javadoc.jar` URL patterns
- Parsing POMs for direct dependencies; transitive trees via Maven/Gradle on the consumer project
- Versions Maven Plugin — ensure `org.codehaus.mojo:versions-maven-plugin` is declared, then `./mvnw versions:display-property-updates`, `versions:display-dependency-updates`, `versions:display-plugin-updates`
- Output format: structured coordinates, tables, and verifiable HTTPS links
]]></goal>

  <constraints>
    <constraints-description>Verify coordinates against the Search API or repository responses before asserting availability. Prefer release versions unless snapshots are explicitly required.</constraints-description>
    <constraint-list>
      <constraint>**VERIFY**: Do not invent GAVs — confirm via Search API or successful GET of metadata/POM</constraint>
      <constraint>**FORMAT**: Always express full coordinates as `groupId:artifactId:version` when a version is fixed</constraint>
      <constraint>**BEFORE APPLYING**: Read the reference for step-by-step workflows, query syntax, and URL patterns</constraint>
    </constraint-list>
  </constraints>

  <triggers>
    <trigger-list>
        <trigger>Search Maven Central</trigger>
        <trigger>Find Maven dependency</trigger>
        <trigger>Maven coordinates</trigger>
        <trigger>groupId artifactId version</trigger>
        <trigger>Latest version Maven</trigger>
        <trigger>maven-metadata.xml</trigger>
        <trigger>Download JAR from Maven Central</trigger>
        <trigger>Download javadocs</trigger>
        <trigger>Dependency tree transitive</trigger>
        <trigger>display-dependency-updates</trigger>
        <trigger>display-plugin-updates</trigger>
        <trigger>Outdated Maven dependencies</trigger>
    </trigger-list>
  </triggers>

  <references>
    <reference-list>
        <reference>references/114-java-maven-search.md</reference>
    </reference-list>
  </references>
</prompt>
