<?xml version="1.0" encoding="UTF-8"?>
<!--
  Invalid: standard command missing required <workflow>.
  Expected failure: XSD (commands.xsd) — missing required 'workflow' in sequence
  (output/safeguards appear where workflow is required).
-->
<command id="update-issue"
         kind="standard"
         slash="/update-issue"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="../../xsd/pml/0.9.0/commands.xsd">

  <purpose>Update an existing project issue description with structured, evidence-backed content.</purpose>

  <usage><![CDATA[/update-issue <issue> [<source>] [<tracker>]]]></usage>

  <accepted-inputs>
    <input>Existing GitHub or Jira issue number, key, or URL</input>
  </accepted-inputs>

  <owning-agent>robot-business-analyst</owning-agent>

  <associations>
    <association refkind="skill">043-planning-github-issues</association>
  </associations>

  <!-- Intentionally omitted: <workflow> -->

  <output>
    <item>Updated structured issue description</item>
  </output>

  <safeguards>
    <item>Do not invent requirements, acceptance criteria, or comments.</item>
  </safeguards>
</command>
