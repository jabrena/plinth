<?xml version="1.0" encoding="UTF-8"?>
<command id="create-feature-branch"
         kind="standard"
         slash="/create-feature-branch"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="../commands.xsd">

    <purpose>Create and switch the current checkout to a conventionally named local branch for analysis, design, or implementation work.</purpose>

    <usage><![CDATA[/create-feature-branch <issue-or-change|type description> [<base-reference>]]]></usage>

    <accepted-inputs heading="Accepted Inputs">
        <input>An issue/change identifier, or an explicit branch type and description</input>
        <input>Optional base reference</input>
        <input>Supported branch types: `feat`, `fix`, `docs`, `refactor`, and `chore`</input>
    </accepted-inputs>

    <owning-agent>robot-tech-lead</owning-agent>

    <associations heading="Associated Capabilities">
        <association refkind="capability">Git branch naming and validation</association>
        <association refkind="capability">Issue and OpenSpec change traceability</association>
        <association refkind="capability">Analysis/design-to-implementation transition</association>
    </associations>

    <workflow>
        <step number="1">Resolve the branch type, issue/change identifier, and kebab-case description.</step>
        <step number="2">Verify the repository and selected base reference.</step>
        <step number="3">Resolve the repository default branch and the current branch.</step>
        <step number="4">Verify the current checkout is `main` or the repository default branch before creating the new branch.</step>
        <step number="5">If the current branch is not `main` or the default branch, stop and ask whether to switch to the default branch, choose a different base, or continue explicitly from the current branch.</step>
        <step number="6">Verify a safe working tree before changing the current checkout.</step>
        <step number="7">Stop if the proposed branch already exists or is checked out in another worktree.</step>
        <step number="8">Create and switch to the conventionally named local branch.</step>
        <step number="9">Report the branch name and base reference.</step>
    </workflow>

    <output>
        <item>Created local branch</item>
        <item>Confirmation with the exact branch name and base reference</item>
        <item>A checkout ready for plans, OpenSpec artifacts, ADRs, diagrams, documentation, or application code</item>
    </output>

    <safeguards>
        <item>Stop when uncommitted work could be displaced or mixed into the new branch.</item>
        <item>Stop and ask when the current branch is not `main` or the repository default branch.</item>
        <item>Do not overwrite, delete, or force-update an existing branch.</item>
        <item>Allow analysis and design artifacts to be committed before application-code implementation.</item>
        <item>The command does not create a commit automatically.</item>
        <item>Do not push or open a pull request automatically.</item>
    </safeguards>
</command>
