<?xml version="1.0" encoding="UTF-8"?>
<command id="close-spec"
         kind="cli"
         slash="/close-spec"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="../commands.xsd">

    <purpose>Archive an OpenSpec change by name, using the OpenSpec CLI, so completed changes can be reconciled and removed from the active change list.</purpose>

    <usage><![CDATA[/close-spec <change-name>]]></usage>

    <accepted-inputs heading="Inputs">
        <input><![CDATA[`<change-name>`: the OpenSpec change name to archive (for example `add-close-spec-command`)]]></input>
    </accepted-inputs>

    <owning-agent heading="Owner">robot-architect</owning-agent>

    <workflow>
        <step number="1"><![CDATA[Validate the argument is present.
   - If missing, print usage: `/close-spec <change-name>` and stop.]]></step>
        <step number="2"><![CDATA[Verify the OpenSpec CLI is available (for example, `openspec --version`).
   - If OpenSpec cannot be executed, report the missing prerequisite and provide a short hint to install/enable OpenSpec.]]></step>
        <step number="3"><![CDATA[Verify the change exists before attempting to archive it.
   - Prefer checking `openspec list` and matching `<change-name>`, or
   - Use `openspec show <change-name>` and treat “not found” as an unknown-change error.
   - If the change is unknown, report that the change was not found and suggest running `openspec list`.]]></step>
        <step number="4"><![CDATA[From the `documentation/` working directory, execute the equivalent of:
   - `openspec archive <change-name>`]]></step>
        <step number="5">If the archive command fails, surface the OpenSpec CLI output and stop (do not claim success).</step>
        <step number="6"><![CDATA[If the archive command succeeds, report a success message that includes `<change-name>`.]]></step>
    </workflow>

    <safeguards>
        <item>Do not guess or auto-correct the change name.</item>
        <item>Do not claim success when `openspec archive` fails; surface the CLI output.</item>
        <item>Do not run `openspec archive` unless the change exists.</item>
    </safeguards>
</command>
