# Acceptance Test Prompts for Commands

## /benchmark

```bash
execute @plinth-commands-generator/src/test/resources/gherkin/commands/benchmark.feature
and verify that acceptance-tests passes.
```

## /create-acceptance-criteria

```bash
execute @plinth-commands-generator/src/test/resources/gherkin/commands/create-acceptance-criteria.feature
and verify that acceptance-tests passes.
```

## /create-adr

```bash
execute @plinth-commands-generator/src/test/resources/gherkin/commands/create-adr.feature
and verify that acceptance-tests passes.
```

## /create-diagram

```bash
execute @plinth-commands-generator/src/test/resources/gherkin/commands/create-diagram.feature
and verify that acceptance-tests passes.
```

## /create-spec

```bash
execute @plinth-commands-generator/src/test/resources/gherkin/commands/create-spec.feature
and verify that acceptance-tests passes.
```

## /explore-design

```bash
execute @plinth-commands-generator/src/test/resources/gherkin/commands/explore-design.feature
and verify that acceptance-tests passes.
```

## /create-feature-branch

```bash
execute @plinth-commands-generator/src/test/resources/gherkin/commands/create-feature-branch.feature
and verify that acceptance-tests passes.
```

## /create-worktree

```bash
execute @plinth-commands-generator/src/test/resources/gherkin/commands/create-worktree.feature
and verify that acceptance-tests passes.
```

## /implement-spec (Spring-boot)

```bash
execute @plinth-commands-generator/src/test/resources/gherkin/commands/implement-spec.feature
and verify that acceptance-tests passes.
```

## /implement-spec (Quarkus)

```bash
execute @plinth-commands-generator/src/test/resources/gherkin/commands/implement-spec.feature
and verify that acceptance-tests passes.
Implement it but using Quarkus, not Spring boot as the default requirement.
```

## /implement-spec (Micronaut)

```bash
execute @plinth-commands-generator/src/test/resources/gherkin/commands/implement-spec.feature
and verify that acceptance-tests passes.
Implement it but using Micronaut, not Spring boot as the default requirement.
```

## /profile

```bash
execute @plinth-commands-generator/src/test/resources/gherkin/commands/profile.feature
and verify that acceptance-tests passes.
```

## /update-issue

```bash
execute @plinth-commands-generator/src/test/resources/gherkin/commands/update-issue.feature
and verify that acceptance-tests passes.
```
