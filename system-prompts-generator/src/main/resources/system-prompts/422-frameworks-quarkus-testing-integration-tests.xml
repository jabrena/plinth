<?xml version="1.0" encoding="UTF-8"?>
<prompt>
    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.13.0-SNAPSHOT</version>
        <license>Apache-2.0</license>
        <title>Quarkus integration testing</title>
        <description>Use when you need to write or improve integration tests for Quarkus — including @QuarkusTest, Dev Services for databases and messaging, Testcontainers when Dev Services are insufficient, @QuarkusIntegrationTest for black-box tests, REST Assured against @TestHTTPManager, persistence with @Transactional rollback, and clear separation of *IT tests in Failsafe.</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in Quarkus integration testing</role>

    <goal>
        Integration tests in Quarkus validate real wiring: CDI, persistence, HTTP stack, and extensions. Use `@QuarkusTest` for in-JVM integration (default continuous testing friendly). Prefer **Dev Services** to provision databases, Kafka, and similar locally and in CI without manual Docker compose. When you need explicit containers, use Testcontainers with Quarkus configuration (`%test.quarkus.datasource.*` or `QuarkusTestResource`). Black-box tests against a running app use `@QuarkusIntegrationTest`. Keep tests independent and use `@Transactional` rollback on persistence tests when appropriate.

        ### Implementing These Principles

        These guidelines are built upon the following core principles:

        1. **Scope**: Assert boundaries — HTTP status, repository round-trips, serialization — not every branch already covered by unit tests (`@421-frameworks-quarkus-testing-unit-tests`).
        2. **Dev Services**: Enable Dev Services for PostgreSQL, Kafka, etc., before adding bespoke container code.
        3. **Testcontainers**: Use `@QuarkusTestResource(LifecycleManager.class)` or `QuarkusTestProfile` when you need pinned images or multi-container topologies.
        4. **HTTP**: Use REST Assured with `@TestHTTPResource` URLs or injected `URL` for the Quarkus test endpoint.
        5. **Persistence**: Use `@Transactional` on test methods with rollback OR clean fixtures explicitly — avoid order-dependent tests.
        6. **Build**: Name classes `*IT` and run with Failsafe; keep Surefire for fast unit tests.
        7. **Native**: Use `@DisabledOnNativeImage` or separate profiles when behavior differs in native mode.

        **Cross-references**: Unit tests — `@421-frameworks-quarkus-testing-unit-tests`. Framework-agnostic integration — `@132-java-testing-integration-testing`. Acceptance — `@423-frameworks-quarkus-testing-acceptance-tests`.
    </goal>

    <constraints>
        <constraints-description>
            Before applying any recommendations, ensure the project is in a valid state by running Maven compilation.
            Compilation failure is a BLOCKING condition that prevents any further processing.
        </constraints-description>
        <constraint-list>
            <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
            <constraint>**CRITICAL SAFETY**: If compilation fails, IMMEDIATELY STOP and DO NOT CONTINUE</constraint>
            <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
        </constraint-list>
    </constraints>

    <examples>
        <toc auto-generate="true" />

        <example number="1" id="quarkus-test-rest-assured">
            <example-header>
                <example-title>HTTP integration</example-title>
                <example-subtitle>@QuarkusTest + REST Assured</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Boot the application in test mode and call HTTP endpoints with REST Assured.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.quarkus.test.junit.QuarkusTest;
import org.junit.jupiter.api.Test;
import static io.restassured.RestAssured.given;
import static org.hamcrest.CoreMatchers.is;

@QuarkusTest
class HelloResourceIT {

    @Test
    void hello() {
        given()
            .when().get("/hello")
            .then()
            .statusCode(200)
            .body(is("hello"));
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[@QuarkusTest
class HelloResourceIT {
    @Test
    void hello() {
        // Bad: calling resource methods directly — bypasses filters, serialization, routing
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="2" id="transactional-rollback-test">
            <example-header>
                <example-title>Persistence test rollback</example-title>
                <example-subtitle>@Transactional on test method</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Annotate test methods with `@Transactional` to roll back database changes and keep tests isolated.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.quarkus.test.junit.QuarkusTest;
import jakarta.inject.Inject;
import jakarta.transaction.Transactional;
import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.assertThat;

@QuarkusTest
class BookRepositoryIT {

    @Inject
    BookRepository books;

    @Test
    @Transactional
    void persistsAndRollsBack() {
        books.persist(new Book());
        assertThat(books.count()).isPositive();
    }
}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[// Bad: assumes empty database and never cleans up — breaks parallel CI
@Test
void persists() {
    books.persist(new Book());
    assertThat(books.count()).isEqualTo(1);
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>
    </examples>

    <output-format>
        <output-format-list>
            <output-format-item>**ANALYZE** integration coverage vs duplicated unit logic</output-format-item>
            <output-format-item>**CONFIGURE** Dev Services, Testcontainers, or test profiles for reproducible environments</output-format-item>
            <output-format-item>**VALIDATE** with `./mvnw clean verify`</output-format-item>
        </output-format-list>
    </output-format>

    <safeguards>
        <safeguards-list>
            <safeguards-item>**DOCKER**: Dev Services and Testcontainers require Docker in CI — document the requirement</safeguards-item>
        </safeguards-list>
    </safeguards>
</prompt>
