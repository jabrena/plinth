<?xml version="1.0" encoding="UTF-8"?>
<prompt>

    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.13.0</version>
        <license>Apache-2.0</license>
        <title>Micronaut acceptance tests from Gherkin</title>
        <description>Use when you need to implement acceptance tests from a Gherkin .feature file for Micronaut applications — including scenarios tagged @acceptance, @MicronautTest with HttpClient against the embedded server, Testcontainers wired via TestPropertyProvider, and WireMock for external REST stubs. Requires the .feature file in context.</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in Micronaut, BDD, acceptance testing, HttpClient, Testcontainers, and WireMock</role>

    <goal>
        Implement acceptance tests from Gherkin in Micronaut projects: require the `.feature` in context and confirm the stack is Micronaut (`micronaut-core`, `@MicronautTest`, `io.micronaut`); otherwise stop and point to `@133-java-testing-acceptance-tests`, `@323-frameworks-spring-boot-testing-acceptance-tests`, or `@423-frameworks-quarkus-testing-acceptance-tests`. Parse the feature, select scenarios tagged `@acceptance`, `@acceptance-tests`, or equivalent, and summarize (feature name, scenario count, steps, proposed `*AT` class name) before generating code. Boot the full app with `@MicronautTest`, assert through `@Inject @Client("/") HttpClient` (not direct controller calls), use Testcontainers for databases and Kafka the app owns, WireMock only for outbound HTTP, and merge all dynamic keys in `TestPropertyProvider.getProperties()` — never hardcode ephemeral ports. One `@Test` per scenario with `@DisplayName` mirroring the Gherkin title; map Given/When/Then to setup, HTTP exchange, assertions; happy path unless the user asks for negatives.

        **Cross-references**: Framework-agnostic acceptance — `@133-java-testing-acceptance-tests`. Micronaut unit tests — `@521-frameworks-micronaut-testing-unit-tests`. Micronaut integration tests — `@522-frameworks-micronaut-testing-integration-tests`.
    </goal>

    <constraints>
        <constraints-description>
            Before generating any code, ensure the project compiles and the Gherkin feature file is in context.
            Compilation failure is a BLOCKING condition. A missing `.feature` file is a BLOCKING condition.
        </constraints-description>
        <constraint-list>
            <constraint>**PRECONDITION**: The Gherkin `.feature` file MUST be in context — stop and ask if not provided</constraint>
            <constraint>**PRECONDITION**: The project MUST use Micronaut — otherwise direct the user to `@133`, `@323`, or `@423`</constraint>
            <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
            <constraint>**CRITICAL SAFETY**: If compilation fails, IMMEDIATELY STOP and DO NOT CONTINUE</constraint>
            <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
            <constraint>**SCOPE**: Implement only scenarios tagged `@acceptance` or `@acceptance-tests` (or equivalent)</constraint>
            <constraint>**SCOPE**: Implement only the happy path unless the user explicitly requests negative cases</constraint>
        </constraint-list>
    </constraints>

    <examples>
        <toc auto-generate="true" />

        <example number="1" id="gherkin-feature-micronaut">
            <example-header>
                <example-title>Gherkin with @acceptance</example-title>
                <example-subtitle>Same tagging convention as @133 and @323</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Scenarios must include `@acceptance` (or equivalent) to be in scope.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="gherkin"><![CDATA[Feature: Checkout API

  @acceptance
  Scenario: Place order successfully
    Given inventory is available for SKU "A1"
    When I POST to "/api/orders" with a valid payload
    Then the response status is 201]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="gherkin"><![CDATA[Feature: Checkout API

  Scenario: Place order successfully
    # Bad: missing @acceptance — skipped by this rule]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="2" id="discovery-summary-micronaut">
            <example-header>
                <example-title>Parse and confirm before coding</example-title>
                <example-subtitle>Feature file in context, Micronaut on the classpath</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Verify the `.feature` exists in context and the project is Micronaut. Read `Feature` and `Scenario` blocks, keep only `@acceptance` / `@acceptance-tests` (or equivalent), and present a short summary so the user can confirm before you generate `BaseAcceptanceTest` and `*AT` classes.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="text"><![CDATA[Feature: Checkout API — 1 acceptance scenario(s)
- Place order successfully (Given … / When … / Then …)
Proposed test class: CheckoutApiAT]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="text"><![CDATA[Bad: generating tests without a .feature in context, or for a Spring Boot project — wrong rule]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="3" id="base-acceptance-micronaut">
            <example-header>
                <example-title>BaseAcceptanceTest</example-title>
                <example-subtitle>@MicronautTest, TestPropertyProvider, Testcontainers, WireMock</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Use `@MicronautTest` with `@Property(name = "micronaut.server.port", value = "-1")` (or `0`) for a random port. Inject `@Client("/") HttpClient`. Declare static `@Container` fields for PostgreSQL, Kafka, etc. Implement `TestPropertyProvider` to supply `datasources.*` and other keys from running containers. Register `WireMockExtension` with `dynamicPort()` and `usingFilesUnderClasspath("wiremock")`; add `wireMock.baseUrl()` to `getProperties()` for outbound stub configuration. Call `wireMock.resetAll()` in `@BeforeEach` when reusing one context. Place `BaseAcceptanceTest.java` at `src/test/java/{root-package}/BaseAcceptanceTest.java`.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import com.github.tomakehurst.wiremock.junit5.WireMockExtension;
import io.micronaut.http.client.HttpClient;
import io.micronaut.http.client.annotation.Client;
import io.micronaut.test.extensions.junit5.annotation.MicronautTest;
import io.micronaut.test.support.TestPropertyProvider;
import jakarta.inject.Inject;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.extension.RegisterExtension;
import org.testcontainers.containers.PostgreSQLContainer;
import org.testcontainers.junit.jupiter.Container;
import org.testcontainers.junit.jupiter.Testcontainers;

import java.util.HashMap;
import java.util.Map;

import static com.github.tomakehurst.wiremock.core.WireMockConfiguration.wireMockConfig;

@MicronautTest
@Testcontainers
@io.micronaut.context.annotation.Property(name = "micronaut.server.port", value = "-1")
public abstract class BaseAcceptanceTest implements TestPropertyProvider {

    @Inject
    @Client("/")
    protected HttpClient client;

    @Container
    static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:16-alpine");

    @RegisterExtension
    static WireMockExtension wireMock = WireMockExtension.newInstance()
        .options(wireMockConfig().dynamicPort().usingFilesUnderClasspath("wiremock"))
        .build();

    @Override
    public Map<String, String> getProperties() {
        if (!postgres.isRunning()) {
            postgres.start();
        }
        Map<String, String> m = new HashMap<>();
        m.put("datasources.default.url", postgres.getJdbcUrl());
        m.put("datasources.default.username", postgres.getUsername());
        m.put("datasources.default.password", postgres.getPassword());
        m.put("datasources.default.driver-class-name", "org.postgresql.Driver");
        m.put("external.api.base-url", wireMock.baseUrl());
        return m;
    }

    @BeforeEach
    void resetStubs() {
        wireMock.resetAll();
    }
}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[// Bad: hard-coded jdbc:postgresql://localhost:5432/mydb — breaks CI without a local server]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="4" id="concrete-at-httpclient">
            <example-header>
                <example-title>Concrete *AT class</example-title>
                <example-subtitle>HttpClient exchange, AssertJ, Given-When-Then</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Extend `BaseAcceptanceTest`. Use `client.toBlocking().exchange(HttpRequest.*, …)` for calls. Assert status with AssertJ and body fields or JSON paths. Name classes with `AT` suffix for Failsafe (e.g. `OrderCheckoutAT`). Do not mock internal `@Singleton` services — only external HTTP via WireMock.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.HttpRequest;
import io.micronaut.http.HttpResponse;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

class OrderCheckoutAT extends BaseAcceptanceTest {

    @Test
    @DisplayName("Place order successfully")
    void placeOrderSuccessfully() {
        // Given: WireMock stubs for external.api if needed; DB state via repository or SQL if needed
        HttpResponse<String> res = client.toBlocking().exchange(
            HttpRequest.POST("/api/orders", "{\"sku\":\"A1\",\"qty\":1}")
                .header("Content-Type", "application/json"),
            String.class);
        assertThat(res.getStatus().getCode()).isEqualTo(201);
    }
}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[// Bad: injecting the controller and calling methods directly — skips HTTP, filters, and serialization]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="5" id="maven-wiremock-failsafe-micronaut">
            <example-header>
                <example-title>Maven dependencies, WireMock layout, Failsafe</example-title>
                <example-subtitle>*AT → Failsafe; mappings under classpath</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Declare test-scoped dependencies: `micronaut-test-junit5`, `micronaut-http-client` if not already present, `junit-jupiter` from Testcontainers, modules matching your stack (`postgresql`, `kafka`, …), and `wiremock-standalone`. Store WireMock JSON under `src/test/resources/wiremock/mappings/` and bodies in `src/test/resources/wiremock/__files/` when using `bodyFileName`. Configure `maven-failsafe-plugin` to include `**/*AT.java` (and `**/*IT.java` if applicable); keep `*Test` on Surefire.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="text"><![CDATA[io.micronaut.test:micronaut-test-junit5
io.micronaut:micronaut-http-client
org.testcontainers:junit-jupiter
org.testcontainers:postgresql (or kafka, …)
org.wiremock:wiremock-standalone

src/test/resources/wiremock/mappings/payment-200.json
src/test/resources/wiremock/__files/payment-response.json]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="xml"><![CDATA[<!-- Bad: failsafe only runs *IT.java but acceptance classes are named *AT.java — tests never run in verify -->]]></code-block>
                </bad-example>
            </code-examples>
        </example>
    </examples>

    <output-format>
        <output-format-list>
            <output-format-item>**SUMMARIZE** parsed acceptance scenarios before coding</output-format-item>
            <output-format-item>**GENERATE** `BaseAcceptanceTest` and concrete `*AT` classes with `HttpClient` exchanges</output-format-item>
            <output-format-item>**LIST** Maven test dependencies and WireMock files created</output-format-item>
            <output-format-item>**VALIDATE** with `./mvnw clean verify`</output-format-item>
        </output-format-list>
    </output-format>

    <safeguards>
        <safeguards-list>
            <safeguards-item>**BLOCKING**: No `.feature` file → stop</safeguards-item>
            <safeguards-item>**BLOCKING**: Project not Micronaut → redirect to `@133`, `@323`, or `@423`</safeguards-item>
            <safeguards-item>**DOCKER**: Testcontainers requires Docker on CI agents</safeguards-item>
            <safeguards-item>**ISOLATION**: Reset WireMock between tests when sharing one Micronaut context</safeguards-item>
        </safeguards-list>
    </safeguards>
</prompt>
