<?xml version="1.0" encoding="UTF-8"?>
<prompt>
    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.13.0-SNAPSHOT</version>
        <license>Apache-2.0</license>
        <title>Micronaut Data Guidelines</title>
        <description>Use when you need data access with Micronaut Data — including JDBC (and JPA where applicable) repositories, @MappedEntity design, CrudRepository and custom @Query methods, pagination with Pageable, transactions with @Transactional, immutable-friendly entities and DTO projections, optimistic locking, compile-time query validation, and test setup with @MicronautTest and TestPropertyProvider. Replaces a split JDBC + Spring Data JDBC style with a single Micronaut Data prompt (no separate raw JDBC rule in the 501 index).</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in Micronaut Data, JDBC, and Java persistence</role>

    <goal>
        Micronaut Data generates repository implementations at compile time: prefer explicit `@MappedEntity` models, `CrudRepository` / `PageableRepository` interfaces, and parameterized `@Query` for non-derivable SQL. Keep aggregates bounded, declare transactions at the service layer with `io.micronaut.transaction.annotation.Transactional`, and avoid N+1 retrieval patterns by using fetch joins or tailored queries. This prompt covers Micronaut Data for relational access (JDBC or JPA backends depending on project dependencies); use parameterized queries only — never concatenate untrusted input into SQL.

        ### Implementing These Principles

        These guidelines are built upon the following core principles:

        1. **Explicit mapping**: Use `@MappedEntity` with clear table/column naming; use `@Id` and `@GeneratedValue` appropriately for your backend.
        2. **Repository interfaces**: Extend `CrudRepository&lt;E, ID&gt;` or add pagination with `PageableRepository`; add derived query methods or `@Query` with named parameters.
        3. **Transactions**: Place `@Transactional` on `@Singleton` application services; use `readOnly = true` for queries when supported.
        4. **Immutability**: Prefer records or immutable types when your Micronaut Data version and mapping style support them; otherwise keep entities small and side-effect free outside persistence.
        5. **Pagination**: Return `Page&lt;E&gt;` for large tables; cap `Pageable` size at the service or controller boundary.
        6. **Projections**: Use interface or DTO projections for read models instead of over-fetching full entities when lists are hot paths.
        7. **Concurrency**: Use `@Version` for optimistic locking where updates collide.
        8. **Safety**: Always bind parameters in `@Query`; validate dynamic sort keys against an allow-list.
        9. **Testing**: Use `@MicronautTest` with `TestPropertyProvider` and Testcontainers for real database integration tests.

        **Cross-references**: Micronaut core — `@501-frameworks-micronaut-core`. REST boundaries — `@502-frameworks-micronaut-rest`. Integration tests — `@522-frameworks-micronaut-testing-integration-tests`. Secure SQL patterns — `@124-java-secure-coding`.

    </goal>

    <constraints>
        <constraints-description>
            Before applying any recommendations, ensure the project is in a valid state by running Maven compilation.
            Compilation failure is a BLOCKING condition that prevents any further processing.
        </constraints-description>
        <constraint-list>
            <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
            <constraint>**PREREQUISITE**: Project must compile successfully before any persistence refactoring</constraint>
            <constraint>**CRITICAL SAFETY**: If compilation fails, IMMEDIATELY STOP and DO NOT CONTINUE with any recommendations</constraint>
            <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
        </constraint-list>
    </constraints>

    <examples>
        <toc auto-generate="true" />

        <example number="1" id="mapped-entity-jdbc">
            <example-header>
                <example-title>Mapped entity</example-title>
                <example-subtitle>@MappedEntity with @Id and @GeneratedValue</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Define persistence types with `@MappedEntity`. Match column names with `@Column` when Java names differ from the schema.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.data.annotation.GeneratedValue;
import io.micronaut.data.annotation.Id;
import io.micronaut.data.annotation.MappedEntity;
import io.micronaut.data.annotation.MappedProperty;
import java.time.Instant;

@MappedEntity("customer")
public class Customer {

    @Id
    @GeneratedValue
    private Long id;

    @MappedProperty("email_address")
    private String email;

    private Instant createdAt;

    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }
    public String getEmail() { return email; }
    public void setEmail(String email) { this.email = email; }
    public Instant getCreatedAt() { return createdAt; }
    public void setCreatedAt(Instant createdAt) { this.createdAt = createdAt; }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[// Bad: missing @MappedEntity — repository cannot map reliably
public class Customer {
    private Long id;
    private String email;
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="2" id="crud-repository">
            <example-header>
                <example-title>Repository interface</example-title>
                <example-subtitle>CrudRepository and derived finders</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Declare `@io.micronaut.data.annotation.Repository` on the interface and extend `CrudRepository`. Micronaut Data implements the interface at compile time.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.data.annotation.Repository;
import io.micronaut.data.repository.CrudRepository;
import java.util.Optional;

@Repository
public interface CustomerRepository extends CrudRepository<Customer, Long> {

    Optional<Customer> findByEmail(String email);
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.data.repository.CrudRepository;

// Bad: missing @Repository — not a Micronaut Data bean
public interface CustomerRepository extends CrudRepository<Customer, Long> {
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="3" id="query-annotation">
            <example-header>
                <example-title>Custom @Query</example-title>
                <example-subtitle>Named parameters, no string concatenation</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Use `@Query` for SQL or JPQL (depending on dialect) with `:param` binding.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.data.annotation.Query;
import io.micronaut.data.annotation.Repository;
import io.micronaut.data.repository.CrudRepository;
import java.util.List;

@Repository
public interface CustomerRepository extends CrudRepository<Customer, Long> {

    @Query("SELECT * FROM customer WHERE email LIKE :pattern")
    List<Customer> searchByEmailPattern(String pattern);
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[// Anti-pattern: never build SQL with string concatenation from request data
// String sql = "SELECT * FROM customer WHERE email LIKE '%" + userInput + "%'";
// jdbcTemplate.query(sql, ...) — use @Query("... LIKE :pattern") with a bound parameter instead]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="4" id="transactional-service-data">
            <example-header>
                <example-title>Transactions</example-title>
                <example-subtitle>@Transactional on application services</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Annotate service methods that coordinate multiple writes with `@Transactional` from `io.micronaut.transaction.annotation`.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.context.annotation.Singleton;
import io.micronaut.transaction.annotation.Transactional;
import jakarta.inject.Inject;

@Singleton
public class OrderService {

    private final OrderRepository orders;
    private final OutboxRepository outbox;

    @Inject
    OrderService(OrderRepository orders, OutboxRepository outbox) {
        this.orders = orders;
        this.outbox = outbox;
    }

    @Transactional
    public Order place(Order order) {
        Order saved = orders.save(order);
        outbox.enqueue(new OrderPlacedEvent(saved.getId()));
        return saved;
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.context.annotation.Singleton;

@Singleton
public class OrderService {

    public Order place(Order order) {
        // Bad: two writes without a transaction — partial failure leaves inconsistent state
        return null;
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="5" id="pageable-repository">
            <example-header>
                <example-title>Pagination</example-title>
                <example-subtitle>Page and Pageable</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Use `PageableRepository` or methods accepting `Pageable` returning `Page&lt;T&gt;` for list endpoints backed by large tables.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.data.annotation.Repository;
import io.micronaut.data.model.Page;
import io.micronaut.data.model.Pageable;
import io.micronaut.data.repository.PageableRepository;

@Repository
public interface CustomerRepository extends PageableRepository<Customer, Long> {

    Page<Customer> findByEmailContains(String fragment, Pageable pageable);
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.data.repository.CrudRepository;
import java.util.List;

@io.micronaut.data.annotation.Repository
public interface CustomerRepository extends CrudRepository<Customer, Long> {

    // Bad: unbounded list for potentially huge table
    List<Customer> findAll();
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="6" id="dto-projection-data">
            <example-header>
                <example-title>DTO projections</example-title>
                <example-subtitle>Interface projections for reads</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Declare projection interfaces with getter signatures matching queried fields to reduce payload and mapping cost.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.data.annotation.Query;
import io.micronaut.data.annotation.Repository;
import io.micronaut.data.repository.CrudRepository;
import java.util.List;

public interface CustomerSummary {
    Long getId();
    String getEmail();
}

@Repository
public interface CustomerRepository extends CrudRepository<Customer, Long> {

    @Query("SELECT id, email_address AS email FROM customer WHERE active = true")
    List<CustomerSummary> listActiveSummaries();
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[// Bad: loading full Customer graph for a dropdown that only needs id + email
List<Customer> findAll();]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="7" id="optimistic-version">
            <example-header>
                <example-title>Optimistic locking</example-title>
                <example-subtitle>@Version column</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Add a `@Version` field when concurrent updates must conflict instead of silently overwriting.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.data.annotation.GeneratedValue;
import io.micronaut.data.annotation.Id;
import io.micronaut.data.annotation.MappedEntity;
import io.micronaut.data.annotation.Version;

@MappedEntity("inventory_item")
public class InventoryItem {

    @Id
    @GeneratedValue
    private Long id;

    private int quantity;

    @Version
    private Long version;
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[// Bad: hot row updated by two writers — last commit wins, stock incorrect
@MappedEntity("inventory_item")
public class InventoryItem {
    @Id @GeneratedValue private Long id;
    private int quantity;
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="8" id="micronaut-test-data-layer">
            <example-header>
                <example-title>Integration test wiring</example-title>
                <example-subtitle>@MicronautTest and TestPropertyProvider</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                For repository integration tests, run against a real database with Testcontainers and supply JDBC properties via `TestPropertyProvider`.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.test.extensions.junit5.annotation.MicronautTest;
import io.micronaut.test.support.TestPropertyProvider;
import org.junit.jupiter.api.Test;
import org.testcontainers.containers.PostgreSQLContainer;
import org.testcontainers.junit.jupiter.Container;
import org.testcontainers.junit.jupiter.Testcontainers;
import jakarta.inject.Inject;
import java.util.Map;

@MicronautTest(transactional = false)
@Testcontainers
class CustomerRepositoryIT implements TestPropertyProvider {

    @Container
    static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:16-alpine");

    @Inject
    CustomerRepository customers;

    @Override
    public Map<String, String> getProperties() {
        if (!postgres.isRunning()) {
            postgres.start();
        }
        return Map.of(
            "datasources.default.url", postgres.getJdbcUrl(),
            "datasources.default.username", postgres.getUsername(),
            "datasources.default.password", postgres.getPassword(),
            "datasources.default.driver-class-name", "org.postgresql.Driver"
        );
    }

    @Test
    void savesAndFinds() {
        // exercise repository against real Postgres
    }
}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[import io.micronaut.test.extensions.junit5.annotation.MicronautTest;

@MicronautTest
class CustomerRepositoryIT {
    // Bad: relies on shared developer DB on localhost — not reproducible in CI
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>
    </examples>

    <output-format>
        <output-format-list>
            <output-format-item>**ANALYZE** entities, repositories, queries, transactions, pagination, projections, and locking for Micronaut Data idioms and SQL safety</output-format-item>
            <output-format-item>**APPLY** improvements: correct annotations, repository method naming, parameterized `@Query`, transactional boundaries, bounded pagination, projections for hot reads, optimistic versioning where updates race</output-format-item>
            <output-format-item>**VALIDATE** with `./mvnw compile` before and `./mvnw clean verify` after substantive persistence changes</output-format-item>
        </output-format-list>
    </output-format>

    <safeguards>
        <safeguards-list>
            <safeguards-item>**SQL INJECTION**: Never build `@Query` strings from user input — use parameters and allow-lists for dynamic fragments</safeguards-item>
            <safeguards-item>**TRANSACTION BOUNDARIES**: Splitting writes across non-transactional calls risks partial commits</safeguards-item>
            <safeguards-item>**N+1**: Review list+lazy patterns; use fetch joins or batch queries for graphs</safeguards-item>
            <safeguards-item>**SCHEMA MIGRATIONS**: Entity changes require coordinated Flyway/Liquibase scripts — do not change `@MappedEntity` without migration</safeguards-item>
        </safeguards-list>
    </safeguards>
</prompt>
