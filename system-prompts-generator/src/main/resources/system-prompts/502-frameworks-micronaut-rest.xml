<?xml version="1.0" encoding="UTF-8"?>
<prompt>
    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.13.0</version>
        <license>Apache-2.0</license>
        <title>Micronaut REST API Guidelines</title>
        <description>Use when you need to design, review, or improve REST APIs with Micronaut — including @Controller routes, HTTP methods and status codes, request/response DTOs, Bean Validation at the boundary, exception handlers and RFC 7807-style problem responses, pagination and sorting, idempotency keys, ETag / If-Match, HTTP caching headers, API versioning, OpenAPI with Swagger, content negotiation, and security-aware boundaries.</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in REST API design and Micronaut</role>

    <goal>
        Micronaut REST endpoints should honor HTTP semantics, return accurate status codes, keep controllers thin, and expose stable DTO contracts. Use Bean Validation on incoming DTOs, centralize error mapping (including structured problem JSON when the `micronaut-problem` module is present), document APIs with OpenAPI, and apply pagination, idempotency, and concurrency controls where writes and lists matter.

        ### Implementing These Principles

        These guidelines are built upon the following core principles:

        1. **Semantic HTTP**: Map `GET`/`POST`/`PUT`/`PATCH`/`DELETE` to safe vs unsafe operations; never use `GET` for state changes.
        2. **Resource-oriented URLs**: Prefer noun-based paths (`/users`, `/users/{id}`) over RPC-style verb paths.
        3. **Status codes**: Use `200`/`201`/`204`/`400`/`404`/`409`/`412`/`422` deliberately — not `200` for every outcome.
        4. **DTO boundaries**: Do not leak persistence entities or arbitrary maps; version breaking changes explicitly.
        5. **Validation**: Annotate request DTOs with Bean Validation and mark controller parameters with `@Valid` (or equivalent) so bad input becomes **400** with field errors.
        6. **Errors**: Map domain failures to HTTP outcomes via `@Error` handlers or `ExceptionHandler` beans; prefer consistent JSON problem bodies for clients.
        7. **Lists**: Bound page size, document sort keys, and return page metadata instead of unbounded arrays by default.
        8. **Idempotency**: Support `Idempotency-Key` (or domain deduplication) for retried creates where appropriate.
        9. **Concurrency**: Use `ETag` / `If-Match` or version columns for optimistic locking on updates.
        10. **Caching**: Set `Cache-Control` and validators consciously for authenticated vs public resources.
        11. **Versioning**: Choose URI, header, or media-type strategy and apply it consistently; signal deprecation with headers when phasing endpoints out.
        12. **Time in JSON**: Prefer `Instant` or `OffsetDateTime` (ISO-8601) in API DTOs.

        **Cross-references**: Micronaut core — `@501-frameworks-micronaut-core`. Micronaut Data — `@512-frameworks-micronaut-data`. Integration tests — `@522-frameworks-micronaut-testing-integration-tests`.
    </goal>

    <constraints>
        <constraints-description>
            Before applying any recommendations, ensure the project is in a valid state by running Maven compilation.
            Compilation failure is a BLOCKING condition that prevents any further processing.
        </constraints-description>
        <constraint-list>
            <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
            <constraint>**PREREQUISITE**: Project must compile successfully before any REST API refactoring</constraint>
            <constraint>**CRITICAL SAFETY**: If compilation fails, IMMEDIATELY STOP and DO NOT CONTINUE with any recommendations</constraint>
            <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
        </constraint-list>
    </constraints>

    <examples>
        <toc auto-generate="true" />

        <example number="1" id="http-methods-micronaut">
            <example-header>
                <example-title>HTTP methods</example-title>
                <example-subtitle>Controller routes with correct verbs</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Use Micronaut `@Get`, `@Post`, `@Put`, `@Patch`, `@Delete` on `@Controller` types. Return `HttpResponse` when you need status codes beyond defaults.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.HttpResponse;
import io.micronaut.http.annotation.*;
import java.net.URI;

@Controller("/users")
public class UserController {

    private final UserService users;

    public UserController(UserService users) {
        this.users = users;
    }

    @Get("/{id}")
    public HttpResponse<UserDto> get(String id) {
        return users.find(id)
            .map(HttpResponse::ok)
            .orElseGet(HttpResponse::notFound);
    }

    @Post
    public HttpResponse<UserDto> create(@Body UserCreateDto body) {
        UserDto created = users.create(body);
        URI location = URI.create("/users/" + created.id());
        return HttpResponse.created(location).body(created);
    }

    @Delete("/{id}")
    public HttpResponse<Void> delete(String id) {
        users.delete(id);
        return HttpResponse.noContent();
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;

@Controller("/api")
public class RpcStyleController {

    @Get("/deleteUser/{id}") // Bad: unsafe state change via GET
    public String deleteUser(String id) {
        return "deleted";
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="2" id="validation-at-boundary-micronaut">
            <example-header>
                <example-title>Validation at the boundary</example-title>
                <example-subtitle>@Valid @Body on DTOs</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Apply Bean Validation annotations to DTOs and enable validation on the controller parameter with `@Valid`.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;
import io.micronaut.http.HttpResponse;
import jakarta.validation.Valid;
import jakarta.validation.constraints.Email;
import jakarta.validation.constraints.NotBlank;

public record UserCreateDto(
    @NotBlank String name,
    @Email String email
) {}

@Controller("/users")
public class UserController {

    private final UserService users;

    public UserController(UserService users) {
        this.users = users;
    }

    @Post
    public HttpResponse<UserDto> create(@Body @Valid UserCreateDto body) {
        return HttpResponse.created(java.net.URI.create("/users/x")).body(users.create(body));
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;

@Controller("/users")
public class UserController {

    @Post
    public UserDto create(@Body UserCreateDto body) {
        // Bad: no @Valid — invalid emails reach the service layer
        return null;
    }
}
record UserCreateDto(String name, String email) {}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="3" id="error-handling-micronaut">
            <example-header>
                <example-title>Centralized error mapping</example-title>
                <example-subtitle>@Error or dedicated exception handlers</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Map domain exceptions to HTTP responses in one place. When using `micronaut-problem`, emit RFC 7807-compatible JSON for machine-readable client handling.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.HttpRequest;
import io.micronaut.http.HttpResponse;
import io.micronaut.http.server.exceptions.ExceptionHandler;
import jakarta.inject.Singleton;

@Singleton
public class NotFoundHandler implements ExceptionHandler<EntityNotFoundException, HttpResponse<?>> {

    @Override
    public HttpResponse<?> handle(HttpRequest request, EntityNotFoundException exception) {
        return HttpResponse.notFound();
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;

@Controller("/orders")
public class OrderController {

    @Get("/{id}")
    public OrderDto get(String id) {
        try {
            return load(id);
        } catch (EntityNotFoundException ex) {
            // Bad: returning error payload with 200 OK
            return new OrderDto("ERROR", null);
        }
    }

    OrderDto load(String id) { throw new EntityNotFoundException(); }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="4" id="pagination-micronaut">
            <example-header>
                <example-title>Pagination</example-title>
                <example-subtitle>Pageable request and bounded page size</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Accept `page` and `size` (or cursor) query parameters with upper bounds. Return total or next-cursor metadata instead of dumping entire tables.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.data.model.Page;
import io.micronaut.data.model.Pageable;
import io.micronaut.http.annotation.*;

@Controller("/users")
public class UserController {

    private final UserService users;

    public UserController(UserService users) {
        this.users = users;
    }

    @Get
    public Page<UserDto> list(@Valid Pageable pageable) {
        // Service layer should cap size and validate sort properties
        return users.list(pageable);
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;
import java.util.List;

@Controller("/users")
public class UserController {

    @Get
    public List<UserDto> all() {
        // Bad: unbounded list — OOM and slow clients at scale
        return List.of();
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="5" id="openapi-micronaut">
            <example-header>
                <example-title>OpenAPI documentation</example-title>
                <example-subtitle>@Operation and @Tag on controllers</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Document public endpoints with Swagger annotations so generated OpenAPI stays aligned with behavior.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;

@Controller("/users")
@Tag(name = "users")
public class UserController {

    @Get("/{id}")
    @Operation(summary = "Get user by id")
    public UserDto get(String id) {
        return null;
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;

@Controller("/users")
public class UndocumentedController {

    @Get("/{id}")
    // Bad: no OpenAPI metadata — consumers rely on guesswork
    public UserDto get(String id) {
        return null;
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="6" id="content-type-json">
            <example-header>
                <example-title>Content negotiation</example-title>
                <example-subtitle>Prefer JSON with explicit produces/consumes when needed</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Default JSON is typical; when versioning via media types, keep `produces`/`consumes` consistent across the controller surface.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.MediaType;
import io.micronaut.http.annotation.*;

@Controller("/users")
@Produces(MediaType.APPLICATION_JSON)
@Consumes(MediaType.APPLICATION_JSON)
public class UserController {

    @Get("/{id}")
    public UserDto get(String id) {
        return null;
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;

@Controller("/users")
public class UserController {

    @Get(value = "/{id}", produces = {"application/json", "application/xml"})
    // Bad: multiple representations without tests or client contract
    public UserDto get(String id) {
        return null;
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="7" id="security-boundary">
            <example-header>
                <example-title>Security at the boundary</example-title>
                <example-subtitle>@Secured or security rules on routes</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Declare authentication/authorization on sensitive routes. Do not rely on obscurity or client-side-only checks.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;
import io.micronaut.security.annotation.Secured;
import io.micronaut.security.rules.SecurityRule;

@Controller("/admin")
@Secured(SecurityRule.IS_AUTHENTICATED)
public class AdminController {

    @Get("/metrics")
    public String metrics() {
        return "ok";
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;

@Controller("/admin")
public class OpenAdminController {

    @Get("/metrics")
    // Bad: sensitive endpoint without Micronaut Security annotations
    public String metrics() {
        return "secrets";
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="8" id="idempotency-post">
            <example-header>
                <example-title>Idempotent creates</example-title>
                <example-subtitle>Idempotency-Key header handling</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                For POST that must survive retries, accept an idempotency key, persist it with the outcome, and return the same response for duplicates.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.HttpResponse;
import io.micronaut.http.annotation.*;

@Controller("/payments")
public class PaymentController {

    private final PaymentService payments;

    public PaymentController(PaymentService payments) {
        this.payments = payments;
    }

    @Post
    public HttpResponse<PaymentDto> pay(
            @Header("Idempotency-Key") String key,
            @Body @jakarta.validation.Valid PaymentRequest body) {
        return payments.payOnce(key, body);
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;

@Controller("/payments")
public class PaymentController {

    @Post
    public PaymentDto pay(@Body PaymentRequest body) {
        // Bad: duplicate POST on retry may double-charge
        return null;
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="9" id="etag-if-match">
            <example-header>
                <example-title>Optimistic concurrency</example-title>
                <example-subtitle>ETag and If-Match</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Return weak or strong ETags for versioned resources; require `If-Match` on mutating requests when clients must detect stale writes.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.HttpHeaders;
import io.micronaut.http.HttpResponse;
import io.micronaut.http.annotation.*;

@Controller("/items")
public class ItemController {

    private final ItemService items;

    public ItemController(ItemService items) {
        this.items = items;
    }

    @Put("/{id}")
    public HttpResponse<ItemDto> update(
            String id,
            @Header(HttpHeaders.IF_MATCH) String ifMatch,
            @Body ItemUpdateDto body) {
        return items.updateIfMatch(id, ifMatch, body);
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;

@Controller("/items")
public class ItemController {

    @Put("/{id}")
    public ItemDto update(String id, @Body ItemUpdateDto body) {
        // Bad: last writer wins — lost updates under concurrency
        return null;
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="10" id="cache-control-micronaut">
            <example-header>
                <example-title>HTTP caching headers</example-title>
                <example-subtitle>Cache-Control for public vs private responses</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Set caching headers deliberately. Personalized or authorized responses usually need `private` or `no-store`.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.HttpResponse;
import io.micronaut.http.MutableHttpResponse;
import io.micronaut.http.annotation.*;

@Controller("/catalog")
public class CatalogController {

    @Get("/products/{id}")
    public MutableHttpResponse<ProductDto> get(String id) {
        return HttpResponse.ok(load(id))
            .header("Cache-Control", "public, max-age=60");
    }

    ProductDto load(String id) { return null; }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;

@Controller("/me")
public class ProfileController {

    @Get
    public UserProfileDto me() {
        // Bad: default caching might cache authenticated profile at shared proxies
        return null;
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="11" id="api-versioning-uri">
            <example-header>
                <example-title>API versioning</example-title>
                <example-subtitle>Stable /v1 prefix</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Place version in the path or negotiate via headers/media types consistently. Avoid silent breaking changes on unversioned URLs used in production.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="text"><![CDATA[GET /api/v1/users/{id}
POST /api/v1/users]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="text"><![CDATA[GET /users/{id}   (breaking field renames without version)
GET /getUserV2/{id} (inconsistent versioning style)]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="12" id="time-dtos-instant">
            <example-header>
                <example-title>Time in DTOs</example-title>
                <example-subtitle>Instant or OffsetDateTime in JSON</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Prefer ISO-8601-friendly types in API records; avoid `java.util.Date` in new contracts.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import java.time.Instant;

public record EventDto(String id, Instant occurredAt) {}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[import java.util.Date;

public record EventDto(String id, Date occurredAt) {} // Bad: legacy type, timezone pitfalls]]></code-block>
                </bad-example>
            </code-examples>
        </example>
    </examples>

    <output-format>
        <output-format-list>
            <output-format-item>**ANALYZE** controllers for HTTP semantics, validation, error mapping, pagination, security annotations, caching, versioning, and OpenAPI coverage</output-format-item>
            <output-format-item>**CATEGORIZE** findings by client impact (breaking vs safe) and by concern (validation, errors, performance)</output-format-item>
            <output-format-item>**APPLY** improvements: correct status codes, `@Valid` DTOs, centralized exception handling, bounded list endpoints, idempotency for retried writes, ETag/If-Match where updates collide, explicit security rules, cache headers, documented operations</output-format-item>
            <output-format-item>**VALIDATE** with `./mvnw compile` before and `./mvnw clean verify` after substantive edits</output-format-item>
        </output-format-list>
    </output-format>

    <safeguards>
        <safeguards-list>
            <safeguards-item>**BLOCKING SAFETY CHECK**: Run `./mvnw compile` before ANY REST refactoring</safeguards-item>
            <safeguards-item>**SECURITY**: Never return stack traces or secrets in JSON error bodies in production</safeguards-item>
            <safeguards-item>**COMPATIBILITY**: Changing status codes or DTO shapes breaks clients — use versioning or deprecation headers</safeguards-item>
            <safeguards-item>**INCREMENTAL SAFETY**: Ship error-handler and validation changes with integration tests covering representative failures</safeguards-item>
        </safeguards-list>
    </safeguards>
</prompt>
