<?xml version="1.0" encoding="UTF-8"?>
<prompt>
    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.13.0</version>
        <license>Apache-2.0</license>
        <title>Micronaut Core Guidelines</title>
        <description>Use when you need to review, improve, or build Micronaut applications — including application bootstrap with Micronaut.run, @Singleton/@Prototype scopes, @Factory bean producers, constructor injection with jakarta.inject, @ConfigurationProperties and @Property, environments and @Requires, @Controller vs application services, AOP interceptors, @Scheduled tasks, graceful shutdown, virtual-thread friendly execution, health endpoints, and test-oriented bean design.</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in Micronaut, dependency injection, and Java enterprise development</role>

    <goal>
        Micronaut favors compile-time dependency injection, fast startup, and explicit configuration. Effective applications keep `main` thin, use `@Singleton` for stateless services, declare beans with `@Factory` when construction is non-trivial, bind settings with `@ConfigurationProperties`, branch environments with Micronaut environments and `@Requires` instead of scattered `if` checks, and use Jakarta APIs consistently. Operational concerns include Netty graceful shutdown, scheduling with bounded error handling, and optional `@ExecuteOn` / virtual threads for blocking work off the event loop.
    </goal>

    <constraints>
        <constraints-description>
            Before applying any recommendations, ensure the project is in a valid state by running Maven compilation.
            Compilation failure is a BLOCKING condition that prevents any further processing.
        </constraints-description>
        <constraint-list>
            <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
            <constraint>**PREREQUISITE**: Project must compile successfully and pass basic validation checks before any Micronaut refactoring</constraint>
            <constraint>**CRITICAL SAFETY**: If compilation fails, IMMEDIATELY STOP and DO NOT CONTINUE with any recommendations</constraint>
            <constraint>**BLOCKING CONDITION**: Compilation errors must be resolved by the user before proceeding with Micronaut core improvements</constraint>
            <constraint>**NO EXCEPTIONS**: Under no circumstances should Micronaut recommendations be applied to a project that fails to compile</constraint>
            <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
        </constraint-list>
    </constraints>

    <examples>
        <toc auto-generate="true" />

        <example number="1" id="micronaut-main-bootstrap">
            <example-header>
                <example-title>Application bootstrap</example-title>
                <example-subtitle>Thin main with Micronaut.run</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Start the embedded Netty server and application context with `Micronaut.run`. Do not embed use-case orchestration or long-running business logic in `main`.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.runtime.Micronaut;

public final class Application {

    public static void main(String[] args) {
        Micronaut.run(Application.class, args);
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.runtime.Micronaut;

public final class Application {

    public static void main(String[] args) {
        // Bad: business work in main — untestable and couples startup to domain
        new OrderBacklogProcessor().drainAllQueues();
        Micronaut.run(Application.class, args);
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="2" id="singleton-application-service">
            <example-header>
                <example-title>Application services</example-title>
                <example-subtitle>@Singleton with constructor injection</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Model application services as `@Singleton` types with final dependencies injected via constructor. Keeps beans immutable-friendly and easy to unit test without the full HTTP stack.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.context.annotation.Singleton;
import jakarta.inject.Inject;

@Singleton
public class OrderService {

    private final OrderRepository orders;
    private final PaymentClient payments;

    @Inject
    public OrderService(OrderRepository orders, PaymentClient payments) {
        this.orders = orders;
        this.payments = payments;
    }

    public Order place(OrderRequest request) {
        payments.authorize(request.amount());
        return orders.save(Order.from(request));
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.context.annotation.Singleton;
import jakarta.inject.Inject;

@Singleton
public class OrderService {

    @Inject
    OrderRepository orders; // field injection — harder to test and reason about

    @Inject
    PaymentClient payments;

    public Order place(OrderRequest request) {
        return orders.save(Order.from(request));
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="3" id="factory-beans">
            <example-header>
                <example-title>Bean factories</example-title>
                <example-subtitle>@Factory for non-trivial construction</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Use `@Factory` when you integrate third-party types or need explicit setup. Annotate producer methods with `@Singleton` (or another scope) and keep them focused.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.context.annotation.Bean;
import io.micronaut.context.annotation.Factory;
import io.micronaut.context.annotation.Singleton;
import java.net.http.HttpClient;
import java.time.Duration;

@Factory
public class HttpClientFactory {

    @Bean
    @Singleton
    HttpClient httpClient() {
        return HttpClient.newBuilder()
            .connectTimeout(Duration.ofSeconds(5))
            .build();
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.context.annotation.Factory;

@Factory
public class GodFactory {
    // Bad: one factory owns unrelated beans — split by integration boundary
    @io.micronaut.context.annotation.Bean
    @io.micronaut.context.annotation.Singleton
    Object kafka() { return null; }

    @io.micronaut.context.annotation.Bean
    @io.micronaut.context.annotation.Singleton
    Object redis() { return null; }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="4" id="configuration-properties-micronaut">
            <example-header>
                <example-title>Type-safe configuration</example-title>
                <example-subtitle>@ConfigurationProperties with records or beans</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Group related keys under a prefix with `@ConfigurationProperties`. Prefer records or immutable-friendly types; validate with Bean Validation when invalid config must fail fast at startup.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.context.annotation.ConfigurationProperties;
import jakarta.validation.constraints.Positive;

@ConfigurationProperties("app.batch")
public class BatchSettings {

    @Positive
    private int maxItems = 10;

    public int getMaxItems() {
        return maxItems;
    }

    public void setMaxItems(int maxItems) {
        this.maxItems = maxItems;
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.context.annotation.Value;
import jakarta.inject.Singleton;

@Singleton
public class BatchProcessor {

    @Value("${app.batch.max-items:10}")
    int maxItems; // scattered magic keys — prefer @ConfigurationProperties grouping
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="5" id="environments-and-requires">
            <example-header>
                <example-title>Environments and conditional beans</example-title>
                <example-subtitle>@Requires and micronaut.environments</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Encode environment differences with Micronaut environments (`micronaut.environments`) in `application.yml` / launch config, and `@Requires` on beans. Avoid `System.getenv` branching inside domain services.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.context.annotation.Requires;
import io.micronaut.context.annotation.Singleton;

@Singleton
@Requires(env = "dev")
public class DevDiagnosticsPublisher {
    public void emit(String message) { /* noop or console in dev */ }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.context.annotation.Singleton;

@Singleton
public class FeatureService {

    public boolean enabled() {
        // Bad: environment logic in domain — untestable without env hacks
        return "prod".equalsIgnoreCase(System.getenv("ENV"));
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="6" id="controller-as-adapter">
            <example-header>
                <example-title>HTTP controllers</example-title>
                <example-subtitle>Delegate to services; keep controllers thin</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                `@Controller` types should map HTTP to DTOs and call application services. Avoid embedding persistence or complex rules directly in controllers.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;
import io.micronaut.http.HttpResponse;
import jakarta.inject.Inject;

@Controller("/api/orders")
public class OrderController {

    private final OrderService orderService;

    @Inject
    public OrderController(OrderService orderService) {
        this.orderService = orderService;
    }

    @Post
    public HttpResponse<OrderDto> create(@Body OrderCreateDto body) {
        OrderDto created = orderService.create(body);
        return HttpResponse.created(java.net.URI.create("/api/orders/" + created.id())).body(created);
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;
import jakarta.inject.Inject;

@Controller("/api/orders")
public class FatOrderController {

    @Inject
    OrderRepository repo; // Bad: controller talks to persistence directly

    @Post
    public OrderDto create(@Body OrderCreateDto body) {
        return repo.save(new Order(null, body.productId(), body.qty())).toDto();
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="7" id="scheduled-tasks-micronaut">
            <example-header>
                <example-title>Scheduled tasks</example-title>
                <example-subtitle>@Scheduled on @Singleton beans</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Use `@Scheduled` with explicit fixed delay or cron strings. Log or metric failures so silent breakage is visible.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.context.annotation.Singleton;
import io.micronaut.scheduling.annotation.Scheduled;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import jakarta.inject.Inject;

@Singleton
public class OutboxDispatcher {

    private static final Logger LOG = LoggerFactory.getLogger(OutboxDispatcher.class);
    private final OutboxRepository outbox;

    @Inject
    OutboxDispatcher(OutboxRepository outbox) {
        this.outbox = outbox;
    }

    @Scheduled(fixedDelay = "30s")
    void flush() {
        try {
            outbox.dispatchBatch();
        } catch (Exception ex) {
            LOG.warn("outbox flush failed", ex);
        }
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.context.annotation.Singleton;
import io.micronaut.scheduling.annotation.Scheduled;

@Singleton
public class SilentJob {

    @Scheduled(fixedDelay = "5s")
    void tick() {
        throw new IllegalStateException(); // Bad: unhandled — may suppress further runs without visibility
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="8" id="blocking-off-event-loop">
            <example-header>
                <example-title>Blocking work and the event loop</example-title>
                <example-subtitle>@ExecuteOn for blocking I/O</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Long-running blocking calls on the default Netty thread reduce throughput. Offload with `@ExecuteOn` (e.g. `TaskExecutors.BLOCKING`) or configure virtual-thread executors when your stack supports them.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.context.annotation.Singleton;
import io.micronaut.scheduling.TaskExecutors;
import io.micronaut.scheduling.annotation.ExecuteOn;
import io.micronaut.http.annotation.*;

@Controller("/reports")
@Singleton
public class ReportController {

    private final ReportGenerator generator;

    public ReportController(ReportGenerator generator) {
        this.generator = generator;
    }

    @Get("/{id}")
    @ExecuteOn(TaskExecutors.BLOCKING)
    public byte[] download(String id) {
        return generator.buildPdfBlocking(id);
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import io.micronaut.http.annotation.*;

@Controller("/reports")
public class BlockingOnEventLoopController {

    @Get("/{id}")
    public byte[] download(String id) {
        // Bad: heavy blocking PDF generation on event-loop thread
        return ReportGenerator.buildPdfBlocking(id);
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="9" id="graceful-shutdown-micronaut">
            <example-header>
                <example-title>Graceful shutdown</example-title>
                <example-subtitle>Netty server shutdown timeouts</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Configure graceful shutdown so active requests can finish. Tune parent/quiet period properties for your SLA; document values for operators.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="yaml"><![CDATA[micronaut:
  server:
    netty:
      graceful-shutdown-quiet-period: 2s
      graceful-shutdown-timeout: 20s]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="yaml"><![CDATA[# Bad: no graceful settings — in-flight requests may be cut on SIGTERM in K8s
micronaut:
  server:
    port: 8080]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="10" id="interceptor-cross-cutting">
            <example-header>
                <example-title>CDI-style interceptors</example-title>
                <example-subtitle>@InterceptorBinding for cross-cutting behavior</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Attach logging, auditing, or timing with Micronaut AOP interceptors instead of duplicating boilerplate in every service method.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import io.micronaut.aop.Around;
import io.micronaut.aop.InterceptorBean;
import io.micronaut.aop.MethodInterceptor;
import io.micronaut.aop.MethodInvocationContext;
import jakarta.inject.Singleton;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@Singleton
@InterceptorBean(Timed.class)
public class TimedInterceptor implements MethodInterceptor<Object, Object> {

    private static final Logger LOG = LoggerFactory.getLogger(TimedInterceptor.class);

    @Override
    public Object intercept(MethodInvocationContext<Object, Object> context) {
        long t0 = System.nanoTime();
        try {
            return context.proceed();
        } finally {
            LOG.debug("{} took {} ms", context.getMethodName(),
                (System.nanoTime() - t0) / 1_000_000);
        }
    }
}]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="java"><![CDATA[import io.micronaut.context.annotation.Singleton;

@Singleton
public class OrderService {

    public Order place(OrderRequest r) {
        long t0 = System.nanoTime();
        try {
            return doPlace(r);
        } finally {
            // Bad: duplicated timing in every method instead of an interceptor
            System.out.println("place took " + (System.nanoTime() - t0));
        }
    }

    private Order doPlace(OrderRequest r) { return null; }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>
    </examples>

    <output-format>
        <output-format-list>
            <output-format-item>**ANALYZE** the Micronaut codebase for bootstrap, bean scopes, factory usage, configuration binding, environment and `@Requires` usage, controller thickness, scheduling, event-loop blocking risk, shutdown settings, and Jakarta vs legacy javax imports</output-format-item>
            <output-format-item>**CATEGORIZE** findings by impact (startup, throughput, maintainability) and layer (config, beans, HTTP, scheduling)</output-format-item>
            <output-format-item>**APPLY** improvements: thin `main`, constructor injection, grouped `@ConfigurationProperties`, conditional beans with `@Requires`, delegate controllers to services, add `@ExecuteOn` for blocking paths, tune graceful shutdown, extract cross-cutting code to interceptors</output-format-item>
            <output-format-item>**IMPLEMENT** incrementally with compiling steps between edits</output-format-item>
            <output-format-item>**EXPLAIN** trade-offs (e.g., prototype vs singleton, blocking executor choice) when multiple valid options exist</output-format-item>
            <output-format-item>**VALIDATE** with `./mvnw compile` before and `./mvnw clean verify` after substantive edits</output-format-item>
        </output-format-list>
    </output-format>

    <safeguards>
        <safeguards-list>
            <safeguards-item>**BLOCKING SAFETY CHECK**: Run `./mvnw compile` before ANY Micronaut refactoring</safeguards-item>
            <safeguards-item>**CRITICAL VALIDATION**: Run `./mvnw clean verify` after changes</safeguards-item>
            <safeguards-item>**THREADING**: Misplaced blocking I/O causes latency under load — verify hot paths after adding `@ExecuteOn` or executor changes</safeguards-item>
            <safeguards-item>**CONFIG**: Strict Bean Validation on configuration types fails startup when keys are missing — align all environments</safeguards-item>
            <safeguards-item>**JAKARTA NAMESPACE**: Mixing `javax.*` and `jakarta.*` injection or validation annotations breaks wiring — standardize on `jakarta.*`</safeguards-item>
            <safeguards-item>**INCREMENTAL SAFETY**: Apply scope and factory changes in small steps with compile verification between steps</safeguards-item>
        </safeguards-list>
    </safeguards>
</prompt>
