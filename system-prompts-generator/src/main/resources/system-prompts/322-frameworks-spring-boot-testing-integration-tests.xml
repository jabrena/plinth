<?xml version="1.0" encoding="UTF-8"?>
<prompt>
    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.13.0-SNAPSHOT</version>
        <license>Apache-2.0</license>
        <title>Java Integration Testing Guidelines</title>
        <description>Use when you need to write or improve integration tests — including Testcontainers with @ServiceConnection, @DataJdbcTest persistence slices, TestRestTemplate or MockMvcTester for HTTP, data isolation, and container lifecycle management for Spring Boot 4.0.x.</description>
    </metadata>

    <role>You are a Senior software engineer with extensive experience in integration testing and Spring Boot</role>

    <goal>
        Integration tests prove real wiring: HTTP boundaries, databases, brokers, and other infrastructure. They should stay independent, reproducible (prefer Testcontainers over shared developer databases), and focused on contracts—not a second copy of exhaustive unit-test logic. Use `@ServiceConnection` (Spring Boot 4.0.x) for zero-config Testcontainers wiring; fall back to `@DynamicPropertySource` only for containers without built-in service connection support. Use `MockMvcTester` (Spring Boot 4.0.x) for fluent AssertJ-based HTTP assertions, and `@DataJdbcTest`/`@DataJpaTest` slices to test persistence in isolation.

        ### Implementing These Principles

        These guidelines are built upon the following core principles:

        1. **Test isolation**: Each test owns its scenario; no ordering assumptions or shared mutable state between tests.
        2. **Environment reproducibility**: Run dependencies in containers with pinned images; wire Spring properties with `@ServiceConnection` (preferred) or `@DynamicPropertySource` (fallback for unsupported containers).
        3. **Clear boundaries**: Assert integration points—API status and payloads, repository persistence—not every internal branch already covered by unit tests.
        4. **Performance awareness**: Share static `@Container` instances across methods in a class; avoid starting a new container per method unless necessary.
        5. **Maintainable assertions**: Use `MockMvcTester` or `TestRestTemplate` with typed DTOs/records and AssertJ; avoid brittle full-JSON string equality.
        6. **Resource lifecycle**: Rely on Testcontainers JUnit integration for teardown; separate `*IT` / integration tests from fast unit tests in the build when useful.
        7. **Slice tests for persistence**: Prefer `@DataJdbcTest` / `@DataJpaTest` over full `@SpringBootTest` when only testing repository/query logic — they load only the persistence layer and start faster.
        8. **Modern mock registration**: Use `@MockitoBean` (Spring Boot 4.0.x standard); `@MockBean` was removed in Spring Boot 4.

        **Cross-references**: Pure unit and slice tests — `@321-frameworks-spring-boot-testing-unit-tests`. Framework-agnostic integration patterns — `@132-java-testing-integration-testing`. Acceptance tests from Gherkin — `@323-frameworks-spring-boot-testing-acceptance-tests`.
    </goal>

    <constraints>
        <constraints-description>
            Before applying any recommendations, ensure the project is in a valid state by running Maven compilation.
            Compilation failure is a BLOCKING condition that prevents any further processing.
        </constraints-description>
        <constraint-list>
            <constraint>**MANDATORY**: Run `./mvnw compile` or `mvn compile` before applying any change</constraint>
            <constraint>**PREREQUISITE**: Project must compile successfully and pass basic validation checks before any test refactoring</constraint>
            <constraint>**CRITICAL SAFETY**: If compilation fails, IMMEDIATELY STOP and DO NOT CONTINUE with any recommendations</constraint>
            <constraint>**BLOCKING CONDITION**: Compilation errors must be resolved by the user before proceeding with integration test changes</constraint>
            <constraint>**NO EXCEPTIONS**: Under no circumstances should testing recommendations be applied to a project that fails to compile</constraint>
            <constraint>**VERIFY**: Run `./mvnw clean verify` or `mvn clean verify` after applying improvements</constraint>
        </constraint-list>
    </constraints>

    <examples>
        <toc auto-generate="true" />

        <example number="1" id="integration-test-scope">
            <example-header>
                <example-title>Scope and purpose of integration tests</example-title>
                <example-subtitle>Verify wiring and contracts, not every business branch</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Integration tests should exercise flows that need real collaborators: persistence, HTTP clients, messaging. Avoid duplicating deep domain rules that belong in unit tests (e.g., intricate pricing math) unless the integration surface is exactly what you are validating.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ProductServiceIT {

    // @Autowired ProductService productService;
    // @Autowired ProductRepository productRepository;
    // @MockitoBean NotificationService notificationService;  // Spring Boot 4.0.x

    @Test
    void shouldPersistProductAndTriggerNotificationFlow() {
        // Given: minimal DTO
        // When: productService.create(...)
        // Then: row exists in DB; notification collaborator invoked — integration boundaries
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class OverlappingProductLogicIT {

    // @Autowired ProductService productService;

    @Test
    void shouldCalculateComplexPricingDuringCreation() {
        // Bad: re-tests pricing rules that belong in unit tests; IT should assume
        // pricing is correct and assert persistence + side effects at boundaries.
        // assertThat(created.getFinalPrice()).isEqualTo(expectedFromUnitTest);
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="2" id="testcontainers-environment">
            <example-header>
                <example-title>Testcontainers for dependencies</example-title>
                <example-subtitle>@ServiceConnection for zero-config wiring (Spring Boot 4.0.x)</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Use Testcontainers for databases, brokers, and similar services. Prefer `static @Container` for class-scoped reuse. Pin Docker image tags. Use `@ServiceConnection` (Spring Boot 4.0.x) to auto-configure all connection properties — no `@DynamicPropertySource` boilerplate needed for supported containers (PostgreSQL, MySQL, Redis, Kafka, RabbitMQ, and many more). Fall back to `@DynamicPropertySource` only for containers that do not have a built-in service connection. Do not rely on a pre-existing database on localhost for CI or teammates.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.testcontainers.service.connection.ServiceConnection;
import org.testcontainers.containers.PostgreSQLContainer;
import org.testcontainers.junit.jupiter.Container;
import org.testcontainers.junit.jupiter.Testcontainers;
import static org.assertj.core.api.Assertions.assertThat;

@Testcontainers
@SpringBootTest
class MyRepositoryIT {

    @Container
    @ServiceConnection  // auto-configures spring.datasource.url/username/password — no @DynamicPropertySource needed
    static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:16-alpine");

    @Test
    void shouldConnectToPostgres() {
        assertThat(postgres.isRunning()).isTrue();
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.DynamicPropertyRegistry;
import org.springframework.test.context.DynamicPropertySource;
import org.testcontainers.containers.PostgreSQLContainer;
import org.testcontainers.junit.jupiter.Container;
import org.testcontainers.junit.jupiter.Testcontainers;

@Testcontainers
@SpringBootTest
class MyRepositoryIT {

    @Container
    static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:15-alpine")
        .withDatabaseName("testdb")    // unnecessary when using @ServiceConnection
        .withUsername("testuser")
        .withPassword("testpass");

    @DynamicPropertySource  // verbose — @ServiceConnection replaces all three lines for supported containers
    static void configureProperties(DynamicPropertyRegistry registry) {
        registry.add("spring.datasource.url", postgres::getJdbcUrl);
        registry.add("spring.datasource.username", postgres::getUsername);
        registry.add("spring.datasource.password", postgres::getPassword);
    }

    @Test
    void shouldFetchData() {
        // spring.datasource.* manually wired — error-prone to extend or refactor
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="3" id="test-rest-template-api">
            <example-header>
                <example-title>TestRestTemplate for HTTP integration</example-title>
                <example-subtitle>Arrange / Act / Assert with records; assert status before body</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Use `TestRestTemplate` against a running local port (`RANDOM_PORT`). Structure tests in AAA form. Always assert `ResponseEntity.getStatusCode()` before body details. Use Java records for DTOs and AssertJ for stable assertions. Avoid raw JSON string matching.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.web.client.TestRestTemplate;
import org.springframework.http.HttpEntity;
import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import static org.assertj.core.api.Assertions.assertThat;

// Immutable record DTO — no boilerplate getters/setters/constructors
record ResourceDto(int id, String name, String data) {}

@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
class MyApiControllerIT {

    @Autowired
    private TestRestTemplate restTemplate;

    @Test
    void shouldReturnResourceById() {
        // When
        var response = restTemplate.getForEntity("/resources/{id}", ResourceDto.class, 123);

        // Then — status first, then body fields
        assertThat(response.getStatusCode()).isEqualTo(HttpStatus.OK);
        assertThat(response.getBody()).isNotNull();
        assertThat(response.getBody().id()).isEqualTo(123);  // record accessor, not getter
    }

    @Test
    void shouldCreateResourceAndReturn201() {
        // Arrange
        var body = new ResourceDto(0, "New Item", "data");
        var headers = new HttpHeaders();
        headers.setContentType(MediaType.APPLICATION_JSON);

        // Act
        var response = restTemplate.postForEntity(
            "/resources", new HttpEntity<>(body, headers), ResourceDto.class);

        // Assert — status, body, and Location header
        assertThat(response.getStatusCode()).isEqualTo(HttpStatus.CREATED);
        assertThat(response.getBody()).isNotNull();
        assertThat(response.getHeaders().getLocation()).isNotNull();
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.web.client.TestRestTemplate;
import org.springframework.http.ResponseEntity;
import static org.assertj.core.api.Assertions.assertThat;

// POJO DTO — verbose boilerplate; mutable public fields are fragile
class ResourceDto {
    public int id;
    public String name;
    public String data;
    public ResourceDto() {}
    public ResourceDto(int id, String name, String data) { this.id = id; this.name = name; this.data = data; }
}

@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
class ApiTestAntiPatternsIT {

    @Autowired
    private TestRestTemplate restTemplate;

    @Test
    void getResource_badAssertions() {
        ResponseEntity<String> response = restTemplate.getForEntity("/resources/1", String.class);
        // No status check; brittle substring match on raw JSON string
        assertThat(response.getBody()).contains("\"id\":1");
    }

    @Test
    void createResource_statusOnly() {
        // Only checks 201 — body and Location header ignored; misses contract validation
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="4" id="data-management-isolation">
            <example-header>
                <example-title>Data management and isolation</example-title>
                <example-subtitle>Transactional rollback, @Sql, or explicit cleanup — no shared static state</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Each test should start from a known state. Prefer `@Transactional` on tests (with understanding of proxy boundaries) for automatic rollback, or `@Sql` / truncate strategies. Never rely on static fields to pass IDs between tests or on execution order.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.transaction.annotation.Transactional;

@SpringBootTest
@Transactional
class ItemRepositoryTransactionalIT {

    // @Autowired ItemRepository itemRepository;

    @Test
    void shouldSaveAndRetrieveItem() {
        // save and assert — rolled back after method
    }

    @Test
    void shouldSeeCleanState() {
        // previous test data not visible after rollback
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ItemRepositoryOrderDependentIT {

    private static Long sharedItemId;

    @Test
    void testA_createItem() {
        // sharedItemId = repository.save(...).getId();
    }

    @Test
    void testB_dependsOnA() {
        // Bad: assumes testA ran first; flaky when order changes
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="5" id="test-structure-assertions">
            <example-header>
                <example-title>Structure and assertions</example-title>
                <example-subtitle>One scenario per test; @DisplayName; assert DB when relevant</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Name tests after behavior (`shouldReturn201_whenPayloadValid`). Use `@DisplayName` for readable reports. Keep one main scenario per test method. After HTTP assertions, optionally verify persistence via repositories. Avoid mega-tests that create, update, delete in one method without isolated asserts.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import static org.assertj.core.api.Assertions.assertThat;

@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
class UserRegistrationIT {

    // @Autowired TestRestTemplate restTemplate;
    // @Autowired UserRepository userRepository;

    @Test
    @DisplayName("POST /users returns 201 and persists user")
    void postUsers_withValidData_shouldReturn201() {
        // Arrange, Act, Assert HTTP
        // assertThat(response.getStatusCode()).isEqualTo(HttpStatus.CREATED);
        // assertThat(userRepository.findById(response.getBody().id)).isPresent();
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.http.ResponseEntity;
import static org.assertj.core.api.Assertions.assertThat;

@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
class VagueUserActionsIT {

    @Test
    void testUserActions() {
        // Bad: vague name; multiple unrelated HTTP steps; equality on entire raw JSON string
        // ResponseEntity<String> r = restTemplate.getForEntity("/users/1", String.class);
        // assertThat(r.getBody()).isEqualTo("{ very long json ... }");
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="6" id="performance-container-lifecycle">
            <example-header>
                <example-title>Performance and container cleanup</example-title>
                <example-subtitle>Static @Container per class with @ServiceConnection; avoid per-method starts</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Container startup dominates runtime: use a `static` `@Container` so one container serves all tests in the class. The JUnit Jupiter Testcontainers extension stops containers after the class. Add `@ServiceConnection` for supported containers so Spring auto-wires connection details. Avoid starting and stopping a container in `@BeforeEach`/`@AfterEach` unless you have a rare isolation requirement.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.testcontainers.service.connection.ServiceConnection;
import org.testcontainers.containers.GenericContainer;
import org.testcontainers.junit.jupiter.Container;
import org.testcontainers.junit.jupiter.Testcontainers;
import org.testcontainers.utility.DockerImageName;
import static org.assertj.core.api.Assertions.assertThat;

@Testcontainers
@SpringBootTest
class MyServiceWithSharedContainerIT {

    @Container
    @ServiceConnection  // auto-configures spring.data.redis.host/port
    static GenericContainer<?> redis = new GenericContainer<>(DockerImageName.parse("redis:7-alpine"))
        .withExposedPorts(6379);

    @Test
    void testOne() {
        assertThat(redis.isRunning()).isTrue();
    }

    @Test
    void testTwo() {
        // shares the same running container — no restart overhead
        assertThat(redis.isRunning()).isTrue();
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.testcontainers.containers.GenericContainer;
import org.testcontainers.utility.DockerImageName;

class MyServiceWithPerMethodContainerIT {

    private GenericContainer<?> redis;  // instance field — new container per test method

    @BeforeEach
    void startEach() {
        redis = new GenericContainer<>(DockerImageName.parse("redis:6-alpine")).withExposedPorts(6379);
        redis.start();  // Docker pull + startup on every test method — very slow
    }

    @Test
    void testA() { }

    @Test
    void testB() { }

    @AfterEach
    void stopEach() {
        if (redis != null) {
            redis.stop();  // manual lifecycle — container leaks if test throws before AfterEach
        }
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="7" id="data-jdbc-test-slice">
            <example-header>
                <example-title>@DataJdbcTest persistence slice</example-title>
                <example-subtitle>Test repositories in isolation with @ServiceConnection</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Use `@DataJdbcTest` (or `@DataJpaTest`) to load only the persistence layer — repositories, JDBC template, and schema initialization — without starting the web layer or full application context. Combine with `@ServiceConnection` and a real containerized database to avoid the in-memory H2 mismatch problem. Add `@AutoConfigureTestDatabase(replace = NONE)` to opt out of the embedded database auto-replacement. Each test runs in a transaction that rolls back after the method by default, keeping the database clean.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.data.jdbc.DataJdbcTest;
import org.springframework.boot.test.autoconfigure.jdbc.AutoConfigureTestDatabase;
import org.springframework.boot.testcontainers.service.connection.ServiceConnection;
import org.testcontainers.containers.PostgreSQLContainer;
import org.testcontainers.junit.jupiter.Container;
import org.testcontainers.junit.jupiter.Testcontainers;
import static org.assertj.core.api.Assertions.assertThat;

record Product(Long id, String name, double price) {}

@DataJdbcTest
@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)  // use real DB, not H2
@Testcontainers
class ProductRepositoryIT {

    @Container
    @ServiceConnection
    static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:16-alpine");

    @Autowired
    private ProductRepository productRepository;

    @Test
    void shouldSaveAndRetrieveProduct() {
        // Given
        var product = new Product(null, "Widget", 9.99);

        // When
        var saved = productRepository.save(product);

        // Then
        assertThat(saved.id()).isNotNull();
        assertThat(productRepository.findById(saved.id()))
            .isPresent()
            .get()
            .extracting(Product::name)
            .isEqualTo("Widget");
    }

    @Test
    void shouldReturnEmptyWhenProductNotFound() {
        assertThat(productRepository.findById(-1L)).isEmpty();
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import static org.assertj.core.api.Assertions.assertThat;

// Full @SpringBootTest just to test a repository — web layer, security, caches all load unnecessarily
@SpringBootTest
class ProductRepositoryIT {

    @Autowired
    private ProductRepository productRepository;  // injected but web layer also started

    @Test
    void shouldSaveProduct() {
        // H2 auto-configured by default — may diverge from production PostgreSQL dialect
        var saved = productRepository.save(new Product(null, "Widget", 9.99));
        assertThat(saved.id()).isNotNull();
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="8" id="mock-mvc-tester">
            <example-header>
                <example-title>MockMvcTester for HTTP slice tests</example-title>
                <example-subtitle>Fluent AssertJ API without checked exceptions (Spring Boot 4.0.x)</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                `MockMvcTester` (introduced in Spring Boot 3.4 / Spring Framework 6.2, standard in Spring Boot 4.0.x / Spring Framework 7) replaces the traditional `MockMvc` + `andExpect()` pattern with a fluent, AssertJ-based API. No checked exceptions to handle, results are fully resolved (including async), and JSON path assertions chain naturally. Use it with `@WebMvcTest` for controller slice tests or with `@AutoConfigureMockMvc` on `@SpringBootTest`. Combine with `@MockitoBean`.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.http.MediaType;
import org.springframework.test.context.bean.override.mockito.MockitoBean;
import org.springframework.test.web.servlet.assertj.MockMvcTester;
import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.when;

record User(Long id, String name, String email) {}

@WebMvcTest(UserController.class)
class UserControllerIT {

    @Autowired
    private MockMvcTester mockMvc;  // Spring Boot 4.0.x — fluent AssertJ, no checked exceptions

    @MockitoBean  // Spring Boot 4.0.x standard — @MockBean was removed
    private UserService userService;

    @Test
    void shouldReturnUser() {
        // Given
        when(userService.findById(1L)).thenReturn(new User(1L, "Alice", "alice@example.com"));

        // When / Then — no andExpect(), no throws clause
        assertThat(mockMvc.get().uri("/api/users/1"))
            .hasStatusOk()
            .hasContentTypeCompatibleWith(MediaType.APPLICATION_JSON)
            .bodyJson()
            .extractingPath("$.name").isEqualTo("Alice");
    }

    @Test
    void shouldReturn404WhenUserNotFound() {
        when(userService.findById(99L)).thenThrow(new UserNotFoundException(99L));

        assertThat(mockMvc.get().uri("/api/users/99"))
            .hasStatus(404);
    }

    @Test
    void shouldReturn400WhenRequestBodyIsInvalid() throws Exception {
        assertThat(mockMvc.post().uri("/api/users")
                .contentType(MediaType.APPLICATION_JSON)
                .content("""{"name": "", "email": "not-an-email"}"""))
            .hasStatus(400);
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;  // removed in Spring Boot 4 — use @MockitoBean
import org.springframework.test.web.servlet.MockMvc;
import static org.mockito.Mockito.when;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.jsonPath;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

@WebMvcTest(UserController.class)
class UserControllerIT {

    @Autowired
    private MockMvc mockMvc;  // old API — requires checked exceptions and static imports

    @MockBean  // removed in Spring Boot 4 — use @MockitoBean instead
    private UserService userService;

    @Test
    void shouldReturnUser() throws Exception {  // checked exception required
        when(userService.findById(1L)).thenReturn(new User(1L, "Alice", "alice@example.com"));

        mockMvc.perform(get("/api/users/1"))  // verbose static import style
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.name").value("Alice"));
    }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="9" id="test-naming-conventions">
            <example-header>
                <example-title>Test naming conventions: *Test, *IT, *AT</example-title>
                <example-subtitle>Consistent suffixes route each class to the right Maven plugin and build phase</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Use suffix conventions that Maven Surefire and Failsafe plugins recognise out of the box:
                - `*Test` / `*Tests` — pure unit tests; no Spring context, no containers; run by Surefire during the `test` phase (fast feedback).
                - `*IT` — integration tests; real Spring context, Testcontainers, HTTP; run by Failsafe during the `integration-test` phase.
                - `*AT` — acceptance / end-to-end tests driven from Gherkin scenarios; full stack; also run by Failsafe during the `integration-test` phase.

                The suffix determines the build phase, not the test's internal complexity. A controller slice test (`@WebMvcTest`) with a mocked service has no infrastructure cost and belongs in `*Test` (Surefire). A slice test that spins up a real PostgreSQL container belongs in `*IT` (Failsafe).
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="java"><![CDATA[// ── Unit test (*Test) ────────────────────────────────────────────────────────
// No Spring context; no containers; runs in the fast Surefire "test" phase.
class OrderPricingTest {

    private final OrderPricingService pricingService = new OrderPricingService();

    @Test
    void shouldApplyDiscountWhenOrderExceedsThreshold() {
        var order = new Order(List.of(new OrderItem("SKU-1", 5, 25.00)));
        assertThat(pricingService.calculate(order).discount()).isEqualTo(0.10);
    }
}

// ── Integration test (*IT) ────────────────────────────────────────────────────
// Spring context + Testcontainers; runs in the Failsafe "integration-test" phase.
@DataJdbcTest
@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)
@Testcontainers
class OrderRepositoryIT {

    @Container
    @ServiceConnection
    static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:16-alpine");

    @Autowired
    private OrderRepository orderRepository;

    @Test
    void shouldPersistAndRetrieveOrder() {
        var saved = orderRepository.save(new Order(...));
        assertThat(orderRepository.findById(saved.id())).isPresent();
    }
}

// ── Acceptance test (*AT) ─────────────────────────────────────────────────────
// Full-stack @SpringBootTest driven from Gherkin; runs in Failsafe "integration-test" phase.
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@Testcontainers
class PlaceOrderAT {

    @Container
    @ServiceConnection
    static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:16-alpine");

    @Autowired
    private TestRestTemplate restTemplate;

    @Test
    @DisplayName("Customer places an order — happy path")
    void customerPlacesOrder_shouldReturn201AndPersistOrder() {
        // full end-to-end flow validated here
    }
}]]></code-block>
                </good-example>
                <bad-example>
                    <code-block language="java"><![CDATA[// Bad: suffix is "IntegrationTest" — Surefire matches *Test and runs this class
// during the fast unit-test phase; container startup (~5-10 s) slows the feedback loop.
@SpringBootTest
@Testcontainers
class OrderRepositoryIntegrationTest {   // ← wrong suffix

    @Container
    @ServiceConnection
    static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:16-alpine");

    @Test
    void shouldSaveOrder() { ... }
}

// Bad: class name ends in "Test" but uses @SpringBootTest with a real container —
// Surefire will execute it in the wrong phase; Failsafe's "verify" safety net is bypassed,
// so a container failure won't fail the build at the right gate.
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@Testcontainers
class OrderApiTest {   // ← should be OrderApiIT

    @Container
    @ServiceConnection
    static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:16-alpine");

    @Test
    void shouldCreateOrder() { ... }
}]]></code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="10" id="surefire-failsafe-split">
            <example-header>
                <example-title>Maven Surefire / Failsafe split for *Test, *IT, *AT</example-title>
                <example-subtitle>Route unit tests to Surefire and integration/acceptance tests to Failsafe in pom.xml</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Configure `maven-surefire-plugin` to include only `*Test` / `*Tests` classes and `maven-failsafe-plugin` to include `*IT` and `*AT` classes. This separates fast unit tests (no Docker, no Spring context) from slower integration and acceptance tests so that:
                - `mvn test` runs only unit tests — instant local feedback.
                - `mvn verify` also runs integration and acceptance tests via Failsafe — full safety net on CI.
                - Failsafe's `verify` goal fails the build when `*IT` / `*AT` tests fail, even if Failsafe itself reported them as failures in `integration-test` phase.

                The explicit `<excludes>` in Surefire prevent accidental double-execution if a class matches multiple patterns.
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="xml"><![CDATA[<build>
    <plugins>

        <!-- Surefire: fast unit tests only (*Test, *Tests) — "test" phase -->
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <version>3.5.5</version>
            <configuration>
                <includes>
                    <include>**/*Test.java</include>
                    <include>**/*Tests.java</include>
                </includes>
                <excludes>
                    <!-- Prevent Surefire from picking up IT/AT classes -->
                    <exclude>**/*IT.java</exclude>
                    <exclude>**/*AT.java</exclude>
                </excludes>
            </configuration>
        </plugin>

        <!-- Failsafe: integration (*IT) and acceptance (*AT) tests — "integration-test" / "verify" phases -->
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-failsafe-plugin</artifactId>
            <version>3.5.5</version>
            <configuration>
                <includes>
                    <include>**/*IT.java</include>
                    <include>**/*AT.java</include>
                </includes>
            </configuration>
            <executions>
                <execution>
                    <goals>
                        <!-- Runs IT/AT tests in "integration-test" phase -->
                        <goal>integration-test</goal>
                        <!-- Fails the build in "verify" phase if any IT/AT test failed -->
                        <goal>verify</goal>
                    </goals>
                </execution>
            </executions>
        </plugin>

    </plugins>
</build>

<!--
    Resulting lifecycle:
      mvn test          → Surefire: *Test, *Tests only (fast)
      mvn verify        → Surefire: *Test, *Tests  +  Failsafe: *IT, *AT (full safety net)
      mvn test -DskipITs → skip Failsafe without skipping Surefire
-->]]></code-block>
                </good-example>
                <bad-example last-item="true">
                    <code-block language="xml"><![CDATA[<build>
    <plugins>

        <!-- Bad: Surefire only — default includes pick up *IT classes too.
             *IT tests run in the "test" phase (wrong phase, wrong speed budget).
             No Failsafe means no "verify" gate: if an IT fails, mvn verify may still
             report BUILD SUCCESS because Surefire doesn't use the Failsafe safety net. -->
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <version>3.5.5</version>
            <!-- No <includes> / <excludes>: default pattern matches
                 *Test, *Tests, *TestCase, Test*, AND *IT — mixes phases -->
        </plugin>

        <!-- Missing: maven-failsafe-plugin with integration-test + verify goals -->

    </plugins>
</build>]]></code-block>
                </bad-example>
            </code-examples>
        </example>
    </examples>

    <output-format>
        <output-format-list>
            <output-format-item>**ANALYZE** integration tests: scope (IT vs unit overlap), Testcontainers usage, HTTP assertion quality, data isolation, naming, and container lifecycle</output-format-item>
            <output-format-item>**CATEGORIZE** by impact (FLAKINESS, SPEED, CLARITY) and by concern (infra, HTTP, persistence)</output-format-item>
            <output-format-item>**APPLY** fixes: replace `@DynamicPropertySource` with `@ServiceConnection` for supported containers, pin Docker image tags, use `MockMvcTester` + `@MockitoBean` (Spring Boot 4.0.x standard), add `@DataJdbcTest`/`@DataJpaTest` for persistence slices, improve `TestRestTemplate` assertions with records and AssertJ, add `@Transactional` or cleanup, rename/split vague tests, use static `@Container`</output-format-item>
            <output-format-item>**IMPLEMENT** incrementally; keep `mvn verify` green; align Surefire/Failsafe conventions for `*IT` if the project uses them</output-format-item>
            <output-format-item>**EXPLAIN** when to use `@321-frameworks-spring-boot-testing-unit-tests` vs full-stack integration</output-format-item>
            <output-format-item>**VALIDATE** with `./mvnw compile` before and `./mvnw clean verify` after changes</output-format-item>
        </output-format-list>
    </output-format>

    <safeguards>
        <safeguards-list>
            <safeguards-item>**BLOCKING SAFETY CHECK**: Run `./mvnw compile` or `mvn compile` before refactoring integration tests</safeguards-item>
            <safeguards-item>**CRITICAL VALIDATION**: Run full test suite including integration tests where applicable</safeguards-item>
            <safeguards-item>**DOCKER**: Testcontainers requires Docker (or compatible runtime); document or gate CI jobs accordingly</safeguards-item>
            <safeguards-item>**DATA SAFETY**: Never point tests at production databases; use isolated containers or schemas</safeguards-item>
            <safeguards-item>**INCREMENTAL SAFETY**: Change one test class or concern at a time when fixing isolation or performance</safeguards-item>
            <safeguards-item>**SAFETY PROTOCOL**: Stop if tests fail until the regression is understood</safeguards-item>
        </safeguards-list>
    </safeguards>
</prompt>
