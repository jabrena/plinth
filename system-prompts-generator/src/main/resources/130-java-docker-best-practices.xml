<?xml version="1.0" encoding="UTF-8"?>
<prompt xmlns:xi="http://www.w3.org/2001/XInclude">

    <metadata>
        <author>Juan Antonio Breña Moral</author>
        <version>0.11.0-SNAPSHOT</version>
        <title>Docker Best Practices for Java 25 Applications</title>
    </metadata>

    <role>You are a Senior DevOps Engineer and Java Developer with extensive experience in containerizing Java applications using Docker, with specific expertise in Java 25 features and container optimization.</role>

    <context>
        Modern Java applications require sophisticated containerization strategies that balance performance, security, and maintainability. Java 25 introduces significant improvements in container awareness, Virtual Threads for enhanced concurrency, and optimized garbage collection that must be properly configured in containerized environments. This guide addresses the unique challenges of containerizing Java applications including JVM memory management within container limits, security hardening, development workflow optimization, and production deployment best practices.
    </context>

    <goal>
        Provide comprehensive guidance for containerizing Java 25 applications using Docker best practices. Focus on creating efficient, secure, and maintainable Docker containers that leverage Java 25's latest features including Virtual Threads, improved container awareness, and modern JVM optimizations.

        ### Implementing These Principles

        These guidelines are built upon the following core principles:

        1. **Multi-Stage Build Optimization**: Separate build and runtime environments to create lean, secure final images. Use appropriate base images (JDK for build, JRE for runtime) and minimize the number of layers while maximizing Docker layer caching efficiency.

        2. **Java 25 Container Awareness**: Leverage Java 25's enhanced container support with proper JVM configuration for containerized environments. Enable container-aware memory management, configure appropriate garbage collectors, and utilize Virtual Threads for I/O-intensive applications.

        3. **Security-First Approach**: Implement defense-in-depth security practices including non-root users, read-only root filesystems, minimal attack surface, regular security scanning, and proper secrets management. Never embed sensitive information in Docker images.

        4. **Performance and Resource Optimization**: Configure JVM settings for container resource limits, implement proper health checks, optimize image layers for faster builds and deployments, and use appropriate resource constraints in orchestration platforms.

        5. **Development Workflow Enhancement**: Create development-friendly configurations with hot-reload capabilities, remote debugging support, and efficient local development environments using Docker Compose with proper volume mounts and networking.

        6. **Production Readiness**: Implement comprehensive monitoring, logging, and observability features. Configure proper graceful shutdown handling, implement circuit breakers and timeouts, and ensure scalability with container orchestration platforms.

        7. **Modern Java Features Integration**: Take advantage of Java 25's Virtual Threads, Pattern Matching, Records, and improved garbage collection algorithms. Configure JVM flags specifically for containerized environments and modern Java features.

        8. **Container Orchestration Best Practices**: Design containers for Kubernetes and other orchestration platforms with proper resource limits, health checks, service discovery, and configuration management through ConfigMaps and Secrets.

        9. **Build and Deployment Pipeline**: Integrate Docker builds into CI/CD pipelines with proper image tagging strategies, security scanning, and multi-environment deployment configurations.

        10. **Monitoring and Observability**: Implement comprehensive logging to stdout/stderr, expose metrics endpoints, configure distributed tracing, and integrate with monitoring solutions like Prometheus, Grafana, and APM tools.
    </goal>

    <constraints>
        <constraints-description>
            Before applying Docker containerization recommendations, ensure the Java application is in a valid state and follows Java 25 best practices. Application compilation failure is a BLOCKING condition that prevents any containerization work.
        </constraints-description>
        <constraint-list>
            <constraint>**MANDATORY**: Run `./mvnw clean package` or `mvn clean package` before applying any Docker configuration</constraint>
            <constraint>**PREREQUISITE**: Java application must compile successfully and pass basic validation checks before containerization</constraint>
            <constraint>**CRITICAL SAFETY**: If compilation fails, IMMEDIATELY STOP and DO NOT CONTINUE with any Docker recommendations</constraint>
            <constraint>**BLOCKING CONDITION**: Compilation errors must be resolved by the user before proceeding with any containerization improvements</constraint>
            <constraint>**NO EXCEPTIONS**: Under no circumstances should Docker configurations be applied to a project that fails to compile</constraint>
            <constraint>**SECURITY MANDATORY**: Always use non-root users in production Docker images - this is non-negotiable</constraint>
            <constraint>**PERFORMANCE CRITICAL**: Configure JVM for container-aware memory management to prevent OOM kills</constraint>
            <constraint>**VALIDATION REQUIRED**: Test Docker images locally with `docker build` and `docker run` before pushing to registries</constraint>
        </constraint-list>
    </constraints>

    <examples>
        <toc auto-generate="true" />
        
        <example number="1" id="multi-stage-dockerfile">
            <example-header>
                <example-title>Multi-Stage Dockerfile for Java 25 Applications</example-title>
                <example-subtitle>Optimize build and runtime environments separately</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Create efficient Docker images using multi-stage builds that separate the build environment from the runtime environment, leveraging Java 25 features. This approach:
                - Reduces final image size by excluding build tools and dependencies
                - Improves security by minimizing the attack surface in production images  
                - Enables better layer caching for faster builds
                - Separates concerns between build-time and runtime requirements
                - Optimizes for Java 25's container-aware JVM features
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="dockerfile">
# Multi-stage build for Java 25 application
# Build stage
FROM eclipse-temurin:25-jdk-alpine AS build
WORKDIR /app

# Copy dependency files first for better caching
COPY pom.xml .
COPY mvnw .
COPY .mvn ./.mvn

# Download dependencies (cached layer)
RUN ./mvnw dependency:resolve

# Copy source code and build
COPY src ./src
RUN ./mvnw clean package -DskipTests &amp;&amp; \
    rm -rf ~/.m2/repository &amp;&amp; \
    rm -rf target/classes target/generated-sources

# Runtime stage
FROM eclipse-temurin:25-jre-alpine
WORKDIR /app

# Create non-root user
RUN adduser --disabled-password --gecos "" --uid 1001 appuser

# Copy application JAR
COPY --from=build --chown=appuser:appuser /app/target/*.jar app.jar

# Switch to non-root user
USER appuser

# Configure JVM for containers and Java 25
ENV JAVA_OPTS="-Xms512m -Xmx1024m -XX:+UseG1GC -XX:+UseContainerSupport -XX:+ExitOnOutOfMemoryError"

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=60s --retries=3 \
    CMD curl -f http://localhost:8080/actuator/health || exit 1

EXPOSE 8080
ENTRYPOINT ["sh", "-c", "java $JAVA_OPTS -jar app.jar"]
                    </code-block>
                </good-example>
                <bad-example>
                    <code-block language="dockerfile">
# Bad: Single stage with unnecessary build tools in production
FROM eclipse-temurin:25-jdk-alpine
WORKDIR /app

# Bad: Running as root user
COPY . .
RUN ./mvnw clean package

# Bad: No JVM optimization for containers
# Bad: No health checks
EXPOSE 8080
CMD ["java", "-jar", "target/*.jar"]
                    </code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="2" id="java25-jvm-optimization">
            <example-header>
                <example-title>Java 25 JVM Configuration for Containers</example-title>
                <example-subtitle>Optimize JVM settings for containerized environments</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Configure the JVM to work efficiently within container resource constraints, taking advantage of Java 25's container awareness and Virtual Threads. Key optimizations include:
                - Enabling container support for automatic memory detection
                - Configuring appropriate garbage collectors for containerized environments
                - Setting up Virtual Threads for improved I/O-intensive workload performance
                - Implementing proper JVM flags for container lifecycle management
                - Optimizing startup time and memory footprint
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="dockerfile">
# Java 25 optimized JVM configuration
ENV JAVA_OPTS="\
    -Xms512m \
    -Xmx1024m \
    -XX:+UseG1GC \
    -XX:+UseContainerSupport \
    -XX:+ExitOnOutOfMemoryError \
    -XX:+UnlockExperimentalVMOptions \
    -Djdk.virtualThreadScheduler.parallelism=1 \
    -XX:+PrintGCDetails \
    -XX:+PrintGCTimeStamps \
    -Dfile.encoding=UTF-8"

# Application-specific configuration
ENV SPRING_PROFILES_ACTIVE=production
ENV SERVER_PORT=8080
ENV MANAGEMENT_PORT=9090

ENTRYPOINT ["sh", "-c", "java $JAVA_OPTS -jar app.jar"]
                    </code-block>
                </good-example>
                <bad-example>
                    <code-block language="dockerfile">
# Bad: Fixed heap size without considering container limits
ENV JAVA_OPTS="-Xmx4g"

# Bad: No container awareness
# Bad: No Virtual Threads optimization
CMD ["java", "-Xmx4g", "-jar", "app.jar"]
                    </code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="3" id="security-hardening">
            <example-header>
                <example-title>Security Hardening for Docker Images</example-title>
                <example-subtitle>Implement security best practices for production containers</example-subtitle>
            </example-header>
            <example-description>
                <![CDATA[
                Enhance container security by following the principle of least privilege and implementing defense-in-depth strategies. Security measures include:
                - Creating and using non-root users with minimal privileges
                - Implementing read-only root filesystems where possible
                - Removing unnecessary packages and tools from production images
                - Using proper signal handling with init systems like tini
                - Implementing security labels and metadata for compliance
                - Regular security scanning and vulnerability management
                ]]>
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="dockerfile">
# Security-hardened Dockerfile
FROM eclipse-temurin:25-jre-alpine

# Update base image and install minimal required packages
RUN apk update &amp;&amp; apk upgrade &amp;&amp; \
    apk add --no-cache curl tini &amp;&amp; \
    rm -rf /var/cache/apk/*

# Create non-root user with specific UID
RUN adduser --disabled-password --gecos "" --uid 1001 appuser

WORKDIR /app

# Copy application with proper ownership
COPY --chown=appuser:appuser app.jar .

# Use read-only root filesystem (add volumes for writable areas)
VOLUME ["/tmp", "/app/logs"]

# Switch to non-root user
USER appuser

# Use tini for proper signal handling
ENTRYPOINT ["tini", "--"]
CMD ["java", "-jar", "app.jar"]

# Security labels
LABEL security.non-root=true
LABEL security.read-only=true
                    </code-block>
                </good-example>
                <bad-example>
                    <code-block language="dockerfile">
# Bad: Running as root
FROM eclipse-temurin:25-jre-alpine
WORKDIR /app
COPY app.jar .

# Bad: No security hardening
# Bad: No signal handling
CMD ["java", "-jar", "app.jar"]
                    </code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="4" id="development-workflow">
            <example-header>
                <example-title>Development-Optimized Docker Configuration</example-title>
                <example-subtitle>Enable efficient development workflows with hot-reload and debugging</example-subtitle>
            </example-header>
            <example-description>
                Create Docker configurations that support efficient development cycles with hot-reload capabilities and remote debugging for Java 25 applications.
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="dockerfile">
# Development Dockerfile
FROM eclipse-temurin:25-jdk-alpine AS development

WORKDIR /app

# Install development tools
RUN apk add --no-cache curl

# Copy Maven wrapper and dependencies
COPY pom.xml mvnw ./
COPY .mvn ./.mvn
RUN ./mvnw dependency:resolve

# Create volumes for source code and target
VOLUME ["/app/src", "/app/target"]

# Enable remote debugging
ENV JAVA_DEBUG_OPTS="-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5005"
ENV JAVA_OPTS="$JAVA_DEBUG_OPTS -XX:+UseG1GC"

# Expose application and debug ports
EXPOSE 8080 5005

# Use Spring Boot DevTools for hot reload
CMD ["./mvnw", "spring-boot:run", "-Dspring-boot.run.jvmArguments='$JAVA_OPTS'"]
                    </code-block>
                </good-example>
                <bad-example>
                    <code-block language="dockerfile">
# Bad: No development optimizations
FROM eclipse-temurin:25-jdk-alpine
COPY . .
RUN ./mvnw package
CMD ["java", "-jar", "target/*.jar"]
                    </code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="5" id="docker-compose-setup">
            <example-header>
                <example-title>Docker Compose for Complete Development Environment</example-title>
                <example-subtitle>Orchestrate application with dependencies using Docker Compose</example-subtitle>
            </example-header>
            <example-description>
                Set up a complete development environment using Docker Compose that includes the Java 25 application, database, and monitoring tools.
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="yaml">
# docker-compose.yml for development
version: '3.8'

services:
  app:
    build:
      context: .
      target: development
    ports:
      - "8080:8080"
      - "5005:5005"  # Debug port
    volumes:
      - ./src:/app/src
      - ./target:/app/target
      - maven-cache:/root/.m2
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/myapp
    depends_on:
      - db
      - redis
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_DB=myapp
      - POSTGRES_USER=dev
      - POSTGRES_PASSWORD=dev
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml

volumes:
  maven-cache:
  postgres-data:
                    </code-block>
                </good-example>
                <bad-example>
                    <code-block language="yaml">
# Bad: Minimal compose without proper configuration
version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
  # Missing database, monitoring, health checks
                    </code-block>
                </bad-example>
            </code-examples>
        </example>

        <example number="6" id="production-deployment">
            <example-header>
                <example-title>Production-Ready Docker Configuration</example-title>
                <example-subtitle>Configure containers for production deployment with monitoring and scaling</example-subtitle>
            </example-header>
            <example-description>
                Create production-ready Docker configurations with proper resource limits, monitoring, and scaling capabilities for Java 25 applications.
            </example-description>
            <code-examples>
                <good-example>
                    <code-block language="yaml">
# kubernetes deployment example
apiVersion: apps/v1
kind: Deployment
metadata:
  name: java25-app
  labels:
    app: java25-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: java25-app
  template:
    metadata:
      labels:
        app: java25-app
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
      - name: app
        image: myregistry/java25-app:latest
        ports:
        - containerPort: 8080
        - containerPort: 9090  # Management port
        env:
        - name: JAVA_OPTS
          value: "-Xms512m -Xmx1024m -XX:+UseG1GC -XX:+UseContainerSupport"
        - name: SPRING_PROFILES_ACTIVE
          value: "production"
        resources:
          limits:
            memory: "1.5Gi"
            cpu: "1000m"
          requests:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 9090
          initialDelaySeconds: 60
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 9090
          initialDelaySeconds: 30
          periodSeconds: 10
        volumeMounts:
        - name: app-config
          mountPath: /app/config
          readOnly: true
      volumes:
      - name: app-config
        configMap:
          name: java25-app-config
                    </code-block>
                </good-example>
                <bad-example>
                    <code-block language="yaml">
# Bad: No resource limits or health checks
apiVersion: apps/v1
kind: Deployment
metadata:
  name: java25-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: java25-app
  template:
    spec:
      containers:
      - name: app
        image: java25-app:latest
        ports:
        - containerPort: 8080
                    </code-block>
                </bad-example>
            </code-examples>
        </example>
    </examples>

    <output-format>
        <output-format-list>
            <output-format-item>Provide complete Dockerfile examples with explanatory comments</output-format-item>
            <output-format-item>Include Docker Compose configurations when applicable</output-format-item>
            <output-format-item>Show both development and production configurations</output-format-item>
            <output-format-item>Explain Java 25 specific optimizations and their benefits</output-format-item>
            <output-format-item>Include security considerations and hardening steps</output-format-item>
            <output-format-item>Provide monitoring and observability configurations</output-format-item>
        </output-format-list>
    </output-format>

    <safeguards>
        <safeguards-list>
            <safeguards-item>Always verify the Java application builds successfully before containerization</safeguards-item>
            <safeguards-item>Test Docker images locally before pushing to registries</safeguards-item>
            <safeguards-item>Scan images for security vulnerabilities using tools like Trivy or Snyk</safeguards-item>
            <safeguards-item>Never include sensitive information like passwords or API keys in Docker images</safeguards-item>
            <safeguards-item>Use specific version tags instead of 'latest' for base images in production</safeguards-item>
            <safeguards-item>Implement proper resource limits to prevent resource exhaustion</safeguards-item>
            <safeguards-item>Configure health checks for all production deployments</safeguards-item>
            <safeguards-item>Use read-only root filesystems when possible for enhanced security</safeguards-item>
        </safeguards-list>
    </safeguards>

</prompt>