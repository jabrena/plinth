# PostgreSQL Sakila Database

This example starts a local PostgreSQL database with the Sakila schema and sample film data.

## How to run in local?

```bash
docker compose up
docker compose up -d
docker compose ps
docker compose logs -f sakiladb
docker compose down
docker compose down -v
```
